---
phase: 08-improvements
plan: 07
type: execute
wave: 4
depends_on: [08-01, 08-03]
files_modified:
  - backend/main.py
  - backend/schemas.py
  - backend/tests/test_refeicoes.py
  - backend/tests/test_planejamento.py
  - backend/tests/test_alunos_por_periodo.py
autonomous: true
requirements:
  - IMP-09
  - IMP-10
  - IMP-11
  - MEAL-02

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Admin configura quantidades de alunos por período (manha/tarde/noite); cozinheira não edita (D-14)."
    - "O total de cada slot é derivado: Lanche Manhã=manhã, Almoço=manhã+tarde, Lanche Tarde=tarde, Janta=noite (D-15)."
    - "O lançamento recebe o slot (não o tipo) e o backend deriva tipo e qtd_alunos da configuração vigente; refeicao.qtd_alunos grava o valor configurado no momento (D-16, D-16b)."
    - "O planejamento não bloqueia por estoque: GET /planejamento/projecao simula a semana cumulativamente e POST /planejamento retorna avisos aditivos (D-17, D-18)."
    - "Item sem conversão aparece como 'não avaliável' na projeção, sem quebrar (D-17)."
  artifacts:
    - path: "backend/main.py"
      provides: "GET/PUT /alunos-por-periodo; _total_por_slot; POST /refeicoes por slot; GET /planejamento/projecao; avisos no POST /planejamento"
    - path: "backend/schemas.py"
      provides: "AlunosPorPeriodoUpdate/Response; RefeicaoCreate com slot (sem qtd_alunos/tipo_refeicao)"
    - path: "backend/tests/test_alunos_por_periodo.py"
      provides: "Testes de configuração (permissões, validação, atualização)"
    - path: "backend/tests/test_refeicoes.py"
      provides: "R1-R15 revisados para payload por slot + novos R16..R18 (derivação, config ausente, avulso)"
    - path: "backend/tests/test_planejamento.py"
      provides: "Testes da projeção (configurado, ruptura, não avaliável, config ausente) e avisos"
  key_links:
    - from: "backend/main.py"
      to: "backend/models.py"
      via: "AlunosPorPeriodo consultado em _total_por_slot"
    - from: "backend/main.py"
      to: "backend/main.py"
      via: "_converter_para_unidade_oficial reutilizada na simulação da projeção"
---

<objective>
Backend da configuração de alunos por período, lançamento de refeição por slot (cozinheira sem digitar alunos) e projeção cumulativa de estoque no planejamento com avisos não-bloqueantes.

Purpose: Satisfazer IMP-09/10/11 e a revisão MEAL-02, mantendo o bloqueio por estoque no lançamento (`main.py:939-943`) e o contrato de GET /planejamento (R-3: projeção em endpoint próprio).
Output: Endpoints e regras testados; suíte R*/planejamento verde.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-RESEARCH.md
@.planning/phases/08-improvements/08-PATTERNS.md
@backend/main.py
@backend/schemas.py
@backend/models.py
@backend/tests/test_refeicoes.py
@backend/tests/test_planejamento.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>task 1: Endpoints de configuração de alunos por período + helper de total por slot</name>
  <read_first>
    `backend/models.py` (`AlunosPorPeriodo` — criada na 08-01), `backend/main.py` L38-45 (constantes/slots), `backend/auth.py` (`require_perfil`), `backend/tests/conftest.py` (fixtures), `08-CONTEXT.md` D-14/D-15.
  </read_first>
  <files>backend/main.py, backend/schemas.py, backend/tests/test_alunos_por_periodo.py</files>
  <action>
    Em `backend/schemas.py`:
    1. `class AlunosPorPeriodoUpdate(BaseModel)`: `manha: int = Field(gt=0)`, `tarde: int = Field(gt=0)`, `noite: int = Field(gt=0)`.
    2. `class AlunosPorPeriodoResponse(BaseModel)`: `manha: int`, `tarde: int`, `noite: int`, `updated_at: str | None`, `updated_by: int | None`.

    Em `backend/main.py`:
    3. `GET /alunos-por-periodo` → `require_perfil("admin", "secretaria", "cozinheira")`: lê as 3 linhas (`manha`, `tarde`, `noite`); se faltar qualquer período → 404 com detail `"Configuração de alunos por período ainda não definida"`; senão retorna `AlunosPorPeriodoResponse` (updated_at do mais recente).
    4. `PUT /alunos-por-periodo` → `require_perfil("admin")` (D-14: admin configura; cozinheira não edita): recebe `AlunosPorPeriodoUpdate`; upsert das 3 linhas com `updated_at=datetime.datetime.now` e `updated_by=usuario.id`; retorna a response.
    5. Helper `def _total_por_slot(db: Session, slot: str) -> int` (D-15): valida `slot in SLOTS_PLANEJAMENTO` (400 se não); lê config; se ausente → `HTTPException(400, "Configure os alunos por período antes de lançar a refeição")`; retorna soma por regra:
       - `"Lanche da Manhã"` → manha
       - `"Almoço"` → manha + tarde
       - `"Lanche da Tarde"` → tarde
       - `"Janta"` → noite
    6. Helper `def _derivar_tipo_slot(slot: str) -> str` (espelho de `tipoParaLancamento` do frontend): Lanche da Manhã/Lanche da Tarde → `"Lanche"`; senão o próprio slot.

    Em `backend/tests/test_alunos_por_periodo.py` (novo arquivo):
    7. `test_a1_config_inexistente_404`: GET /alunos-por-periodo → 404 com o detail esperado.
    8. `test_a2_put_admin_configura`: PUT com `{"manha": 120, "tarde": 90, "noite": 40}` → 200; GET confirma valores.
    9. `test_a3_put_valor_invalido`: PUT com `manha=0` → 422 (Pydantic gt=0).
    10. `test_a4_put_cozinheira_negado`: cozinheira PUT → 403; `test_a4b_put_secretaria_negado`: secretaria PUT → 403.
    11. `test_a5_get_cozinheira_permite_leitura`: cozinheira GET após configurar → 200 (leitura liberada).
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest backend/tests/test_alunos_por_periodo.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `schemas.py` contém `AlunosPorPeriodoUpdate` (campos `gt=0`) e `AlunosPorPeriodoResponse`.
    - `main.py` contém `@app.get("/alunos-por-periodo")` (admin+secretaria+cozinheira) e `@app.put("/alunos-por-periodo")` (admin).
    - `main.py` contém `_total_por_slot` com as 4 regras de D-15 e `_derivar_tipo_slot`.
    - `test_alunos_por_periodo.py` tem os 6 testes e todos passam.
  </acceptance_criteria>
  <done>Configuração de alunos por período operacional: admin escreve, cozinheira lê, totais derivados por slot.</done>
</task>

<task type="auto">
  <name>task 2: Revisar POST /refeicoes para payload por slot e atualizar a suíte R*</name>
  <read_first>
    `backend/main.py` L897-982 (`lancar_refeicao_v2`), `backend/schemas.py` `RefeicaoCreate` (L165-171), `backend/tests/test_refeicoes.py` (payloads R1-R15), `08-CONTEXT.md` D-16/D-16b, `08-RESEARCH.md` R-5.
  </read_first>
  <files>backend/main.py, backend/schemas.py, backend/tests/test_refeicoes.py</files>
  <action>
    Em `backend/schemas.py` (per R-5/D-16b — quebra de contrato intencional, revisão MEAL-02):
    1. `RefeicaoCreate` passa a: `slot: str`, `planejamento_id: int | None = None`, `itens: list[RefeicaoItemRequest] = Field(min_length=1)`. **Remover** `tipo_refeicao` e `qtd_alunos`.

    Em `backend/main.py` `lancar_refeicao_v2`:
    2. No início: `tipo = _derivar_tipo_slot(dados.slot)`; validar `tipo in TIPOS_REFEICAO_VALIDOS` (400 se slot inválido); `qtd_alunos = _total_por_slot(db, dados.slot)` (400 se config ausente — mensagem da tarefa 1).
    3. Trocar usos de `dados.tipo_refeicao` → `tipo` (validação L908-912, gravação L961) e `dados.qtd_alunos` → `qtd_alunos` (escala L947, validação L952, mensagem L982, gravação L963).
    4. Manter intactos: conversão (`_converter_para_unidade_oficial`), bloqueio por estoque (L939-943), auditoria de divergência com justificativa (L945-956).
    5. Mensagem final: `f"Refeição servida a {qtd_alunos} alunos! Estoque abatido com sucesso."`.

    Em `backend/tests/test_refeicoes.py` (payload por slot — D-16/D-16b):
    6. Helper novo `_configurar_alunos(client, admin_token)` → PUT /alunos-por-periodo com `{"manha": 100, "tarde": 80, "noite": 40}`.
    7. Atualizar TODOS os POST /refeicoes (R1, R2, R3, R4, R6, R9, R10, R11, R12, R13, R15, lanche/antigo): payload `{"slot": "Almoço", "itens": [...]}` (ajustar `qtd_alunos` removido; `tipo_refeicao` removido) + chamar `_configurar_alunos` nos testes que dependem de escala/derivação (R1, R2, R9, R10, R11, R12, R13, R15).
    8. Ajustar asserts de escala (R9/R10): a receita escala por `manha + tarde = 180` no Almoço.
    9. Novos testes:
       - `test_r16_derivacao_por_slot`: `_configurar_alunos(100, 80, 40)`; POST slot `"Lanche da Manhã"` → `qtd_alunos == 100` no GET /refeicoes; slot `"Lanche da Tarde"` → 80; slot `"Janta"` → 40; slot `"Almoço"` → 180.
       - `test_r17_config_ausente_400`: sem `_configurar_alunos`, POST /refeicoes → 400 com `"Configure os alunos por período antes de lançar a refeição"`.
       - `test_r18_slot_invalido_400`: slot `"Brunch"` → 400.
       - `test_r19_avulso_sem_planejamento`: POST sem `planejamento_id` (avulso, D-16b) com slot → 200 e `qtd_alunos` derivado; estoque deduzido.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest backend/tests/test_refeicoes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `schemas.py` `RefeicaoCreate` contém `slot: str` e NÃO contém `tipo_refeicao`/`qtd_alunos` (grep `qtd_alunos` em RefeicaoCreate → 0).
    - `main.py` `lancar_refeicao_v2` usa `_derivar_tipo_slot` e `_total_por_slot` (grep) e mantém o bloqueio `Estoque insuficiente`.
    - `test_refeicoes.py` não contém `"qtd_alunos"` em payloads de POST /refeicoes (grep) e contém `_configurar_alunos`.
    - `test_refeicoes.py` contém `test_r16_derivacao_por_slot` .. `test_r19_avulso_sem_planejamento` e todos passam.
  </acceptance_criteria>
  <done>Lançamento por slot com derivação de tipo/total; cozinheira não envia alunos; bloqueio por estoque preservado.</done>
</task>

<task type="auto">
  <name>task 3: Projeção cumulativa (GET /planejamento/projecao) e avisos no POST /planejamento</name>
  <read_first>
    `backend/main.py` L569-657 (planejamento: `_planejamento_ativo`, GET L599, POST L614-657), `backend/main.py` L871-894 (`_converter_para_unidade_oficial`), L1022-1046, `08-CONTEXT.md` D-17/D-18/D-19, `08-RESEARCH.md` R-3 e Padrão 4.
  </read_first>
  <files>backend/main.py, backend/tests/test_planejamento.py</files>
  <action>
    Em `backend/main.py`:
    1. Helper `def _consumo_slot(db, slot_entrada, total_alunos) -> dict[int, float]`: para o planejamento de um slot, soma `receita.quantidade * total_alunos` por item (via `_converter_para_unidade_oficial`); itens sem conversão → marcados não avaliáveis (não lança 400 — D-17). Total por slot via `_total_por_slot`; config ausente → retorna sinal `None` (não levanta, para a projeção responder `configurado: false`).
    2. `GET /planejamento/projecao?data=YYYY-MM-DD` → `require_perfil("admin", "secretaria")` (D-20 — cozinheira não vê):
       - `data_ref = data or date.today()`; `ativos = _planejamento_ativo(db, data_ref)`; `segunda = data_ref - timedelta(days=data_ref.weekday())`.
       - Config de alunos ausente → `{"configurado": false, "dias": [], "itens": [], "resumo": {...zerados}}`.
       - Simulação dia a dia (0..6, slots na ordem de `SLOTS_PLANEJAMENTO`): saldo corrente por item = `Item.saldo_atual`; para cada slot planejado no dia, `consumo = _consumo_slot(...)`; itens avaliáveis têm saldo decrementado; registra por dia `consumo` e `rupturas` (itens com saldo < 0 → `faltando = -saldo`).
       - Resposta (contrato consumido pela 08-09):
         ```json
         {
           "configurado": true,
           "data_ref": "YYYY-MM-DD",
           "dias": [{"dia": "YYYY-MM-DD", "dia_semana": 0, "rupturas": [{"item_id": 1, "nome": "Arroz", "faltando": 12.5, "unidade_oficial": "KG"}]}],
           "itens": [{"item_id": 1, "nome": "Arroz", "unidade_oficial": "KG", "saldo_atual": 42.5, "consumo_semana": 25.0, "saldo_projetado": 17.5, "primeiro_dia_ruptura": null, "avaliavel": true}],
           "resumo": {"itens_com_ruptura": 2, "itens_nao_avaliaveis": 1}
         }
         ```
         `primeiro_dia_ruptura` = índice do dia (0-6) em que o saldo do item fica negativo pela primeira vez, ou `null`. Itens sem conversão → `avaliavel: false`, `consumo_semana: null`, `saldo_projetado: null`, contados em `itens_nao_avaliaveis`.
       - `from datetime import timedelta` no topo (se ainda não importado).
    3. `POST /planejamento` (L614-657): após persistir (upsert ou criação), calcular `avisos` da refeição salva (D-18): simular consumo acumulado de segunda até o `dia_semana` salvo usando `_consumo_slot`/`_total_por_slot`; para cada item com saldo projetado < 0 → `{"item_id", "nome", "faltando"}`. Config ausente → `avisos: []` (não bloqueia — D-18). Resposta: `{**resposta_existente, "avisos": avisos}` (aditivo).

    Em `backend/tests/test_planejamento.py`:
    4. Helper `_configurar_alunos(client, admin_token)` (mesmo da tarefa 2).
    5. `test_p1_projecao_config_ausente`: GET /planejamento/projecao sem config → 200 com `"configurado": false`.
    6. `test_p2_projecao_sem_ruptura`: 1 item (saldo 100 KG), receita consumindo 1 kg/aluno no Almoço, config 100/80/40 → `itens[0].saldo_projetado == 100 - 180` por dia de Almoço; `primeiro_dia_ruptura == null`; `itens_com_ruptura == 0`.
    7. `test_p3_projecao_ruptura_primeiro_dia`: consumo diário maior que o saldo → `primeiro_dia_ruptura` com o índice esperado e `dias[0].rupturas` com `faltando > 0`.
    8. `test_p4_projecao_sem_conversao_nao_avaliavel`: receita com medida caseira sem conversão → `itens[0].avaliavel == false` e `itens_nao_avaliaveis == 1`; resposta 200 (não quebra).
    9. `test_p5_projecao_cozinheira_403`: cozinheira GET /planejamento/projecao → 403 (D-20).
    10. `test_p6_post_avisos_nao_bloqueia`: POST /planejamento com estoque insuficiente → 200 e `avisos` não vazio quando há ruptura projetada; sem config → `avisos == []`.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest backend/tests/test_planejamento.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` contém `@app.get("/planejamento/projecao")` com `require_perfil("admin", "secretaria")` e o contrato com `configurado`/`dias`/`itens`/`resumo`.
    - `main.py` POST /planejamento retorna `avisos` na resposta (grep `"avisos"`).
    - `test_planejamento.py` contém `test_p1_projecao_config_ausente` .. `test_p6_post_avisos_nao_bloqueia` e todos passam.
    - `pytest backend/tests/test_planejamento.py -x` verde; GET /planejamento (lista) inalterado (testes 8_1..8_9 continuam verdes sem modificação).
  </acceptance_criteria>
  <done>Projeção cumulativa da semana disponível para admin/secretaria; planejamento salva com avisos não-bloqueantes (D-17/D-18).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | PUT /alunos-por-periodo (admin) e POST /refeicoes (cozinheira) — slot/config atravessam fronteira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | T (tampering) | `PUT /alunos-por-periodo` | mitigate | Admin-only (`require_perfil("admin")`) + `Field(gt=0)` (ASVS V4.1/V5.1); `updated_by` gravado para auditoria |
| T-08-11 | T (tampering) | `POST /refeicoes` | mitigate | Slot validado contra `SLOTS_PLANEJAMENTO`; tipo e qtd derivados no backend (nunca do payload); bloqueio por estoque inalterado (ASVS V5.1) |
| T-08-12 | I (info disclosure) | `GET /planejamento/projecao` | mitigate | Restrito a admin/secretaria (D-20); cozinheira 403 — testado (ASVS V4.1.3) |
| T-08-13 | D (DoS) | projeção | accept | Simulação limitada a 7 dias × 4 slots × itens do catálogo local; sem loops externos |
</threat_model>

<verification>
- `cd backend && source venv/bin/activate && pytest backend/tests/ -x` — suíte completa verde (R* revisado, planejamento 8_1..8_9 intacto, novos arquivos).
- Grep gate: `RefeicaoCreate` sem `qtd_alunos`; payloads de teste sem `"qtd_alunos"`.
</verification>

<success_criteria>
- IMP-09/10 + MEAL-02: config admin-only, totais derivados, lançamento sem digitação.
- IMP-11: projeção não-bloqueante com avisos; lançamento continua bloqueando.
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-07-SUMMARY.md`
</output>
