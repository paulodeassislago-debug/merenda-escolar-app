---
phase: 08-improvements
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - backend/main.py
  - backend/tests/test_itens.py
  - backend/tests/test_dashboard.py
  - frontend/src/pages/admin/Itens.tsx
  - frontend/src/pages/DashboardGestao.tsx
  - frontend/src/pages/admin/constants.ts
autonomous: true
requirements:
  - IMP-01
  - IMP-02

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Admin cadastra e edita o limiar de cada item; zero/negativo são rejeitados com 400 (D-03)."
    - "Toda superfície que alerta baixo estoque usa o limiar do próprio item na unidade de exibição (D-02, D-04)."
    - "A constante LIMIAR_BAIXO_ESTOQUE desaparece do frontend e do backend (D-04)."
    - "Itens sem limiar informado nascem com 5.0 (IMP-01: valor padrão preserva comportamento atual)."
  artifacts:
    - path: "backend/main.py"
      provides: "Checagem 400 de limiar em POST/PUT /itens; dashboard críticos por item.limiar na unidade de exibição"
    - path: "backend/tests/test_itens.py"
      provides: "Testes de validação de limiar (default, 400 para zero/negativo)"
    - path: "backend/tests/test_dashboard.py"
      provides: "Testes de críticos com limiar individual"
    - path: "frontend/src/pages/admin/Itens.tsx"
      provides: "Campo limiar no form + badge por item.limiar"
    - path: "frontend/src/pages/DashboardGestao.tsx"
      provides: "Copy dinâmica e badge por item.limiar"
  key_links:
    - from: "frontend/src/pages/admin/Itens.tsx"
      to: "backend/main.py"
      via: "PUT /itens/{id} com limiar no payload"
    - from: "frontend/src/pages/DashboardGestao.tsx"
      to: "frontend/src/types.ts"
      via: "Item.limiar na comparação saldoExibicao(item) < item.limiar"
---

<objective>
Entrega do limiar individual de baixo estoque: campo novo no cadastro/edição de item, validação 400, alertas calculados por item na unidade de exibição e remoção da constante global `LIMIAR_BAIXO_ESTOQUE`.

Purpose: Satisfazer IMP-01/IMP-02 e D-01..D-04 sem alterar o comportamento de saldo. O backend permanece autoridade da regra (D-02).
Output: Backend validando e expondo `limiar`; Itens.tsx e DashboardGestao.tsx consumindo `item.limiar`; testes verdes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-RESEARCH.md
@.planning/phases/08-improvements/08-PATTERNS.md
@.planning/phases/08-improvements/08-UI-SPEC.md
@backend/main.py
@backend/tests/test_itens.py
@backend/tests/test_dashboard.py
@frontend/src/pages/admin/Itens.tsx
@frontend/src/pages/admin/constants.ts
@frontend/src/pages/DashboardGestao.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Validar limiar com 400 no backend e usar item.limiar no dashboard</name>
  <read_first>
    `backend/main.py` L161-300 (`criar_item`/`atualizar_item`), L1059-1113 (`dashboard_admin`), L44-45 (constante `LIMIAR_BAIXO_ESTOQUE`), `08-CONTEXT.md` D-02/D-03.
  </read_first>
  <files>backend/main.py, backend/tests/test_itens.py, backend/tests/test_dashboard.py</files>
  <action>
    Em `backend/main.py`:
    1. **Remover** a constante `LIMIAR_BAIXO_ESTOQUE = 5.0` (L45) — D-04.
    2. Em `criar_item` (L181-224) e `atualizar_item` (L226-283): adicionar checagem explícita antes de persistir (per D-03 — exige 400, não 422):
       ```python
       if dados.limiar is not None and dados.limiar <= 0:
           raise HTTPException(status_code=400, detail="limiar deve ser maior que zero")
       ```
       (`limiar` com default 5.0 em ItemCreate; em ItemUpdate é `None` quando ausente — checar `is not None`.) Persistir `limiar` no modelo em ambos.
    3. Em `dashboard_admin` (L1068-1078): substituir a comparação com a constante pela do próprio item, mantendo o cálculo na unidade de exibição:
       - `criticos` passa a filtrar `(i.saldo_atual / (i.fator_conversao or 1.0)) < (i.limiar or 5.0)` e o dict inclui `"limiar": i.limiar or 5.0`.
    4. Garantir que `ItemResponse` serializa `limiar` (já no schema da 08-01).

    Em `backend/tests/test_itens.py`:
    5. Adicionar:
       - `test_4_6_limiar_default`: POST /itens sem `limiar` → 200, resposta `limiar == 5.0`.
       - `test_4_7_limiar_invalido_400`: POST /itens com `limiar=0` → 400; com `limiar=-1` → 400.
       - `test_4_8_limiar_atualizado`: PUT /itens/{id} com `limiar=2.5` → 200; GET /itens confirma `limiar == 2.5`.

    Em `backend/tests/test_dashboard.py`:
    6. Adicionar:
       - `test_d5_limiar_individual_critico`: criar item com `limiar=100` e saldo 50 → `baixo_estoque == 1` e item em `itens_criticos` com `limiar == 100`.
       - `test_d6_limiar_individual_estavel`: criar item com `limiar=0.5` e saldo 2 (na unidade de exibição) → NÃO aparece em `itens_criticos`.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest backend/tests/test_itens.py backend/tests/test_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/main.py` não contém `LIMIAR_BAIXO_ESTOQUE` (grep retorna 0).
    - `backend/main.py` contém `limiar deve ser maior que zero` em criar_item e atualizar_item.
    - `dashboard_admin` compara com `i.limiar` (grep `i.limiar` em dashboard).
    - `test_itens.py` contém `test_4_6_limiar_default`, `test_4_7_limiar_invalido_400`, `test_4_8_limiar_atualizado` e todos passam.
    - `test_dashboard.py` contém `test_d5_limiar_individual_critico` e `test_d6_limiar_individual_estavel` e todos passam.
  </acceptance_criteria>
  <done>Backend é a autoridade do limiar: 400 para valores inválidos, default 5.0, dashboard crítico por item na unidade de exibição.</done>
</task>

<task type="auto">
  <name>task 2: Adicionar campo limiar ao form e badge por item em Itens.tsx</name>
  <read_first>
    `frontend/src/pages/admin/Itens.tsx` (686 linhas — form L100-165, badge L315/L329, import de constantes L7), `frontend/src/pages/admin/constants.ts`, `frontend/src/types.ts` (Item.limiar), `08-UI-SPEC.md` (Copywriting Contract: label/ajuda do limiar).
  </read_first>
  <files>frontend/src/pages/admin/Itens.tsx, frontend/src/pages/admin/constants.ts</files>
  <action>
    Em `frontend/src/pages/admin/constants.ts`: **remover** `LIMIAR_BAIXO_ESTOQUE` (L15) — D-04.

    Em `frontend/src/pages/admin/Itens.tsx` (padrões do form atual — campos com `form-input` e `campo-formulario`):
    1. Tipo `ItemPayload` (L10): adicionar `limiar: number;`.
    2. Estado do form de item: incluir `limiar` (default `5.0` no estado inicial de "novo item"; preencher com `editando.limiar` na edição).
    3. No formulário (junto aos demais campos numéricos), adicionar campo numérico:
       - Label: `Limiar de baixo estoque` (associado com `htmlFor`/`id`).
       - `type="number"`, `step="0.01"`, `min="0.01"`, `required`, value do estado, onChange atualizando o estado.
       - Ajuda: `Alerta quando o saldo ficar abaixo deste valor, na unidade de exibição.` (exatamente — UI-SPEC).
       - Validação local: se `limiar <= 0` no submit → mensagem de erro e não envia.
    4. Enviar `limiar` nos payloads de POST /itens e PUT /itens/{id} (L139-144).
    5. Badge de baixo estoque (L315 e L329): trocar a comparação `... < LIMIAR_BAIXO_ESTOQUE` por `... < item.limiar`, usando o saldo na unidade de exibição (`saldo_atual / fator_conversao` — padrão existente na página).
    6. Remover o import de `LIMIAR_BAIXO_ESTOQUE` (L7) — manter `UNIDADES_SUGERIDAS`.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/admin/constants.ts` não contém `LIMIAR_BAIXO_ESTOQUE` (grep retorna 0).
    - `Itens.tsx` contém `Limiar de baixo estoque` (label) e `min="0.01"` no input.
    - `Itens.tsx` envia `limiar` no body de POST e PUT (grep `limiar:` nos payloads).
    - `Itens.tsx` compara badges com `item.limiar` (grep `item.limiar`) e não referencia `LIMIAR_BAIXO_ESTOQUE`.
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Admin cadastra/edita limiar por item e a página de Itens alerta pelo limiar do item, na unidade de exibição.</done>
</task>

<task type="auto">
  <name>task 3: Consumir limiar por item em DashboardGestao.tsx com copy dinâmica</name>
  <read_first>
    `frontend/src/pages/DashboardGestao.tsx` (L1-27 imports/helper saldoExibicao, L238-258 seção estoque, L241 texto fixo "limiar de 5"), `08-CONTEXT.md` D-04, `08-UI-SPEC.md` copy do dashboard.
  </read_first>
  <files>frontend/src/pages/DashboardGestao.tsx</files>
  <action>
    1. No import de `./admin/constants` (L6): manter apenas `SLOTS_REFEICAO` — remover `LIMIAR_BAIXO_ESTOQUE`.
    2. L241: substituir o texto fixo pelo copy dinâmico exato: `O alerta considera o limiar configurado de cada item na unidade de exibição.`
    3. L253-254: trocar `const baixoEstoque = saldo < LIMIAR_BAIXO_ESTOQUE;` por `const baixoEstoque = saldo < item.limiar;` (saldo já vem de `saldoExibicao(item)` — unidade de exibição, D-02).
    4. Garantir que o badge existente `Baixo estoque`/`Estável` continua usando essa variável; nenhuma outra mudança de layout nesta tarefa.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardGestao.tsx` não referencia `LIMIAR_BAIXO_ESTOQUE` (grep retorna 0).
    - `DashboardGestao.tsx` contém a string exata `O alerta considera o limiar configurado de cada item na unidade de exibição.`
    - `DashboardGestao.tsx` compara com `item.limiar` (grep `item.limiar`).
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Painel da secretaria alerta pelo limiar individual de cada item, sem texto fixo (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | valor `limiar` cruza a fronteira em POST/PUT /itens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | T (tampering) | `criar_item`/`atualizar_item` | mitigate | Checagem explícita `limiar <= 0 → 400` + `Field(gt=0)` no schema (ASVS V5.1.3 input validation); limiar nunca altera saldo |
| T-08-04 | T (tampering) | `dashboard_admin` | mitigate | Cálculo na unidade de exibição com `fator_conversao or 1.0` e `limiar or 5.0` — sem divisão por zero (ASVS V5.3.3) |
</threat_model>

<verification>
- `cd backend && source venv/bin/activate && pytest backend/tests/ -x` — suíte completa verde.
- `cd frontend && npm run build && npm run lint` — gates verdes.
- Grep gate global: nenhum arquivo de `frontend/src` referencia `LIMIAR_BAIXO_ESTOQUE`.
</verification>

<success_criteria>
- IMP-01: limiar por item cadastrável/editável com default 5.0.
- IMP-02: listagens, dashboard e alertas usam o limiar do item na unidade de exibição; inválidos rejeitados (400) sem alterar saldo.
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-02-SUMMARY.md`
</output>
