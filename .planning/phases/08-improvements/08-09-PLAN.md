---
phase: 08-improvements
plan: 09
type: execute
wave: 5
depends_on: [08-07]
files_modified:
  - frontend/src/pages/admin/Planejamento.tsx
  - frontend/src/pages/admin/Planejamento.css
autonomous: true
requirements:
  - IMP-11

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "A célula da grade exibe badge de alerta quando o dia tem déficit projetado, com tooltip listando itens e quanto falta (D-19)."
    - "Existe o painel colapsável 'Projeção da semana' com a tabela item | saldo atual | consumo projetado | saldo projetado final | 1º dia de ruptura, ruptura em vermelho e sobra em verde (D-19)."
    - "Salvar o planejamento não bloqueia; um banner não-bloqueante lista refeições afetadas/itens faltantes com ação 'Ver projeção' (D-19)."
    - "Config de alunos vazia → painel exibe 'Configure os alunos por período para ativar a projeção.' sem quebrar a grade (D-19)."
    - "D-20: a secretaria vê os mesmos alertas (Planejamento acessível a admin e secretaria); a cozinheira não vê projeção."
  artifacts:
    - path: "frontend/src/pages/admin/Planejamento.tsx"
      provides: "Fetch de /planejamento/projecao, badge por célula, painel colapsável, banner com avisos"
    - path: "frontend/src/pages/admin/Planejamento.css"
      provides: "Estilos de badge, painel, tabela de projeção e banner com tokens institucionais"
  key_links:
    - from: "frontend/src/pages/admin/Planejamento.tsx"
      to: "frontend/src/api.ts"
      via: "fetchJson<ProjecaoSemana>('/planejamento/projecao?data=' + formatISO(segunda))"
    - from: "frontend/src/pages/admin/Planejamento.tsx"
      to: "frontend/src/types.ts"
      via: "tipos ProjecaoSemana/ProjecaoDia/ProjecaoItem"
---

<objective>
Exibição da projeção cumulativa na página Planejamento (admin e secretaria — D-20): badge de déficit por célula, painel colapsável "Projeção da semana" e banner não-bloqueante após salvar, conforme UI-SPEC aprovado em D-19.

Purpose: Satisfazer IMP-11 na UI — o planejamento avisa (nunca bloqueia) e o usuário enxerga o primeiro dia de ruptura de cada item.
Output: Planejamento.tsx/css com badge, painel e banner, validados pelos gates do frontend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-RESEARCH.md
@.planning/phases/08-improvements/08-PATTERNS.md
@.planning/phases/08-improvements/08-UI-SPEC.md
@frontend/src/pages/admin/Planejamento.tsx
@frontend/src/pages/admin/Planejamento.css
@frontend/src/types.ts
@frontend/src/pages/CardapioPublico.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Tipos de projeção, fetch do endpoint e badge de déficit na célula</name>
  <read_first>
    `frontend/src/pages/admin/Planejamento.tsx` (estado/fetch L54-128, grade L253-309, helpers `formatISO`/`segundaDaSemana` L13-26), `frontend/src/types.ts`, `08-RESEARCH.md` (contrato da projeção — `configurado`/`dias`/`itens`/`resumo`), `08-UI-SPEC.md` (badge: `⚠ {N} item(ns) faltando` + tooltip `{Item} −{qtd} {unidade}`).
  </read_first>
  <files>frontend/src/types.ts, frontend/src/pages/admin/Planejamento.tsx</files>
  <action>
    Em `frontend/src/types.ts` (espelhar o contrato de `GET /planejamento/projecao` da 08-07):
    1. `export interface ProjecaoRuptura { item_id: number; nome: string; faltando: number; unidade_oficial: string; }`
    2. `export interface ProjecaoDia { dia: string; dia_semana: number; rupturas: ProjecaoRuptura[]; }`
    3. `export interface ProjecaoItem { item_id: number; nome: string; unidade_oficial: string; saldo_atual: number; consumo_semana: number | null; saldo_projetado: number | null; primeiro_dia_ruptura: number | null; avaliavel: boolean; }`
    4. `export interface ProjecaoSemana { configurado: boolean; data_ref: string; dias: ProjecaoDia[]; itens: ProjecaoItem[]; resumo: { itens_com_ruptura: number; itens_nao_avaliaveis: number }; }`

    Em `frontend/src/pages/admin/Planejamento.tsx`:
    5. Estado `projecao: ProjecaoSemana | null` + `carregandoProjecao`. No efeito de carregamento (L95-128) e em `carregarDados` (L69-92), disparar também `fetchJson<ProjecaoSemana>('/planejamento/projecao?data=' + formatISO(seg))` (Promise.all com os demais — tolerar falha do endpoint: erro de projeção não derruba a grade; `projecao` fica null e o painel mostra estado de erro com retry).
    6. Badge na célula (L282-302): para cada `td` do dia `diaIdx`, se `projecao?.configurado` e `projecao.dias[diaIdx]?.rupturas.length > 0`, renderizar abaixo do select:
       - `<span className="badge-ruptura">⚠ {N} item(ns) faltando</span>` (N = `rupturas.length`; singular/plural correto: `item faltando` / `itens faltando`).
       - Tooltip: `title` com `rupturas.map(r => `${r.nome} −${r.faltando.toLocaleString('pt-BR', { maximumFractionDigits: 2 })} ${r.unidade_oficial}`).join('\n')` (ex.: `Arroz −12,5 kg` — D-19).
    7. Nenhum bloqueio: a grade e o save continuam funcionando com `projecao === null` (estado de erro/ausência silencioso ou com retry discreto no painel — tarefa 2).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` contém `ProjecaoSemana`, `ProjecaoDia`, `ProjecaoItem`, `ProjecaoRuptura` com os campos do contrato.
    - `Planejamento.tsx` chama `'/planejamento/projecao?data='` (grep) e a falha do endpoint não impede o render da grade (tratamento try/catch separado).
    - A célula renderiza `badge-ruptura` quando `dias[diaIdx]?.rupturas.length > 0` (grep `badge-ruptura` e `rupturas.length`).
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Badge de déficit por célula com tooltip por item (D-19) alimentado pelo endpoint de projeção.</done>
</task>

<task type="auto">
  <name>task 2: Painel 'Projeção da semana' e banner não-bloqueante com 'Ver projeção'</name>
  <read_first>
    `frontend/src/pages/admin/Planejamento.tsx` (retorno JSX L186-327 — onde inserir painel abaixo da grade e banner pós-save), `frontend/src/pages/CardapioPublico.tsx` (padrão `<details>/<summary>` nativo — Fase 7), `frontend/src/pages/admin/Planejamento.css`, `frontend/src/index.css` (tokens), `08-UI-SPEC.md` (painel: colunas, resumo, `não avaliável`; banner: copy e ação `Ver projeção`).
  </read_first>
  <files>frontend/src/pages/admin/Planejamento.tsx, frontend/src/pages/admin/Planejamento.css</files>
  <action>
    1. **Painel colapsável** abaixo da grade (dentro do form ou após — decisão do executor mantendo o layout atual):
       - `<details className="painel-projecao">` com `<summary>Projeção da semana</summary>` (disclosure nativo, padrão Fase 7; fechado inicialmente).
       - Se `!projecao?.configurado` (ou null): mensagem exata `Configure os alunos por período para ativar a projeção.` (D-19 — a grade continua funcional).
       - Se configurado: tabela com cabeçalho `Item | Saldo atual | Consumo projetado | Saldo projetado final | 1º dia de ruptura` (UI-SPEC):
         - Linha por `projecao.itens`: nome; `saldo_atual` e `saldo_projetado` com `toLocaleString('pt-BR', { maximumFractionDigits: 2 })` + unidade;
         - `primeiro_dia_ruptura !== null` → linha com classe `linha-ruptura` (texto `--erro`, fundo `--erro-fundo`) e célula `1º dia` com o dia da semana (`DIAS_SEMANA[primeiro_dia_ruptura]`);
         - `primeiro_dia_ruptura === null` e `avaliavel` → linha com classe `linha-sobra` (texto `--verde-escuro` ou `--verde-vivo` sobre `--verde-tint` conforme caso) e `1º dia` = `—`;
         - `avaliavel === false` → célula de consumo/saldo projetado = `não avaliável` (texto `--texto-suave`).
       - Resumo acima da tabela: `{resumo.itens_com_ruptura} item(ns) com ruptura prevista` (plural correto) e, quando `itens_nao_avaliaveis > 0`, `{N} item(ns) sem conversão cadastrada`.
    2. **Banner não-bloqueante ao salvar** (após `handleSalvar` com sucesso — L174-176):
       - Se `projecao` indica rupturas na semana (ou `avisos` retornados pelo POST — usar `avisos` do POST quando presentes, senão derivar de `projecao`): renderizar banner `role="status"` com classe `banner-aviso` (fundo `--erro-fundo`, borda `--erro`, texto `--erro`):
         - Copy exata: `Atenção: {N} item(ns) podem faltar nas refeições planejadas.`
         - Lista: `{item.nome} −{faltando} {unidade}` por item (do `avisos` ou da semana).
       - Ação `Ver projeção` (botão `btn-secundario`): abre o `<details>` (ref do summary, `open = true`) e rola até o painel (`painelRef.current.scrollIntoView({ behavior: 'smooth' })`).
       - O save NUNCA é bloqueado por estoque (D-18): o banner aparece após sucesso do POST, que sempre retorna 200 com avisos.
    3. `Planejamento.css`: `.badge-ruptura` (display inline-block, `--raio` 4px, fundo `--erro-fundo`, texto `--erro`, fonte 12px, margem top `--sm`), `.painel-projecao` (margem `--lg`, `--raio`, borda `--borda`, `--sombra-card`), `.linha-ruptura`/`.linha-sobra`, `.banner-aviso` (padding `--md`, `--raio`, margem `--lg`). Nenhuma cor nova fora dos tokens (UI-SPEC).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Planejamento.tsx` contém `<summary>Projeção da semana</summary>` e a string exata `Configure os alunos por período para ativar a projeção.`
    - O banner pós-save contém a string exata `podem faltar nas refeições planejadas` e o botão `Ver projeção` que chama `scrollIntoView`.
    - `Planejamento.css` usa apenas tokens (`--erro`, `--erro-fundo`, `--verde-tint`, `--borda`, `--raio`, `--sombra-card`, `--texto-suave`) — sem hex novo fora de `index.css`.
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Painel e banner operacionais: projeção visível, não-bloqueante, com navegação 'Ver projeção' (D-19).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | GET /planejamento/projecao (leitura) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | I (info disclosure) | Planejamento.tsx | mitigate | Endpoint restrito a admin/secretaria (D-20); página já protegida por ProtectedRoute; cozinheira nunca recebe o contrato (ASVS V4.1) |
| T-08-15 | T (tampering) | renderização da projeção | accept | Dados são apenas exibidos (números formatados via toLocaleString); nenhum HTML bruto — sem XSS por injeção (ASVS V5.1) |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — gates verdes.
- Backstop manual (08-VALIDATION.md): estoque insuficiente → badge na célula, painel com ruptura em vermelho, banner com 'Ver projeção' rolando ao painel; config vazia → mensagem de ativação.
</verification>

<success_criteria>
- IMP-11 na UI: badge, painel e banner de projeção não-bloqueante; mesma visão para admin e secretaria.
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-09-SUMMARY.md`
</output>
