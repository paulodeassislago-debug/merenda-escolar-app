---
phase: 08-improvements
plan: 11
wave: 1
depends_on: [08-10]
gap_closure: true
files_modified:
  - backend/models.py
  - backend/migracao.py
  - backend/schemas.py
  - backend/main.py
  - backend/tests/test_refeicoes.py
  - backend/tests/test_publico.py
  - frontend/src/types.ts
  - frontend/src/pages/PainelCozinha.tsx
  - frontend/src/pages/PainelCozinha.css
  - frontend/src/pages/CardapioPublico.tsx
  - frontend/src/pages/CardapioPublico.css
  - frontend/src/pages/admin/Planejamento.tsx
  - frontend/src/pages/admin/Planejamento.css
autonomous: true
requirements:
  - IMP-10
  - PUBLIC-01
  - PUBLIC-02

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Uma refeição avulsa exige nome próprio, persiste esse nome e continua marcada como EXTRA."
    - "Uma refeição extra aparece no cardápio público na data e no slot em que foi servida."
    - "Uma refeição planejada e uma extra no mesmo slot aparecem simultaneamente; a extra não substitui nem oculta a planejada."
    - "Os quatro cards de refeições planejadas da cozinha permanecem alinhados quando títulos, estados ou tags têm tamanhos diferentes."
    - "As tags Confirmado, Disponível, Pendente e EXTRA possuem altura, tipografia, espaçamento e tratamento visual consistentes."
    - "O grid de Planejamento mantém select, aviso de ruptura e colunas alinhados quando apenas algumas células possuem avisos."
    - "O layout do Planejamento permanece utilizável em viewport estreita, com overflow horizontal controlado e sem deformar os slots."
  artifacts:
    - path: "backend/models.py"
      provides: "Refeicao.nome_extra nullable para legado e preenchido em novos lançamentos avulsos"
      contains: "nome_extra"
    - path: "backend/migracao.py"
      provides: "Migração idempotente da coluna nome_extra em refeicoes"
      exports: ["migrar"]
    - path: "backend/main.py"
      provides: "Validação/persistência do nome extra, status por slot e cardápio público com planejadas e extras"
      contains: "nome_extra"
    - path: "frontend/src/pages/PainelCozinha.tsx"
      provides: "Campo obrigatório de nome da refeição avulsa e exibição do nome no card EXTRA"
    - path: "frontend/src/pages/CardapioPublico.tsx"
      provides: "Renderização de múltiplas refeições no mesmo slot, incluindo extras"
    - path: "frontend/src/pages/admin/Planejamento.css"
      provides: "Células com altura reservada para avisos e grid responsivo estável"

<objective>
Implementar as três correções manuais posteriores ao UAT:

1. Refeições extraordinárias recebem um nome informado pela cozinheira e aparecem no cardápio público.
2. Os widgets do painel da cozinheira ficam alinhados e usam uma linguagem visual consistente para estados e a tag EXTRA.
3. O grid responsivo do Planejamento reserva espaço para avisos de ruptura sem desalinhamento entre slots.

Quando uma refeição planejada e uma extraordinária ocuparem o mesmo slot, ambas devem ser exibidas no cardápio público. A extra recebe a tag `EXTRA` e não substitui a refeição planejada.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-UAT-ISSUES.md
@.planning/phases/08-improvements/08-10-PLAN.md
@backend/models.py
@backend/migracao.py
@backend/schemas.py
@backend/main.py
@frontend/src/types.ts
@frontend/src/pages/PainelCozinha.tsx
@frontend/src/pages/PainelCozinha.css
@frontend/src/pages/CardapioPublico.tsx
@frontend/src/pages/CardapioPublico.css
@frontend/src/pages/admin/Planejamento.tsx
@frontend/src/pages/admin/Planejamento.css
</context>

<tasks>

<task type="auto">
  <name>task 1: Backend — nome da refeição extra e cardápio público combinado</name>
  <read_first>
    `backend/models.py` (Refeicao.slot), `backend/migracao.py` (migrações de refeicoes), `backend/schemas.py` (RefeicaoCreate), `backend/main.py` (POST /refeicoes, GET /refeicoes, /refeicoes/hoje e /publico/cardapio), `backend/tests/test_refeicoes.py`, `backend/tests/test_publico.py`.
  </read_first>
  <files>backend/models.py, backend/migracao.py, backend/schemas.py, backend/main.py, backend/tests/test_refeicoes.py, backend/tests/test_publico.py</files>
  <action>
    1. Adicionar `nome_extra = Column(String, nullable=True)` em `models.Refeicao`.
    2. Estender `migracao.py` com `ALTER TABLE refeicoes ADD COLUMN nome_extra VARCHAR` quando ausente. A migração deve ser idempotente e não alterar registros legados.
    3. Adicionar `nome_extra: str | None = None` ao schema de criação. No handler de `POST /refeicoes`, exigir nome não vazio quando `planejamento_id` for `None`; rejeitar nome ausente ou somente espaços com 400. Para refeição planejada, ignorar ou rejeitar nome extra conforme o contrato escolhido pelo código existente, sem permitir que ele substitua o nome planejado.
    4. Persistir `nome_extra` e serializá-lo em `GET /refeicoes`, com `extra = planejamento_id is None` e `nome_refeicao = nome_extra` para extras.
    5. Em `/refeicoes/hoje`, retornar para uma extra o nome informado no campo `prato`, mantendo `extra: true`.
    6. Reestruturar `/publico/cardapio` para retornar uma lista de entradas, não um único registro por tipo/slot. A resposta deve manter as entradas planejadas e acrescentar as refeições extras da data, contendo pelo menos `tipo_refeicao`, `nome_refeicao`, `slot`, `extra` e `ingredientes`.
    7. Ordenar as entradas pela ordem canônica dos quatro slots; dentro do mesmo slot, mostrar primeiro a planejada e depois as extras por `data_hora`/id. Não deduplicar por tipo.
    8. Adicionar testes para: nome obrigatório em avulsa; nome persistido; status por slot com `prato` extra; extra aparecendo no cardápio público; planejada e extra coexistindo no mesmo slot.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_refeicoes.py tests/test_publico.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `Refeicao` contém `nome_extra` e a migração adiciona a coluna somente quando ausente.
    - `POST /refeicoes` rejeita avulsa sem nome e aceita avulsa com nome não vazio.
    - `GET /refeicoes/hoje` retorna `prato` com o nome da extra e `extra: true`.
    - `/publico/cardapio` retorna simultaneamente prato planejado e extra no mesmo slot.
    - `pytest tests/ -q` permanece verde.
  </acceptance_criteria>
  <done>O backend registra, identifica e publica refeições extraordinárias com nome próprio sem ocultar refeições planejadas.</done>
</task>

<task type="auto">
  <name>task 2: Frontend — nome da extra e cards da cozinha alinhados</name>
  <read_first>
    `frontend/src/types.ts` (StatusSlotRefeicao/RefeicaoHistorico), `frontend/src/pages/PainelCozinha.tsx` (lançamento avulso e cards dos quatro slots), `frontend/src/pages/PainelCozinha.css` (slots-grade, slot-card, slot-badges e estado-badge), `frontend/src/pages/CardapioPublico.tsx` e `CardapioPublico.css`.
  </read_first>
  <files>frontend/src/types.ts, frontend/src/pages/PainelCozinha.tsx, frontend/src/pages/PainelCozinha.css, frontend/src/pages/CardapioPublico.tsx, frontend/src/pages/CardapioPublico.css</files>
  <action>
    1. Espelhar nos tipos o contrato `nome_extra`, `slot` e `extra` retornado pelo backend. O tipo do cardápio público deve permitir múltiplas entradas no mesmo slot.
    2. No lançamento avulso, adicionar campo `Nome da refeição` (`nomeExtra`) no modal. O campo é obrigatório, tem limite de tamanho coerente com o backend e aparece antes dos ingredientes. O payload envia `nome_extra`.
    3. Mostrar o nome da refeição extra no card do slot quando `extra` for verdadeiro, mantendo a tag `EXTRA`.
    4. Padronizar os cards da cozinha: `slots-grade` com linhas de altura uniforme; `slot-card` com `height: 100%`, estrutura flexível e áreas de cabeçalho, nome, metadados e ação previsíveis; títulos longos não devem deslocar tags ou botões.
    5. Padronizar as tags: mesma altura mínima, `font-size`, `line-height`, padding, borda/arredondamento e alinhamento; usar apenas tokens existentes para Confirmado, Disponível, Pendente e EXTRA. A tag EXTRA deve ser visualmente distinta, mas não maior que as demais.
    6. Atualizar `CardapioPublico.tsx` para renderizar entradas como lista ordenada por slot, sem `Map` que elimine duplicatas. Extras aparecem com nome próprio e tag `EXTRA`; planejada e extra do mesmo slot aparecem em cards separados ou em uma composição claramente distinguível, sem ocultação.
    7. Preservar disclosure de ingredientes, estados vazio/erro/loading e responsividade do cardápio público.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - O modal avulso tem campo de nome e o payload contém `nome_extra`.
    - Cards da cozinha mantêm cabeçalho, tags, nome e botão alinhados com nomes/tags de comprimentos diferentes.
    - Confirmado, Disponível, Pendente e EXTRA têm dimensões visuais consistentes e usam tokens existentes.
    - Cardápio público renderiza duas entradas quando planejada e extra compartilham o slot.
    - `npm run build` e `npm run lint` passam.
  </acceptance_criteria>
  <done>A cozinheira nomeia extras, os cards ficam visualmente consistentes e o público vê todas as refeições servidas.</done>
</task>

<task type="auto">
  <name>task 3: Frontend — grid responsivo do Planejamento sem desalinhamento</name>
  <read_first>
    `frontend/src/pages/admin/Planejamento.tsx` (estrutura da tabela e badge-ruptura), `frontend/src/pages/admin/Planejamento.css`, `frontend/src/types.ts` (ProjecaoDia/ProjecaoSlot), `.planning/phases/08-improvements/08-UI-SPEC.md` se existir e os padrões responsivos de `CardapioPublico.css`.
  </read_first>
  <files>frontend/src/pages/admin/Planejamento.tsx, frontend/src/pages/admin/Planejamento.css</files>
  <action>
    1. Envolver o select e o aviso de cada célula em uma estrutura interna estável (por exemplo, `.planejamento-celula-conteudo`).
    2. Reservar uma área de altura mínima para o aviso de ruptura em todas as células, inclusive quando não houver aviso, para impedir que uma célula com badge altere o alinhamento visual dos slots.
    3. Definir a célula como layout vertical estável, com `min-width: 0`, select ocupando a largura disponível e badge limitado à célula com `overflow-wrap`/quebra controlada.
    4. Preservar a semântica de tabela e o overflow horizontal em telas estreitas; ajustar larguras mínimas das colunas e o container para que os quatro slots não comprimam o texto de forma imprevisível.
    5. Garantir que o banner/painel de projeção não altere a largura da grade nem introduza deslocamento horizontal inesperado.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Células com e sem aviso possuem estrutura e altura reservada equivalentes.
    - Selects e badges permanecem alinhados por coluna quando apenas algumas refeições possuem ruptura.
    - O grid continua navegável em 320px, 768px e desktop via overflow horizontal controlado.
    - `npm run build` e `npm run lint` passam.
  </acceptance_criteria>
  <done>O grid do Planejamento permanece alinhado e responsivo com qualquer combinação de avisos de falta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | nome da refeição extra atravessa a fronteira e é persistido; resposta pública é consumida sem autenticação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-08-17 | T (tampering) | POST /refeicoes | mitigate | Nome extra validado no backend, limitado pelo schema e exigido somente para avulsas; slot/tipo continuam derivados no servidor |
| T-08-18 | I (information disclosure) | /publico/cardapio | accept | Nome da refeição e ingredientes pertencem ao cardápio público já existente; não incluir usuário, justificativas ou dados de auditoria |
| T-08-19 | D (DoS) | cardápio com extras | mitigate | Resposta limitada às refeições da data solicitada e aos registros persistidos; sem consultas recursivas externas |
</threat_model>

<verification>
- `cd backend && venv/bin/python -m pytest tests/ -q` — suíte completa verde, incluindo regressões de refeições, status por slot e cardápio público.
- `cd frontend && npm run build && npm run lint` — typecheck, bundle e lint verdes.
- Migração `nome_extra` executada duas vezes no banco de desenvolvimento sem erro.
- Re-UAT manual: lançar extra chamada "Lasanha", conferir o card da cozinheira, `/cardapio` e coexistência com o prato planejado no mesmo slot.
- Re-UAT visual: cards da cozinha com títulos longos/tags diferentes e Planejamento com avisos em slots alternados, em 320px, 768px e desktop.
</verification>

<success_criteria>
- Refeições extras têm nome obrigatório, tag EXTRA e aparecem no cardápio público.
- Refeição planejada e extra no mesmo slot são exibidas simultaneamente.
- Cards da cozinha ficam alinhados e tags possuem padrão visual consistente.
- Grid de Planejamento não desalinha quando avisos de falta aparecem.
- Nenhuma regressão nos fluxos de lançamento planejado/avulso, cardápio público ou projeção.
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-11-SUMMARY.md`
</output>
