---
phase: 08-improvements
plan: 06
type: execute
wave: 5
depends_on: [08-05]
files_modified:
  - frontend/src/pages/admin/Entregas.tsx
  - frontend/src/pages/admin/Entregas.css
autonomous: true
requirements:
  - IMP-03
  - IMP-04
  - IMP-05

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Linha XML sem correspondência oferece a opção 'Cadastrar novo item' no select (D-11)."
    - "O modal de item tem os mesmos campos/validações do form de Itens (unidade livre, KG/L, fator de conversão; limiar default 5.0) (D-11)."
    - "Ao salvar, o novo item vincula-se à linha; o rascunho (demais linhas, XML, fornecedor, data) permanece; o usuário continua revisando antes do submit (D-12)."
    - "Linhas sem correspondência exibem sugestões assistivas (top 3) com confiança e motivo; nenhuma associação é automática (D-22)."
    - "D-23: o histórico preserva a descrição original do XML por linha — selecionar uma sugestão nunca altera descricaoNf/unidadeNf."
  artifacts:
    - path: "frontend/src/pages/admin/Entregas.tsx"
      provides: "Modal 'Cadastrar novo item' (form completo), vínculo item→linha, lista de sugestões por linha"
    - path: "frontend/src/pages/admin/Entregas.css"
      provides: "Estilos do modal inline e das sugestões"
  key_links:
    - from: "frontend/src/pages/admin/Entregas.tsx"
      to: "frontend/src/api.ts"
      via: "POST /itens/inline (admin+secretaria, 08-03)"
    - from: "frontend/src/pages/admin/Entregas.tsx"
      to: "frontend/src/pages/admin/matching.ts"
      via: "sugerirCandidatos(descricaoNf, itens) nas linhas sem itemId"
---

<objective>
Onboarding de item novo dentro do fluxo de Entregas: opção "Cadastrar novo item" no select de linha XML sem correspondência, modal com o form completo de item, vínculo ao rascunho sem perda, e sugestões assistivas por linha.

Purpose: Satisfazer IMP-03 (D-11/D-12) e IMP-04/05 (D-22/D-23) na UI, eliminando a interrupção "item não reconhecido" sem automatizar correspondências.
Output: Entregas.tsx/css com modal inline e sugestões, validado pelos gates do frontend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-RESEARCH.md
@.planning/phases/08-improvements/08-PATTERNS.md
@.planning/phases/08-improvements/08-UI-SPEC.md
@frontend/src/pages/admin/Entregas.tsx
@frontend/src/pages/admin/Entregas.css
@frontend/src/pages/admin/Itens.tsx
@frontend/src/pages/admin/matching.ts
@frontend/src/pages/admin/nfe.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Modal 'Cadastrar novo item' com form completo e vínculo à linha sem perder rascunho</name>
  <read_first>
    `frontend/src/pages/admin/Entregas.tsx` (estado de linhas L20-30/L41-44, select da linha ~L283-301, submit L374-430), `frontend/src/pages/admin/Itens.tsx` (form de item L100-165 — campos e validações a reusar), `frontend/src/pages/PainelCozinha.tsx` L245-266 (`handleDialogKeyDown` — padrão de diálogo acessível), `08-CONTEXT.md` D-11/D-12, `08-UI-SPEC.md` (modal item: título `Cadastrar novo item`, CTA `Salvar item`).
  </read_first>
  <files>frontend/src/pages/admin/Entregas.tsx</files>
  <action>
    1. No select de cada linha XML sem `itemId` (valor vazio), adicionar a opção de valor especial `"__novo__"` com o texto exato `Cadastrar novo item` (D-11). Selecionar essa opção abre o modal inline de item (em vez de fechar/limpar a linha).
    2. Modal `Cadastrar novo item` (estado `modalItem: { linhaIdx: number } | null`):
       - Campos (espelho do form de Itens — D-11): `nome` (obrigatório), `unidade_oficial` (obrigatório, com `UNIDADES_SUGERIDAS`), `unidade_interna` (select KG/L, default "KG"), `fator_conversao` (número > 0, habilitado quando unidade interna KG/L), `limiar` (número, default 5.0, mínimo 0.01). `saldo_atual` não é exibido — permanece 0 (o saldo vem da linha da entrega).
       - Validações locais iguais às do form de Itens (nome não vazio, fator > 0 quando KG/L, limiar > 0).
       - Submit: `POST /itens/inline` com `{ nome, unidade_oficial, unidade_interna, fator_conversao, limiar }` (R-2: endpoint de 08-03; admin e secretaria). Em 200:
         - `setLinhas(prev => prev.map((linha, i) => i === linhaIdx ? { ...linha, itemId: novoItem.id } : linha))` — vincula à linha (D-12);
         - adiciona o novo item ao catálogo local `itens` (para selects futuros);
         - **não confirma/submete a entrega** — o usuário continua revisando quantidade/unidade/ação (não-regressão F12);
         - fecha o modal; foco volta ao select da linha (retorno de foco).
       - Em erro (nome duplicado, etc.): mensagem inline no modal (padrão `erroSubmit` da página), modal permanece aberto com o rascunho do formulário do modal intacto.
       - Cancelar/Escape: fecha o modal; **nenhum estado do rascunho da entrega é alterado** (linhas, `numeroNota`, `emitente`, `dataEntrega`, `fornecedorId`, `observacoes`, `notaNumero` intactos — D-12).
    3. Acessibilidade do modal: padrão `handleDialogKeyDown` (foco inicial no primeiro campo, TrapTab, Escape fecha, retorno de foco ao elemento que abriu — como em `PainelCozinha.tsx`).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Entregas.tsx` contém a string exata `Cadastrar novo item` (opção do select) e `POST /itens/inline` via `fetchJson` (grep `'/itens/inline'`).
    - O handler de sucesso do modal chama `setLinhas` com `itemId: <id do novo item>` sem resetar os demais campos da linha (grep `...linha, itemId`).
    - Nenhuma chamada a `setLinhas([])`/reset do rascunho dentro do fluxo do modal (grep no corpo do modal → 0).
    - O payload do modal não contém `saldo_atual`.
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Item não reconhecido é cadastrado dentro do fluxo, vinculado à linha, sem perder o rascunho (IMP-03).</done>
</task>

<task type="auto">
  <name>task 2: Sugestões assistivas por linha XML sem correspondência</name>
  <read_first>
    `frontend/src/pages/admin/matching.ts` (`sugerirCandidatos`, `SugestaoCandidato` — 08-04), `frontend/src/pages/admin/nfe.ts` (`LinhaNfe.descricao` — descrição original da NF), `08-CONTEXT.md` D-22/D-23, `08-UI-SPEC.md` (copy `Sugestões`, motivo legível).
  </read_first>
  <files>frontend/src/pages/admin/Entregas.tsx</files>
  <action>
    1. Para cada linha com `itemId === null` e `descricaoNf` preenchida, exibir abaixo do select uma lista `Sugestões` com até 3 candidatos de `sugerirCandidatos(linha.descricaoNf, itens)` (D-23a — mesmo mecanismo das sugestões de fornecedor da 08-05):
       - Cada item: nome do item + motivo (ex.: `3 de 4 palavras batem`) e, quando houver, a descrição original da NF em texto suave (preservada no rascunho — D-23);
       - Clicar numa sugestão seleciona o item na linha (`itemId`) — **ação explícita do usuário**, nunca automática (D-22);
       - Sugestões calculadas por memoização simples (derivadas do estado `linhas` + `itens` — recompute ao trocar linha/catálogo; sem efeito colateral).
    2. Remover a opção `Cadastrar novo item` das sugestões (ela permanece apenas no select da linha — tarefa 1).
    3. Garantir que a escolha de uma sugestão NÃO altera `descricaoNf`/`unidadeNf` da linha (histórico preserva a descrição original do XML — D-23).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Entregas.tsx` chama `sugerirCandidatos` com a descrição da NF (grep `sugerirCandidatos(linha` ou equivalente com `descricaoNf`).
    - A lista de sugestões usa o rótulo exato `Sugestões` e exibe `motivo` de cada candidato (grep `Sugestões`).
    - O handler de clique em sugestão apenas seta `itemId` da linha (grep) — sem POST automático.
    - A linha mantém `descricaoNf` ao selecionar sugestão (nenhum `delete`/reset do campo no handler).
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Linhas XML sem correspondência ganham sugestões explicáveis e confirmáveis — sem associação silenciosa (IMP-04/05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | POST /itens/inline (dados digitados no modal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | T (tampering) | modal item inline | mitigate | Mesmas validações do form de Itens (nome obrigatório, fator > 0, limiar > 0); backend revalida (`POST /itens/inline` com ItemCreate + limiar 400) — frontend nunca é autoridade (ASVS V5.1) |
| T-08-10 | T (tampering) | sugestões de linha | accept | Sugestões são leitura derivada (matching puro); seleção exige clique explícito do usuário; nenhum POST automático |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — gates verdes.
- Backstop manual (08-VALIDATION.md): XML sem correspondência → sugestões → cadastro inline → submit sem perder rascunho.
</verification>

<success_criteria>
- IMP-03: item criado e vinculado no próprio fluxo, com rascunho preservado.
- IMP-04/05: sugestões com motivo; zero associações automáticas.
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-06-SUMMARY.md`
</output>
