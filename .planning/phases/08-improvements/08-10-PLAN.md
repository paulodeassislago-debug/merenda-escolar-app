---
phase: 08-improvements
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - backend/migracao.py
  - backend/models.py
  - backend/main.py
  - backend/schemas.py
  - backend/tests/test_entregas.py
  - backend/tests/test_planejamento.py
  - backend/tests/test_refeicoes.py
  - frontend/src/pages/admin/Entregas.tsx
  - frontend/src/pages/admin/Entregas.css
  - frontend/src/pages/admin/Planejamento.tsx
  - frontend/src/pages/admin/Planejamento.css
  - frontend/src/pages/PainelCozinha.tsx
  - frontend/src/pages/PainelCozinha.css
  - frontend/src/pages/DashboardGestao.tsx
  - frontend/src/types.ts
autonomous: true
requirements:
  - IMP-06
  - IMP-07
  - IMP-11
  - MEAL-02
  - IMP-09
  - IMP-10

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 6
  confidence: medium

must_haves:
  truths:
    - "GET /entregas e GET /entregas/{id} retornam id_usuario_nome (nome real) e observacoes na listagem (obs #3, #2)."
    - "A projeção é por slot: dias[].slots[].rupturas apontam apenas o slot em que o item faltará, e o endpoint aceita rascunho para pré-visualizar sem salvar (obs #5, #4)."
    - "Refeicao persiste o slot de lançamento; /refeicoes/hoje responde por slot com flag extra para avulsas (obs #7)."
    - "O botão 'Confirmar recebimento' nunca fica desabilitado em silêncio: linhas incompletas produzem mensagem visível (obs #1)."
    - "Listagem de Entregas mostra Fornecedor no lugar de 'Registrado por'; modal de detalhe mostra fornecedor, observações e registrado por (nome) (obs #2)."
    - "A célula da grade de Planejamento avisa apenas o slot afetado e reage à mudança do select sem salvar (obs #5, #4)."
    - "PainelCozinha ancora o foco no diálogo de descarte e mostra a refeição avulsa no slot com tag EXTRA (obs #6, #7)."
    - "DashboardGestao exibe fornecedor e observações nas entregas da data (obs #2d)."
  artifacts:
    - path: "backend/main.py"
      provides: "id_usuario_nome e observacoes nas entregas; projeção por slot com rascunho; status de refeições por slot com extra"
      contains: "id_usuario_nome"
    - path: "backend/models.py"
      provides: "Refeicao.slot"
      contains: "slot = Column"
    - path: "backend/migracao.py"
      provides: "ALTER TABLE refeicoes ADD COLUMN slot + backfill via planejamento"
      exports: ["migrar"]
    - path: "frontend/src/pages/admin/Entregas.tsx"
      provides: "Botão sem disable silencioso + coluna Fornecedor + modal de detalhe completo"
    - path: "frontend/src/pages/admin/Planejamento.tsx"
      provides: "Badge por slot com contrato novo + refetch com rascunho (debounce)"
    - path: "frontend/src/pages/PainelCozinha.tsx"
      provides: "Foco no alertdialog + slots com status por slot e tag EXTRA"
  key_links:
    - from: "backend/main.py"
      to: "backend/migracao.py"
      via: "migrar(engine) estendido com coluna slot de refeicoes"
    - from: "frontend/src/pages/admin/Planejamento.tsx"
      to: "frontend/src/types.ts"
      via: "tipos ProjecaoDia.slots / ProjecaoSlot"
---

<objective>
Corrigir as 7 observações do UAT manual da Fase 8 (08-UAT-ISSUES.md): botão de confirmação de entrega, detalhes de entrega (admin e secretaria), nome do usuário registrador, projeção reativa e por slot, foco do diálogo da cozinheira e refeição avulsa com tag EXTRA no slot.

Purpose: Fechar os gaps apontados pelo usuário sem alterar os contratos já validados (origem/fornecedor, alunos por período, projeção não-bloqueante, matching).
Output: 7 correções implementadas com testes backend verdes (140 baseline + novos) e build/lint frontend verdes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-UAT-ISSUES.md
@.planning/phases/08-improvements/08-RESEARCH.md
@.planning/phases/08-improvements/08-REVIEW.md
@.planning/phases/08-improvements/08-REVIEW-FIX.md
@backend/main.py
@backend/models.py
@backend/migracao.py
@backend/schemas.py
@backend/tests/test_entregas.py
@backend/tests/test_planejamento.py
@backend/tests/test_refeicoes.py
@frontend/src/pages/admin/Entregas.tsx
@frontend/src/pages/admin/Planejamento.tsx
@frontend/src/pages/PainelCozinha.tsx
@frontend/src/pages/DashboardGestao.tsx
@frontend/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Backend — nome do usuário e observações nas entregas (obs #3, #2d)</name>
  <read_first>
    `backend/main.py` GET /entregas (L~1135) e GET /entregas/{id} (L~1160), `backend/auth.py` (resolução de usuário), `backend/tests/test_entregas.py` (helpers F-tests), `08-UAT-ISSUES.md` obs #2/#3.
  </read_first>
  <files>backend/main.py, backend/tests/test_entregas.py</files>
  <action>
    Em `backend/main.py`:
    1. `listar_entregas`: adicionar `id_usuario_nome` (resolve `models.Usuario.nome` por `e.id_usuario`; null se não existir) e `observacoes` na serialização de cada entrega.
    2. `detalhar_entrega`: adicionar `id_usuario_nome` (mesma resolução).
    3. Não remover campos existentes (não-regressão — `id_usuario` permanece).

    Em `backend/tests/test_entregas.py`:
    4. `test_f6_lista_entregas_nome_usuario_e_obs`: criar entrega manual com observacoes; GET /entregas → item tem `id_usuario_nome` igual ao nome do usuário autenticado (admin) e `observacoes` igual ao enviado.
    5. `test_f7_detalhe_entrega_nome_usuario`: GET /entregas/{id} → `id_usuario_nome` presente e igual ao nome esperado.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_entregas.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/main.py` contém `id_usuario_nome` nas duas serializações de entregas (grep `id_usuario_nome` ≥ 2 ocorrências).
    - `backend/tests/test_entregas.py` contém `test_f6_lista_entregas_nome_usuario_e_obs` e `test_f7_detalhe_entrega_nome_usuario` passando.
    - `pytest tests/ -q` verde (baseline 140 + 2 novos).
  </acceptance_criteria>
  <done>Entregas listam e detalham com nome real do usuário registrador e observações disponíveis na listagem.</done>
</task>

<task type="auto">
  <name>task 2: Backend — projeção por slot + rascunho (obs #5, #4)</name>
  <read_first>
    `backend/main.py` `_simular_semana` (L~710-800) e `GET /planejamento/projecao` (L~860), `backend/tests/test_planejamento.py` (P1-P6), `08-CONTEXT.md` D-17/D-19, `08-UAT-ISSUES.md` obs #4/#5.
  </read_first>
  <files>backend/main.py, backend/tests/test_planejamento.py</files>
  <action>
    Em `backend/main.py` `_simular_semana`:
    1. Novo contrato `dias[]`: cada dia passa a conter `slots: [{slot, rupturas: [{item_id, nome, faltando, unidade_oficial}]}]` — **remover** `consumo` e `rupturas` de nível de dia (consumidor único é o frontend da 08-09). A ruptura é registrada no slot em que o saldo corrente do item **fica negativo pela primeira vez** (se o item já estava negativo em slot anterior do mesmo dia, não repete).
    2. `primeiro_dia_ruptura` continua calculado como hoje (dia 0-6 do primeiro saldo negativo).
    3. Novo parâmetro opcional em `GET /planejamento/projecao`: `rascunho: list[str] | None = Query(None)` — cada item no formato `"{dia}|{slot}|{cardapio_item_id}"` (ex.: `2|Almoço|3`). Entradas malformadas ou com slot inválido são **ignoradas** (pré-visualização não bloqueia — D-18). O rascunho sobrepõe `ativos` na simulação: para cada par (dia, slot), se houver entrada de rascunho, usa o `cardapio_item_id` do rascunho (valida existência do prato; se não existir, ignora e usa o planejado).
    4. Manter `configurado`/`itens`/`resumo` inalterados.

    Em `backend/tests/test_planejamento.py`:
    5. Revisar P2/P3 para o novo contrato (`dias[0]["slots"]`).
    6. Novos testes:
       - `test_p7_ruptura_por_slot`: cenário em que só a Janta rompe (ex.: saldo 5 KG, consumo 2 KG no Almoço e 6 KG na Janta no mesmo dia) → `dias[0].slots` tem `rupturas` apenas no slot Janta; o slot Almoço não lista o item.
       - `test_p8_rascunho_altera_projecao`: com planejamento de Almoço que não rompe, `rascunho=["2|Almoço|<id do prato que consome Batata>"]` → Batata aparece em rupturas do slot Almoço do dia 2; sem rascunho, não aparece.
       - `test_p9_rascunho_malformado_ignorado`: `rascunho=["lixo", "2|SlotInexistente|3"]` → 200 sem erro, resposta equivalente à sem rascunho.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_planejamento.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/main.py` `GET /planejamento/projecao` aceita `rascunho` e a resposta contém `dias[].slots[].rupturas` (grep `"slots"` na construção da resposta).
    - `dias[]` não contém mais `rupturas`/`consumo` de nível de dia (grep na serialização).
    - `test_planejamento.py` contém `test_p7_ruptura_por_slot`, `test_p8_rascunho_altera_projecao`, `test_p9_rascunho_malformado_ignorado` passando.
    - `pytest tests/ -q` verde.
  </acceptance_criteria>
  <done>Projeção com granularidade de slot e pré-visualização por rascunho — base para a UI reativa (task 5).</done>
</task>

<task type="auto">
  <name>task 3: Backend — slot persistido na refeição e status por slot com EXTRA (obs #7)</name>
  <read_first>
    `backend/models.py` (Refeicao), `backend/migracao.py` (padrão ALTER TABLE), `backend/main.py` `lancar_refeicao_v2` (L~1300-1420), `_status_refeicoes_do_dia` (L~1480), GET /refeicoes (L~1430), `backend/tests/test_refeicoes.py` (R16-R20), `08-CONTEXT.md` D-16/D-16b, `08-UAT-ISSUES.md` obs #7.
  </read_first>
  <files>backend/models.py, backend/migracao.py, backend/main.py, backend/tests/test_refeicoes.py</files>
  <action>
    Em `backend/models.py`:
    1. `Refeicao` ganha `slot = Column(String, nullable=True)` (nullable para backfill de legado; novos lançamentos sempre preenchem).

    Em `backend/migracao.py`:
    2. Se coluna `slot` ausente em `refeicoes` → `ALTER TABLE refeicoes ADD COLUMN slot VARCHAR`.
    3. Backfill idempotente: `UPDATE refeicoes SET slot = (SELECT tipo_refeicao FROM planejamento WHERE planejamento.id = refeicoes.planejamento_id) WHERE slot IS NULL AND planejamento_id IS NOT NULL`. Avulsas legadas (planejamento_id NULL) permanecem com slot NULL (aceito — sem como derivar o lanche).

    Em `backend/main.py`:
    4. `lancar_refeicao_v2`: gravar `slot=dados.slot` na criação da Refeicao.
    5. `historico_refeicoes` (GET /refeicoes): serializar `slot` e `extra` (`r.planejamento_id is None`).
    6. `_status_refeicoes_do_dia`: passar a iterar **`SLOTS_PLANEJAMENTO`** (4 slots) em vez de `TIPOS_REFEICAO_VALIDOS` (3 tipos); para cada slot, procura refeição do dia com `r.slot == slot` (fallback: se slot NULL — legado — casa por `tipo_refeicao` derivado do slot quando não houver outra). Resposta por slot: `{slot, status: "confirmado"|"pendente", extra: bool, prato, alunos}`. `prato` segue do planejamento (null quando avulsa). **Contrato de /refeicoes/hoje muda** — consumidores frontend atualizados na task 6.

    Em `backend/tests/test_refeicoes.py`:
    7. `test_r21_avulsa_ocupa_slot_com_extra`: configurar alunos; POST /refeicoes avulso com `slot="Lanche da Manhã"`; GET /refeicoes → item com `slot="Lanche da Manhã"` e `extra=true`; GET /refeicoes/hoje → slot "Lanche da Manhã" com `status="confirmado"` e `extra=true`.
    8. `test_r22_status_por_slot_planejado`: refeição com planejamento (Almoço) → slot "Almoço" `status="confirmado"`, `extra=false`; demais slots `pendente`.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_refeicoes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/models.py` contém `slot = Column` na classe Refeicao.
    - `backend/migracao.py` contém o ALTER de `refeicoes.slot` e o backfill via planejamento.
    - `backend/main.py` grava `slot=dados.slot` e serializa `slot`/`extra` em /refeicoes; `_status_refeicoes_do_dia` usa `SLOTS_PLANEJAMENTO`.
    - `test_refeicoes.py` contém `test_r21_avulsa_ocupa_slot_com_extra` e `test_r22_status_por_slot_planejado` passando.
    - `pytest tests/ -q` verde (test_dashboard.py pode exigir ajuste no formato de /refeicoes/hoje — ajustar asserts).
  </acceptance_criteria>
  <done>Refeições persistem o slot; o status do dia é por slot e avulsas são marcadas `extra` — base da tag EXTRA na UI.</done>
</task>

<task type="auto">
  <name>task 4: Frontend Entregas — botão sem disable silencioso e detalhes da entrega (obs #1, #2, #3)</name>
  <read_first>
    `frontend/src/pages/admin/Entregas.tsx` (`podeSubmeter` L856-873, `handleSubmit` L741-810, listagem L935-960, modal de detalhe ~L1200), `frontend/src/types.ts` (EntregaResumo/EntregaDetalhe — adicionar `id_usuario_nome`/`observacoes`), `frontend/src/pages/admin/Entregas.css`, `08-UAT-ISSUES.md` obs #1/#2/#3.
  </read_first>
  <files>frontend/src/types.ts, frontend/src/pages/admin/Entregas.tsx, frontend/src/pages/admin/Entregas.css</files>
  <action>
    1. `types.ts`: `EntregaResumo` ganha `id_usuario_nome: string | null` e `observacoes: string | null`; `EntregaDetalhe` ganha `id_usuario_nome: string | null` (fornecedor/obs já existem).
    2. **obs #1**: `podeSubmeter()` passa a retornar true sempre que houver linhas ativas e `!salvando` (remove os bloqueios silenciosos de itemId/quantidade/unidade); as validações já existentes em `handleSubmit` mostram `erroSubmit` com a mensagem exata. Além disso, sob o botão, exibir hint dinâmico quando houver linhas ativas incompletas: `N linha(s) sem item vinculado — selecione ou cadastre o item para continuar` (contagem de `ativas.filter(l => l.itemId === null)`), também quando houver quantidade inválida.
    3. **obs #2/#3**: na listagem, a coluna "Registrado por" (que mostra `e.id_usuario`) passa a mostrar **`e.fornecedor_nome`** com cabeçalho "Fornecedor" (fallback "—" se nulo). O modal "Ver detalhes" passa a exibir, no topo: **Fornecedor** (fornecedor_nome), **Observações** (observacoes, com estado vazio "—"), **Registrado por** (`id_usuario_nome`) e Data da entrega; mantém a lista de itens com justificativas.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` contém `id_usuario_nome` em EntregaResumo e EntregaDetalhe, e `observacoes` em EntregaResumo.
    - `Entregas.tsx`: a tabela da listagem tem cabeçalho "Fornecedor" (grep) e não exibe `e.id_usuario` na listagem; o modal de detalhe renderiza fornecedor/observações/registrado por.
    - `Entregas.tsx`: botão "Confirmar recebimento" usa `disabled={ativas.length === 0 || salvando}` e existe o hint de linhas sem item.
    - `npm run build` e `npm run lint` verdes.
  </acceptance_criteria>
  <done>Botão nunca mais "não responde": linhas incompletas mostram erro/hint; detalhes de entrega completos no admin.</done>
</task>

<task type="auto">
  <name>task 5: Frontend Planejamento — badge por slot e projeção reativa (obs #4, #5)</name>
  <read_first>
    `frontend/src/pages/admin/Planejamento.tsx` (carregarProjecao L100-113, badge L357-397, painel L437+), `frontend/src/types.ts` (ProjecaoDia — trocar por `slots: ProjecaoSlot[]`), `frontend/src/pages/admin/Planejamento.css`, `08-UAT-ISSUES.md` obs #4/#5.
  </read_first>
  <files>frontend/src/types.ts, frontend/src/pages/admin/Planejamento.tsx, frontend/src/pages/admin/Planejamento.css</files>
  <action>
    1. `types.ts`: novo tipo `ProjecaoSlot { slot: string; rupturas: ProjecaoRuptura[] }`; `ProjecaoDia` troca `consumo`/`rupturas` por `slots: ProjecaoSlot[]`.
    2. **obs #5**: o badge da célula passa a consultar, no dia, o slot daquela célula: `diaProjecao.slots.find((s) => s.slot === slotDaCelula)` — o tooltip lista apenas os itens que faltam **naquele slot**; badge renderizado apenas quando `slots` do slot tem rupturas (célula sem aviso não exibe badge).
    3. **obs #4**: ao mudar o valor de qualquer select da grade, disparar refetch da projeção com o rascunho: `carregarProjecao(segunda, rascunho)` com `rascunho` montado do estado da grade (`dia_semana|slot|cardapio_item_id` para cada célula preenchida, incluindo as alteradas) e **debounce ~300ms** (useRef + setTimeout, cancelando o anterior). Manter: falha isolada (grade/save nunca quebram), `projecaoRef`, semanaRef.
    4. O painel "Projeção da semana" (itens/resumo) continua consumindo `itens[]` — inalterado; apenas o badge usa o novo contrato. Banner pós-save permanece.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` contém `ProjecaoSlot` e `ProjecaoDia.slots` (sem `rupturas` de nível de dia).
    - `Planejamento.tsx` busca por `s.slot === slotDaCelula` (grep `find((s) => s.slot`) e monta rascunho com `dia|slot|id` (grep `rascunho`).
    - Badge renderiza apenas para slot com rupturas; `npm run build` e `npm run lint` verdes.
  </acceptance_criteria>
  <done>Badge por refeição e projeção atualizada ao mudar o select, sem salvar.</done>
</task>

<task type="auto">
  <name>task 6: Frontend Cozinheira + Secretaria — foco do diálogo, tag EXTRA e entregas no gestao (obs #6, #7, #2d)</name>
  <read_first>
    `frontend/src/pages/PainelCozinha.tsx` (dialogRef L100, slots L699-710, alertdialog L845-860, foco L300), `frontend/src/pages/PainelCozinha.css`, `frontend/src/pages/DashboardGestao.tsx` (seção entregas L277-300), `frontend/src/types.ts` (RefeicaoHistorico — adicionar slot/extra; /refeicoes/hoje com slots), `08-UAT-ISSUES.md` obs #2d/#6/#7.
  </read_first>
  <files>frontend/src/types.ts, frontend/src/pages/PainelCozinha.tsx, frontend/src/pages/PainelCozinha.css, frontend/src/pages/DashboardGestao.tsx</files>
  <action>
    1. `types.ts`: `RefeicaoHistorico` ganha `slot: string | null` e `extra: boolean`; novo tipo para /refeicoes/hoje: `StatusSlotRefeicao { slot: string; status: "confirmado"|"pendente"; extra: boolean; prato: string | null; alunos: number | null }`.
    2. **obs #7**: PainelCozinha — os cards de slot passam a usar o novo contrato de /refeicoes/hoje (4 slots): `status === "confirmado"` → badge "Confirmado"; quando `extra === true` → badge adicional **"EXTRA"** (classe própria, p.ex. `estado-badge-extra`); `slotConfirmado`/`entrada.id` passam a ser derivados do slot confirmado (manter o fluxo de seleção por planejamento). Texto de sucesso continua.
    3. **obs #6**: no `alertdialog` de descarte (rascunho não salvo), ao abrir: ancorar o foco no diálogo (primeiro botão ou título via ref do alertdialog) e, ao fechar, retornar o foco ao elemento que abriu; `aria-labelledby` já presente. Se o padrão de foco existente (focoPossivel) servir, reutilizar; senão adicionar gestão específica para o alertdialog.
    4. **obs #2d**: DashboardGestao — na lista de entregas da data, cada item passa a exibir o **fornecedor** (`fornecedor_nome`, fallback "—") e as **observações** (fallback "—"); usa `EntregaResumo` já estendido na task 4.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` contém `slot`/`extra` em RefeicaoHistorico e `StatusSlotRefeicao`.
    - `PainelCozinha.tsx` renderiza badge EXTRA quando `extra` (grep `EXTRA`) e ancora foco no alertdialog (grep `focus` perto de `alertdialog`/`descarte`).
    - `DashboardGestao.tsx` renderiza `fornecedor_nome` e `observacoes` na seção de entregas (grep).
    - `npm run build` e `npm run lint` verdes.
  </acceptance_criteria>
  <done>Cozinheira com foco correto e refeição avulsa marcada EXTRA no slot; secretaria vê fornecedor e observações das entregas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | rascunho da projeção e status de refeições cruzam a fronteira (leitura) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | T (tampering) | `rascunho` em /planejamento/projecao | mitigate | Entradas malformadas/inexistentes são ignoradas (pré-visualização); nunca persiste nada; validação de slot e prato antes de usar (ASVS V5.1) |
| T-08-15 | I (info disclosure) | /refeicoes/hoje por slot | mitigate | Mesmos perfis de leitura atuais (admin/cozinheira); sem dado novo sensível |
| T-08-16 | D (DoS) | rascunho na projeção | accept | Máximo 28 células (7 dias × 4 slots) por requisição; simulação local |
</threat_model>

<verification>
- `cd backend && venv/bin/python -m pytest tests/ -q` — suíte completa verde (baseline 140 + F6/F7 + P7/P8/P9 + R21/R22; ajustes em test_dashboard.py se /refeicoes/hoje mudar o shape).
- `cd frontend && npm run build && npm run lint` — typecheck e lint verdes.
- Execução dupla da migração no merenda.db dev: idempotente (coluna slot + backfill).
- Re-UAT manual dos 7 pontos em 08-UAT-ISSUES.md após a execução.
</verification>

<success_criteria>
- obs #1: botão "Confirmar recebimento" responde sempre com feedback visível.
- obs #2/#2d: fornecedor na listagem de entregas; modal e rota da secretaria com fornecedor + observações; "registrado por" só no modal, com nome (obs #3).
- obs #4: projeção atualiza ao mudar o select, sem salvar.
- obs #5: aviso de falta aparece apenas no slot/refeição afetada.
- obs #6: foco ancorado no diálogo "fechar ou salvar".
- obs #7: refeição avulsa ocupa o slot com a tag EXTRA.
- Nenhuma regressão nos fluxos validados (entrega manual/XML, lançamento por slot, projeção, matching, limiar).
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-10-SUMMARY.md`
</output>
