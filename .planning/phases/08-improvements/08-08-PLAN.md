---
phase: 08-improvements
plan: 08
type: execute
wave: 5
depends_on: [08-07]
files_modified:
  - frontend/src/pages/admin/Alunos.tsx
  - frontend/src/pages/admin/Alunos.css
  - frontend/src/App.tsx
  - frontend/src/pages/PainelCozinha.tsx
  - frontend/src/types.ts
autonomous: true
requirements:
  - IMP-09
  - IMP-10
  - MEAL-02

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Admin configura manhã/tarde/noite em página própria; a cozinheira não acessa (D-14)."
    - "PainelCozinha não tem campo 'Quantos alunos foram atendidos?'; a receita escala pelo total do slot derivado da configuração (D-16)."
    - "Lançamento avulso (sem planejamento) seleciona um dos 4 slots; o backend deriva tipo e total (D-16b)."
    - "Configuração ausente é estado explícito na UI (mensagem de ativação), sem crash (D-19)."
  artifacts:
    - path: "frontend/src/pages/admin/Alunos.tsx"
      provides: "Página de configuração (3 campos numéricos, PUT, estados de carregamento/erro/vazio)"
    - path: "frontend/src/pages/admin/Alunos.css"
      provides: "Estilos co-localizados da página"
    - path: "frontend/src/App.tsx"
      provides: "Rota /admin/alunos protegida por perfil admin"
    - path: "frontend/src/pages/PainelCozinha.tsx"
      provides: "Total por slot vindo da config; lançamento avulso por slot; payload {slot, planejamento_id, itens}"
  key_links:
    - from: "frontend/src/pages/admin/Alunos.tsx"
      to: "frontend/src/api.ts"
      via: "fetchJson GET/PUT '/alunos-por-periodo'"
    - from: "frontend/src/pages/PainelCozinha.tsx"
      to: "frontend/src/api.ts"
      via: "fetchJson<AlunosPorPeriodo>('/alunos-por-periodo') (GET — cozinheira tem leitura)"
---

<objective>
Frontend dos alunos fixos por período: página admin de configuração (rota nova) e revisão do PainelCozinha — remoção do campo de alunos digitados, escala pela configuração e lançamento avulso por slot.

Purpose: Satisfazer IMP-09/10 e MEAL-02 na UI (D-14..D-16b), com estados explícitos quando a configuração ainda não existe.
Output: Página Alunos.tsx + PainelCozinha revisado, validados pelos gates do frontend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-RESEARCH.md
@.planning/phases/08-improvements/08-PATTERNS.md
@.planning/phases/08-improvements/08-UI-SPEC.md
@frontend/src/App.tsx
@frontend/src/pages/PainelCozinha.tsx
@frontend/src/pages/admin/constants.ts
@frontend/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Página admin Alunos.tsx com rota /admin/alunos</name>
  <read_first>
    `frontend/src/App.tsx` (padrão de rotas protegidas L28-56 — ProtectedRoute + Layout), `frontend/src/pages/admin/Itens.tsx` (padrão de página admin com form e estados), `frontend/src/types.ts`, `08-UI-SPEC.md` (página `Alunos por período`: campos Manhã/Tarde/Noite, CTA `Salvar configuração`).
  </read_first>
  <files>frontend/src/pages/admin/Alunos.tsx, frontend/src/pages/admin/Alunos.css, frontend/src/App.tsx</files>
  <action>
    1. Criar `frontend/src/pages/admin/Alunos.tsx` (padrão das páginas admin — header `pagina-header`, `fetchJson`, estados carregando/erro):
       - Estado: `manha`, `tarde`, `noite` (strings para o input), `carregando`, `erro`, `salvando`, `sucesso`.
       - Ao montar: `GET /alunos-por-periodo`. 404 → estado "não configurado" (campos default vazios + mensagem `Configure os alunos por período para ativar a projeção.`); 200 → preenche campos; 401 → `Sua sessão expirou. Entre novamente.` (padrão das páginas).
       - Form (título `Alunos por período`): 3 inputs numéricos `Manhã`, `Tarde`, `Noite` (`type="number"`, `min="1"`, `step="1"`, obrigatórios), ajuda: `A receita escala pelo total do período de cada refeição.` (exato — UI-SPEC).
       - Submit: `PUT /alunos-por-periodo` com `{ manha: Number(manha), tarde: Number(tarde), noite: Number(noite) }`; validação local `> 0`; sucesso → mensagem `Configuração salva.` (padrão `sucessoMsg`); erro → mensagem do backend.
       - Exibir `updated_at` quando presente (texto suave: `Atualizado em {data legível pt-BR}`).
    2. Criar `frontend/src/pages/admin/Alunos.css` co-localizado (padrão das páginas Itens/Entregas; tokens existentes).
    3. Em `frontend/src/App.tsx`: adicionar rota `/admin/alunos` com `<ProtectedRoute perfis={['admin']}>` renderizando a página dentro do `Layout` (mesmo padrão de `/admin/itens` L48-56).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/admin/Alunos.tsx` contém labels `Manhã`, `Tarde`, `Noite` e o CTA exato `Salvar configuração`.
    - `Alunos.tsx` chama `GET` e `PUT '/alunos-por-periodo'` (grep `'/alunos-por-periodo'`).
    - `Alunos.tsx` trata 404 com a mensagem exata `Configure os alunos por período para ativar a projeção.`
    - `App.tsx` contém `path="/admin/alunos"` com `ProtectedRoute perfis={['admin']}`.
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Admin configura alunos por período em página própria; estados de vazio/erro/sucesso explícitos.</done>
</task>

<task type="auto">
  <name>task 2: PainelCozinha — remover input de alunos e escalar pela configuração</name>
  <read_first>
    `frontend/src/pages/PainelCozinha.tsx` (estado `qtdAlunos` L90 e usos L211-289, L394-420, L577-594; `carregarReceita` L155-203; `tipoParaLancamento` L59-61; `SLOTS_REFEICAO` L7), `frontend/src/types.ts`, `08-CONTEXT.md` D-16.
  </read_first>
  <files>frontend/src/pages/PainelCozinha.tsx</files>
  <action>
    1. Novo estado `alunosConfig: { manha: number; tarde: number; noite: number } | null` carregado no mount com `GET /alunos-por-periodo` (cozinheira tem leitura — 08-07). 404 → `alunosConfig = null` (estado explícito).
    2. Helper `totalPorSlot(slot: SlotRefeicao): number | null` espelhando D-15: Lanche da Manhã → manha; Almoço → manha+tarde; Lanche da Tarde → tarde; Janta → noite; `alunosConfig === null` → null.
    3. **Remover** o campo `Quantos alunos foram atendidos?` (bloco L577-594: label/input/ajuda) e o estado `qtdAlunos` (L90) com todos os usos: `carregarReceita(entrada, qtdAlunos)` passa a `carregarReceita(entrada, totalPorSlot(entrada.tipo_refeicao as SlotRefeicao) ?? 0)` — em `abrirEditor` (L211) e no fluxo de load. `rascunhoTemAlteracoes` (L220) deixa de depender de `qtdAlunos` (usa `ingredientes.length > 0 || itemParaAdicionar !== ''`).
    4. Ao abrir o editor de um slot: se `totalPorSlot` é null → exibir o estado explícito `A configuração de alunos por período ainda não foi definida pelo admin.` (UI-SPEC) e desabilitar o carregamento da receita.
    5. Substituir `quantidadeAlunosValida(qtdAlunos)` (L394, L584, L593, L596, L597, L603, L608, L709) por checagem `totalPorSlot(...) !== null` (config válida) — o botão submit e o carregamento dependem da config, não de digitação.
    6. Textos que usam `qtdAlunos` (L645 `Esperada para {qtdAlunos} alunos`) passam a usar o total do slot derivado (mesmo valor gravado no backend).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `PainelCozinha.tsx` não contém `qtdAlunos` (grep `qtdAlunos` → 0) nem o texto `Quantos alunos foram atendidos?`.
    - `PainelCozinha.tsx` contém `totalPorSlot` com as 4 regras e `'/alunos-por-periodo'` no GET de config.
    - `PainelCozinha.tsx` contém a string exata `A configuração de alunos por período ainda não foi definida pelo admin.`
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Cozinheira lança refeições sem digitar alunos; receita escala pelo total do slot (D-16).</done>
</task>

<task type="auto">
  <name>task 3: Lançamento avulso por slot e payload RefeicaoCreate revisado</name>
  <read_first>
    `frontend/src/pages/PainelCozinha.tsx` (fluxo avulso: `entradasDoDia` L451, submit L415-425, `tipoParaLancamento` L59-61), `frontend/src/types.ts`, `08-CONTEXT.md` D-16b, `08-RESEARCH.md` R-5.
  </read_first>
  <files>frontend/src/pages/PainelCozinha.tsx, frontend/src/types.ts</files>
  <action>
    Em `frontend/src/types.ts`:
    1. Adicionar `export interface RefeicaoCreatePayload { slot: string; planejamento_id?: number | null; itens: RefeicaoItemRequest[]; }` — sem `tipo_refeicao`/`qtd_alunos` (R-5).

    Em `frontend/src/pages/PainelCozinha.tsx`:
    2. No submit (L415-425), trocar o corpo para o novo payload: `{ slot: entradaSelecionada.tipo_refeicao, planejamento_id: entradaSelecionada.id, itens }` (o slot do planejamento já é um dos 4; remover `tipo_refeicao: tipoParaLancamento(...)` e `qtd_alunos`). O backend deriva tipo e total (08-07).
    3. **Lançamento avulso** (D-16b): quando não há entrada planejada para o dia (ou na área de lançamento livre), oferecer um select com os 4 slots de `SLOTS_REFEICAO` (`Lanche da Manhã`, `Almoço`, `Lanche da Tarde`, `Janta`) e o fluxo de itens avulsos:
       - Novo estado `slotAvulso: SlotRefeicao | null`; seleção do slot habilita o formulário de lançamento avulso (sem `planejamento_id`).
       - O submit avulso envia `{ slot: slotAvulso, planejamento_id: null, itens }` (a receita base avulsa não tem escala esperada — divergência exige justificativa conforme regra atual).
       - Mensagem de sucesso vinda do backend (`Refeição servida a {N} alunos! Estoque abatido com sucesso.`) é exibida como hoje.
    4. Se `alunosConfig === null`, o lançamento avulso também mostra o estado explícito da tarefa 2 e desabilita o submit.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` contém `RefeicaoCreatePayload` com `slot` e sem `qtd_alunos`/`tipo_refeicao`.
    - `PainelCozinha.tsx` não envia `qtd_alunos` nem `tipo_refeicao` no POST /refeicoes (grep no corpo do submit → ausentes).
    - `PainelCozinha.tsx` contém select de avulso com os 4 slots (`SLOTS_REFEICAO.map`) e submit avulso com `planejamento_id: null`.
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Avulso por slot eliminando a ambiguidade entre lanches (D-16b); payload novo em toda a superfície.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | PUT /alunos-por-periodo (admin) e POST /refeicoes (cozinheira) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | T (tampering) | PainelCozinha submit | mitigate | Payload envia apenas `slot` — tipo e qtd derivados no backend (nunca confiáveis do cliente); validação local só desbloqueia UI (ASVS V5.1) |
| T-08-14 | I (info disclosure) | Alunos.tsx | mitigate | Rota protegida `perfis={['admin']}` (frontend) + `require_perfil("admin")` no PUT (backend) — dupla camada (ASVS V4.1) |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — gates verdes.
- Backstop manual (08-VALIDATION.md): cozinheira lança do planejamento e avulso sem digitar alunos; admin configura na nova página.
</verification>

<success_criteria>
- IMP-09: admin configura alunos por período; receita escala pelo total derivado.
- IMP-10 + MEAL-02: cozinheira não digita alunos; avulso por slot; `qtd_alunos` gravado vem da configuração.
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-08-SUMMARY.md`
</output>
