---
phase: 08-improvements
plan: 05
type: execute
wave: 4
depends_on: [08-03, 08-04]
files_modified:
  - frontend/src/pages/admin/Entregas.tsx
  - frontend/src/pages/admin/Entregas.css
  - frontend/src/pages/admin/nfe.ts
autonomous: true
requirements:
  - IMP-06
  - IMP-07
  - IMP-08
  - IMP-04
  - IMP-05

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "O form de Entregas envia origem, data_entrega (default hoje no manual), fornecedor_id, observacoes (manual) e nota_numero (XML) (D-05, D-07, D-09)."
    - "Upload XML pré-preenche data de emissão, fornecedor (emitente, confirmável) e número da nota (D-09)."
    - "O fornecedor é escolhido por autocomplete com sugestões (matching) ou cadastrado inline no próprio form (D-08)."
    - "Origem manual oferece somente ação 'recebido'; origem XML mantém o modal de justificativa (D-07)."
    - "Nenhum rascunho é perdido ao abrir/fechar o cadastro inline de fornecedor (D-12)."
  artifacts:
    - path: "frontend/src/pages/admin/Entregas.tsx"
      provides: "Campos novos (data/fornecedor/obs/nota), autocomplete com sugestões, modal inline de fornecedor, regras de ação por origem, payload EntregaCreatePayload"
    - path: "frontend/src/pages/admin/nfe.ts"
      provides: "Extração de dhEmi (data de emissão) para prefill de data_entrega"
    - path: "frontend/src/pages/admin/Entregas.css"
      provides: "Estilos dos novos campos, autocomplete e modal inline"
  key_links:
    - from: "frontend/src/pages/admin/Entregas.tsx"
      to: "frontend/src/api.ts"
      via: "fetchJson<Fornecedor[]>('/fornecedores') e POST /fornecedores no modal inline"
    - from: "frontend/src/pages/admin/Entregas.tsx"
      to: "frontend/src/pages/admin/matching.ts"
      via: "sugerirCandidatos(texto, fornecedores) no autocomplete"
    - from: "frontend/src/pages/admin/nfe.ts"
      to: "frontend/src/pages/admin/Entregas.tsx"
      via: "NfeParseResult com dataEmissao preenchendo data_entrega"
---

<objective>
Revisão do form de Entregas: origem, data da entrega, fornecedor com autocomplete/sugestões e cadastro inline, observações (manual) e número da nota (XML), pré-preenchimento a partir da NF, e regras de ação por origem na UI.

Purpose: Satisfazer IMP-06/07/08 e D-08/D-09 na superfície usada por admin e secretaria, sem perder o rascunho e sem quebrar o fluxo F12 validado.
Output: Entregas.tsx/css revisados e validados pelos gates do frontend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-RESEARCH.md
@.planning/phases/08-improvements/08-PATTERNS.md
@.planning/phases/08-improvements/08-UI-SPEC.md
@frontend/src/pages/admin/Entregas.tsx
@frontend/src/pages/admin/Entregas.css
@frontend/src/pages/admin/nfe.ts
@frontend/src/pages/admin/matching.ts
@frontend/src/types.ts
@frontend/src/api.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Extrair data de emissão da NF e adicionar campos de origem/data/nota/observações ao form</name>
  <read_first>
    `frontend/src/pages/admin/nfe.ts` (parseNfe L47-99 — ide L65-68), `frontend/src/pages/admin/Entregas.tsx` (estados L34-60, fluxo escolha L126+, linhas L20-30, submit L374-430), `frontend/src/types.ts` (`EntregaCreatePayload`/`OrigemEntrega` da 08-01), `08-UI-SPEC.md` (campos novos e copy).
  </read_first>
  <files>frontend/src/pages/admin/nfe.ts, frontend/src/pages/admin/Entregas.tsx</files>
  <action>
    Em `frontend/src/pages/admin/nfe.ts` (per D-09):
    1. No `NfeParseResult` (L21-25), adicionar `dataEmissao: string | null` (YYYY-MM-DD).
    2. No parse, extrair `const dhEmi = String((ide as Record<string, unknown>)?.dhEmi ?? '')`; converter o primeiro segmento da data ISO (`dhEmi.slice(0, 10)`) e validar com `Date`; `dataEmissao = valid ? slice : null`.

    Em `frontend/src/pages/admin/Entregas.tsx` (per D-05/D-07/D-09):
    3. Estado novo: `origem: OrigemEntrega` (default `'manual'`), `dataEntrega: string` (default hoje — `new Date().toISOString().slice(0,10)`), `fornecedorId: number | null`, `notaNumero: string`, `observacoes: string`.
    4. Ao parsear XML (`handleUpload`/parseNfe, ~L330): setar `origem='xml'`, `dataEntrega = resultado.dataEmissao ?? hoje`, `notaNumero = resultado.numeroNota ?? ''`; `emitente` continua em estado (usado na tarefa 2 para sugerir fornecedor). Ao escolher fluxo manual: `origem='manual'`, `dataEntrega = hoje`.
    5. No cabeçalho do form (editando), renderizar:
       - `Data da entrega` — `<input type="date" id="data-entrega">` obrigatório, value `dataEntrega`, onChange atualiza (desabilitar edição quando pré-preenchida de XML? Não — permanece editável, D-09: "confirmável").
       - `Número da nota` — input text, **obrigatório e visível apenas quando `origem === 'xml'`** (placeholder `Número da NF-e`), value `notaNumero`.
       - `Observações` — `<textarea id="observacoes">`, **obrigatório (aria-required) quando `origem === 'manual'`** (D-07), opcional quando xml.
    6. No `handleSubmit` (L374-430): enviar `EntregaCreatePayload` completo — `{ origem, data_entrega: dataEntrega, fornecedor_id: fornecedorId, nota_numero: origem === 'xml' ? notaNumero : null, observacoes: origem === 'manual' ? observacoes : null, itens }` (conforme `EntregaCreatePayload` em types.ts). Validações locais antes do POST: `dataEntrega` preenchida; `fornecedorId` não nulo; manual → `observacoes.trim() !== ''`; xml → `notaNumero.trim() !== ''`; mensagens de erro inline (padrão `erroSubmit` existente).
    7. No `resposta` de sucesso (L427+), limpar os estados novos junto com os existentes.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `nfe.ts` `NfeParseResult` contém `dataEmissao: string | null` e o parse preenche a partir de `dhEmi`.
    - `Entregas.tsx` contém estados `origem`, `dataEntrega`, `fornecedorId`, `notaNumero`, `observacoes`.
    - O payload do submit inclui `origem`, `data_entrega`, `fornecedor_id`, `nota_numero`, `observacoes` (grep no corpo do POST).
    - Condicional de UI: `notaNumero` renderizada quando `origem === 'xml'` e `observacoes` com `aria-required` quando `origem === 'manual'` (grep `origem === 'xml'` / `origem === 'manual'`).
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Form envia o contrato completo de entrega; XML pré-preenche data, nota e fornecedor pendente de confirmação.</done>
</task>

<task type="auto">
  <name>task 2: Autocomplete de fornecedor com sugestões e cadastro inline</name>
  <read_first>
    `frontend/src/pages/admin/matching.ts` (`sugerirCandidatos`, `SugestaoCandidato` — da 08-04), `frontend/src/pages/admin/Entregas.tsx` (estado/ciclo de load L63-92, form), `frontend/src/api.ts` (`fetchJson`), `08-UI-SPEC.md` (modal fornecedor: título/campos/CTA).
  </read_first>
  <files>frontend/src/pages/admin/Entregas.tsx</files>
  <action>
    1. Carregar fornecedores no ciclo inicial (Promise.all do useEffect L66-69): adicionar `fetchJson<Fornecedor[]>('/fornecedores')`; estado `fornecedores: Fornecedor[]`. Tratar erro de `/fornecedores` sem derrubar a página (campo vira "não disponível" com mensagem de retry).
    2. Campo `Fornecedor` no form: input text com datalist/lista de sugestões:
       - Enquanto o usuário digita (>= 1 caractere), `sugerirCandidatos(textoDigitado, fornecedores)` → lista de até 3 sugestões com `nome` + `motivo` (copy: `Sugestões`), navegável por teclado (setas + Enter seleciona) e clicável; selecionar seta `fornecedorId` + mostra nome escolhido.
       - Exibir CNPJ quando presente (`{nome} — CNPJ {cnpj}` na sugestão).
       - Opção fixa no fim da lista: `Cadastrar fornecedor` (D-08).
    3. Modal inline de fornecedor (padrão de diálogo de `PainelCozinha.tsx` `handleDialogKeyDown` — foco inicial, Escape fecha, retorno de foco):
       - Título `Cadastrar fornecedor`; campos `Nome` (obrigatório) e `CNPJ` (opcional, texto); CTA `Salvar fornecedor` + `Cancelar`.
       - Submit: `POST /fornecedores` com `{ nome, cnpj }` → adiciona à lista `fornecedores`, seleciona (`fornecedorId = novo.id`, nome exibido), fecha modal.
       - **Não resetar** `linhas`/`numeroNota`/`emitente`/campos do form ao abrir/fechar (D-12) — o modal é estado isolado.
    4. Prefill XML (D-09): quando `emitente` (da NF) existir, pré-selecionar a melhor sugestão `sugerirCandidatos(emitente, fornecedores)` se `confianca` alta (>= 0.6) — ainda **confirmável**: o campo fica preenchido mas o usuário pode trocar; nunca faz match silencioso irreversível (D-22).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Entregas.tsx` chama `sugerirCandidatos` (import de `./matching`) para fornecedores (grep `sugerirCandidatos`).
    - Existe `POST /fornecedores` via `fetchJson` no fluxo do modal (grep `'/fornecedores'` com `method: 'POST'`).
    - O modal não chama `setLinhas([])` nem limpa `numeroNota` (grep do corpo do modal → rascunho intacto).
    - Pré-seleção XML usa `confianca >= 0.6` e mantém campo editável.
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>Fornecedor escolhido por sugestão ou criado inline sem sair da página e sem perder rascunho (D-08, D-12).</done>
</task>

<task type="auto">
  <name>task 3: Regras de ação por origem na UI e estilos dos novos elementos</name>
  <read_first>
    `frontend/src/pages/admin/Entregas.tsx` (select de ação das linhas ~L283-301, modal de justificativa L853-879, guarda de justificativas L400-408), `frontend/src/pages/admin/Entregas.css` (estrutura atual), `08-UI-SPEC.md` (spacing/tokens/copy).
  </read_first>
  <files>frontend/src/pages/admin/Entregas.tsx, frontend/src/pages/admin/Entregas.css</files>
  <action>
    1. No select de ação de cada linha (L283+): quando `origem === 'manual'`, renderizar somente a opção `recebido` (select desabilitado com valor fixo ou select com única opção — escolha do executor; o payload envia `acao: 'recebido'`); quando `xml`, manter as três opções e o fluxo de justificativa atual (modal L853-879 e guarda L400-408 intactos).
    2. No submit: para `origem === 'manual'`, forçar `justificativa: null` em todas as linhas (backend já ignora/grava None — D-07).
    3. `Entregas.css`: estilos para os campos novos (`.campo-formulario` padrão existente), a lista de sugestões do autocomplete (`.sugestoes-lista` — fundo `--branco`, borda `--borda`, `--raio`, sombra `--sombra-card`, item com `:focus-visible`/hover `--verde-tint`), o modal inline de fornecedor (reusar padrão de diálogo existente da página — overlay + painel com `--raio`), e `textarea` com `min-height` 72px. Respeitar escala de espaçamento 4/8/16/24/32 (UI-SPEC). Sem novas cores fora dos tokens.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - No select de ação existe ramificação por origem (grep `origem === 'manual'` próximo ao select de `acao`), e a guarda de justificativa (L400-408) roda apenas para xml.
    - `Entregas.css` contém classes `.sugestoes-lista` (ou equivalente nomeada) com `--raio` e tokens institucionais; nenhuma cor hex nova fora dos tokens (grep por `#[0-9A-Fa-f]{3,6}` no css → apenas valores já presentes em index.css).
    - `npm run build && npm run lint` verdes.
  </acceptance_criteria>
  <done>UI reflete a auditoria por origem; estilos co-localizados seguem o design system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | payload EntregaCreatePayload e POST /fornecedores (dados digitados + parse de XML local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T (tampering) | form Entregas | mitigate | Validação local antes do submit (data, fornecedor, obs manual, nota xml) espelhando as 400/422 do backend — o backend permanece autoridade (ASVS V5.1) |
| T-08-09 | T (tampering) | parse XML client-side | accept | Parse local com revisão humana (padrão F12); nenhum dado do XML é persistido sem o usuário submeter; sem execução de conteúdo |
| T-08-10 | I (info disclosure) | autocomplete de fornecedores | accept | Lista de fornecedores restrita a admin/secretaria pelo backend (GET /fornecedores já autorizado); dados exibidos são CNPJ/nome (dados públicos) |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — gates verdes.
- Backstop manual (08-VALIDATION.md): upload de XML → pré-preenchimento → cadastro inline de fornecedor → submit sem perder rascunho.
</verification>

<success_criteria>
- IMP-06/07/08 na UI: origem, data, fornecedor (autocomplete/inline), observações e nota com regras por origem.
- IMP-04/05: sugestões de fornecedor com motivo, confirmáveis.
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-05-SUMMARY.md`
</output>
