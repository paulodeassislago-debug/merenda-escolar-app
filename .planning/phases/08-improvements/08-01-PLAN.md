---
phase: 08-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models.py
  - backend/migracao.py
  - backend/main.py
  - backend/schemas.py
  - frontend/src/types.ts
  - backend/tests/test_entregas.py
autonomous: true
requirements:
  - IMP-06
  - IMP-07
  - DELIV-05
  - DELIV-06

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Todo item persistido tem limiar numérico NOT NULL com default 5.0 (D-01), inclusive itens legados após migração."
    - "Toda entrega persistida tem origem ('xml'/'manual'), data_entrega e fornecedor; entregas legadas migram com origem='manual' e data_entrega=data(data_hora) (D-10)."
    - "Fornecedores são criados e listados via API para admin e secretaria (D-06, D-08)."
    - "O contrato TypeScript (types.ts) espelha os novos schemas; a suíte backend de entregas permanece verde com os novos payloads."
  artifacts:
    - path: "backend/models.py"
      provides: "Item.limiar; Entrega.origem/data_entrega/fornecedor_id/nota_numero/observacoes; modelo Fornecedor; modelo AlunosPorPeriodo"
      contains: "limiar"
    - path: "backend/migracao.py"
      provides: "Migração idempotente via sqlalchemy.inspect + ALTER TABLE com defaults e backfill"
      exports: ["migrar"]
    - path: "backend/schemas.py"
      provides: "FornecedorCreate/Response; EntregaCreate com origem/data_entrega/fornecedor_id/nota_numero/observacoes; ItemCreate/Update/Response com limiar"
    - path: "frontend/src/types.ts"
      provides: "Tipos Fornecedor, EntregaCreatePayload e campos novos de Item/EntregaDetalhe/EntregaResumo"
  key_links:
    - from: "backend/main.py"
      to: "backend/migracao.py"
      via: "migrar(engine) chamado após models.Base.metadata.create_all"
      pattern: "migrar\\(engine\\)"
    - from: "backend/main.py"
      to: "backend/schemas.py"
      via: "EntregaCreate/FornecedorCreate nos handlers"
---

<objective>
Fundação de schema da Fase 8: colunas novas em `Item`/`Entrega`, tabelas `fornecedores` e `alunos_por_periodo`, migração SQLite idempotente sem Alembic, schemas Pydantic revisados, contratos TypeScript espelhados e payloads dos testes de entregas atualizados.

Purpose: Toda a Fase 8 (limiar, origem/fornecedor, alunos, projeção, inline) depende deste contrato de dados. Entregue sem quebrar a suíte existente e sem perda de dados legados.
Output: models + migração + schemas + endpoints mínimos de fornecedores + tipos compartilhados + testes verdes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-improvements/08-CONTEXT.md
@.planning/phases/08-improvements/08-RESEARCH.md
@.planning/phases/08-improvements/08-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@backend/models.py
@backend/schemas.py
@backend/main.py
@backend/tests/conftest.py
@backend/tests/test_entregas.py
@frontend/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Adicionar colunas e tabelas novas em models.py e criar a migração idempotente migracao.py</name>
  <read_first>
    `backend/models.py` (estado atual — 155 linhas), `backend/main.py` L1-60 (startup `create_all`), `backend/database.py`, `.planning/phases/08-improvements/08-CONTEXT.md` (D-01, D-05, D-06, D-10), `08-RESEARCH.md` (R-1).
  </read_first>
  <files>backend/models.py, backend/migracao.py, backend/main.py</files>
  <action>
    Em `backend/models.py` (per D-01, D-05, D-06, D-14):
    1. Classe `Item` (L35-43): adicionar `limiar = Column(Float, nullable=False, default=5.0)`.
    2. Classe `Entrega` (L103-110): adicionar `origem = Column(String, nullable=False, default="manual")`, `data_entrega = Column(Date, nullable=True)`, `fornecedor_id = Column(Integer, ForeignKey("fornecedores.id"), nullable=True)`, `nota_numero = Column(String, nullable=True)`, `observacoes = Column(Text, nullable=True)`. `data_entrega` nullable no banco para permitir o backfill da migração; o schema Pydantic garante obrigatoriedade no payload (D-05). Import `Date` já existe.
    3. Nova classe `Fornecedor` (`__tablename__ = "fornecedores"`): `id` (Integer PK index), `nome` (String, nullable=False, index=True), `cnpj` (String, nullable=True).
    4. Nova classe `AlunosPorPeriodo` (`__tablename__ = "alunos_por_periodo"`): `periodo` (String, primary_key), `qtd` (Integer, nullable=False), `updated_at` (DateTime, default=datetime.datetime.now), `updated_by` (Integer, ForeignKey("usuarios.id")). Sem seed de linhas nesta tarefa (admin configura — R-1).
    5. Em `main.py`, após `models.Base.metadata.create_all(bind=engine)` (L15), adicionar: `from migracao import migrar` e `migrar(engine)`.

    Criar `backend/migracao.py` (decisão R-1 — sem Alembic):
    - `def migrar(engine) -> None`: usa `from sqlalchemy import inspect, text`. Tabelas novas (`fornecedores`, `alunos_por_periodo`) já são criadas pelo `create_all` — a migração trata apenas tabelas existentes.
    - Para `itens`: se coluna `limiar` ausente → `ALTER TABLE itens ADD COLUMN limiar FLOAT NOT NULL DEFAULT 5.0`.
    - Para `entregas`: se coluna `origem` ausente → `ALTER TABLE entregas ADD COLUMN origem VARCHAR NOT NULL DEFAULT 'manual'`; se `data_entrega` ausente → `ALTER TABLE entregas ADD COLUMN data_entrega DATE`; se `fornecedor_id` ausente → `ALTER TABLE entregas ADD COLUMN fornecedor_id INTEGER`; se `nota_numero` ausente → `ALTER TABLE entregas ADD COLUMN nota_numero VARCHAR`; se `observacoes` ausente → `ALTER TABLE entregas ADD COLUMN observacoes TEXT`.
    - Backfill: `UPDATE entregas SET data_entrega = date(data_hora) WHERE data_entrega IS NULL` (D-10).
    - Nenhum seed artificial de fornecedor (D-10) e nenhum seed de alunos (R-1).
    - NUNCA use f-strings com dados de usuário em SQL — apenas literais fixos do código.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && python -c "import migracao, models, main; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/models.py` contém `limiar = Column(Float, nullable=False, default=5.0)` na classe Item.
    - `backend/models.py` contém `origem`, `data_entrega`, `fornecedor_id`, `nota_numero`, `observacoes` na classe Entrega.
    - `backend/models.py` contém `class Fornecedor` e `class AlunosPorPeriodo`.
    - `backend/migracao.py` exporta `migrar` e contém exatamente os 6 comandos `ALTER TABLE` (5 colunas entregas + 1 coluna itens) e o backfill `date(data_hora)`.
    - `backend/main.py` chama `migrar(engine)` após `create_all` (grep: `migrar(engine)`).
    - `python -c "import main"` executa sem erro em `backend/`.
  </acceptance_criteria>
  <done>Models e migração prontos: banco legado `backend/merenda.db` recebe colunas novas com defaults e backfill sem perda de dados; novos bancos criados via create_all já nascem com o schema completo.</done>
</task>

<task type="auto">
  <name>task 2: Atualizar schemas.py, criar endpoints de fornecedores e gravar campos novos de entrega</name>
  <read_first>
    `backend/schemas.py` (estado atual — 171 linhas), `backend/main.py` L161-300 (itens), L745-866 (entregas), `backend/auth.py` (require_perfil), `08-CONTEXT.md` D-03/D-05/D-06/D-07.
  </read_first>
  <files>backend/schemas.py, backend/main.py</files>
  <action>
    Em `backend/schemas.py` (per D-03, D-05, D-06):
    1. `ItemCreate`: adicionar `limiar: float = Field(5.0, gt=0)`; `ItemUpdate`: `limiar: float | None = Field(default=None, gt=0)`; `ItemResponse`: `limiar: float`. Interpretação R-4: default 5.0 satisfaz "valor padrão de 5" (IMP-01); `gt=0` rejeita zero/negativo; a checagem explícita 400 fica no handler (tarefa 08-02-01).
    2. Nova `class FornecedorCreate(BaseModel)`: `nome: str`, `cnpj: str | None = None`.
    3. Nova `class FornecedorResponse(BaseModel)`: `id: int`, `nome: str`, `cnpj: str | None`.
    4. `EntregaCreate` (L150-152): adicionar `origem: str` (valores `"xml" | "manual"` — validar no handler), `data_entrega: date` (obrigatório — D-05), `fornecedor_id: int` (obrigatório — D-05), `nota_numero: str | None = None`, `observacoes: str | None = None`. Import `date` já existe (L1).

    Em `backend/main.py`:
    5. Novo bloco de rotas fornecedores (per D-06/D-08), usando o padrão de `/conversoes` (L301-373):
       - `GET /fornecedores` → `require_perfil("admin", "secretaria")`, retorna lista `[{id, nome, cnpj}]` ordenada por `nome`.
       - `POST /fornecedores` → `require_perfil("admin", "secretaria")`, recebe `schemas.FornecedorCreate`, valida `nome` não vazio (400 se vazio/blank), cria e retorna `{id, nome, cnpj}` (pode duplicar nome — sem unique).
    6. `registrar_entrega` (L745-813): na criação do modelo (L793), persistir os campos novos: `models.Entrega(id_usuario=usuario.id, origem=dados.origem, data_entrega=dados.data_entrega, fornecedor_id=dados.fornecedor_id, nota_numero=dados.nota_numero, observacoes=dados.observacoes)`. Validação de fornecedor existente e regras por origem entram na tarefa 08-03-01 (não duplicar aqui).
    7. `listar_entregas` (L816-834) e `detalhar_entrega` (L837-866): incluir campos aditivos na serialização — `origem`, `data_entrega` (isoformat), `fornecedor_id`, `fornecedor_nome` (busca em Fornecedor, null se não existir), `nota_numero`, `observacoes`. Não remover campos existentes (não-regressão).
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest backend/tests/test_entregas.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/schemas.py` contém `limiar: float = Field(5.0, gt=0)` em ItemCreate e `limiar` em ItemResponse.
    - `backend/schemas.py` contém `class FornecedorCreate` e `class FornecedorResponse`.
    - `backend/schemas.py` `EntregaCreate` contém `origem: str`, `data_entrega: date`, `fornecedor_id: int`, `nota_numero: str | None = None`, `observacoes: str | None = None`.
    - `backend/main.py` contém `@app.get("/fornecedores")` e `@app.post("/fornecedores")` com `require_perfil("admin", "secretaria")`.
    - `backend/main.py` `registrar_entrega` grava `origem`, `data_entrega`, `fornecedor_id`, `nota_numero`, `observacoes`.
    - GET /entregas e GET /entregas/{id} retornam `origem`, `data_entrega`, `fornecedor_nome`, `nota_numero`, `observacoes` (verificável com curl/pytest).
  </acceptance_criteria>
  <done>API aceita e devolve os novos campos de entrega e fornecedores; contratos Pydantic alinhados às decisões D-03/D-05/D-06.</done>
</task>

<task type="auto">
  <name>task 3: Espelhar contratos em types.ts e atualizar payloads/asserts dos testes de entregas</name>
  <read_first>
    `frontend/src/types.ts` (estado atual — 171 linhas), `backend/tests/test_entregas.py` (payloads E1-E13), `backend/tests/conftest.py` (fixtures), `08-CONTEXT.md` D-05/D-06.
  </read_first>
  <files>frontend/src/types.ts, backend/tests/test_entregas.py</files>
  <action>
    Em `frontend/src/types.ts` (espelhar schemas — AGENTS.md: "update both frontend and backend Pydantic models when changing data shapes"; usar `import type` onde houver):
    1. `Item` (L41-48): adicionar `limiar: number;`.
    2. Nova `export interface Fornecedor { id: number; nome: string; cnpj: string | null; }`.
    3. Nova `export type OrigemEntrega = 'xml' | 'manual';`.
    4. `EntregaItemRequest` (L106-113): inalterado.
    5. Nova `export interface EntregaCreatePayload { origem: OrigemEntrega; data_entrega: string; fornecedor_id: number; nota_numero?: string | null; observacoes?: string | null; itens: EntregaItemRequest[]; }`.
    6. `EntregaResumo` (L92-97): adicionar `origem: OrigemEntrega; data_entrega: string; fornecedor_nome: string | null;`.
    7. `EntregaDetalhe` (L99-104): adicionar `origem: OrigemEntrega; data_entrega: string; fornecedor_id: number | null; fornecedor_nome: string | null; nota_numero: string | null; observacoes: string | null;`.
    8. `DashboardItemCritico` (L115-121): adicionar `limiar: number;`.

    Em `backend/tests/test_entregas.py` (payloads obrigatórios por D-05):
    9. Novo helper `_criar_fornecedor(client, token, nome="Fornecedor Teste") -> dict` usando `POST /fornecedores` com `{"nome": nome}`.
    10. Atualizar TODOS os `POST /entregas` (E1, E2, E3, E4, E5, E8, E10, E11, E12, E13) para incluir no corpo: `"origem": "xml", "data_entrega": "2026-08-05", "fornecedor_id": <id do fornecedor criado no teste>`. Usar `origem="xml"` para preservar as regras atuais de justificativa (E8 continua válido até a tarefa 08-03-01).
    11. Novos testes no mesmo arquivo:
        - `test_f1_criar_fornecedor`: POST /fornecedores → 200, `nome` e `cnpj` (opcional) persistidos; GET /fornecedores lista.
        - `test_f2_fornecedor_sem_nome`: POST /fornecedores com `nome=""` → 400.
        - `test_f3_fornecedor_perfil_errado`: cozinheira → 403.
        - `test_f4_entrega_exige_campos_novos`: POST /entregas sem `origem`/`data_entrega`/`fornecedor_id` → 422 (Pydantic).
        - `test_f5_detalhe_entrega_campos_novos`: entrega criada com origem/fornecedor → GET /entregas/{id} retorna `origem`, `data_entrega`, `fornecedor_nome`, `nota_numero`, `observacoes`.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest backend/tests/test_entregas.py -x && cd ../../frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types.ts` contém `limiar: number` em Item, `interface Fornecedor`, `type OrigemEntrega`, `interface EntregaCreatePayload` com `origem`/`data_entrega`/`fornecedor_id`, e campos novos em EntregaResumo/EntregaDetalhe/DashboardItemCritico.
    - `backend/tests/test_entregas.py` não contém nenhum `POST /entregas` sem `"origem"` no corpo (grep `json={"itens"` sem origem → 0 resultados).
    - `test_entregas.py` inclui `test_f1_criar_fornecedor` .. `test_f5_detalhe_entrega_campos_novos`.
    - `pytest backend/tests/test_entregas.py` verde (E1-E13 + F1-F5).
    - `cd frontend && npm run build` verde (typecheck com os novos tipos).
  </acceptance_criteria>
  <done>Contratos espelhados frontend/backend e suíte de entregas verde com os novos payloads — base estável para as frentes seguintes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | payloads de entrega/fornecedor cruzam a fronteira e são persistidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (tampering) | `migracao.py` | mitigate | ALTER TABLE apenas com literais fixos do código; detecção via `sqlalchemy.inspect` (idempotente); backfill apenas de colunas novas — nenhum dado existente é sobrescrito; verificação manual no `merenda.db` dev antes de executar |
| T-08-02 | T (tampering) | `POST /entregas`, `POST /fornecedores` | mitigate | Validação Pydantic (origem/data/fornecedor obrigatórios em EntregaCreate; nome não vazio em FornecedorCreate) + `require_perfil("admin","secretaria")` (ASVS V4.1 authz) |
| T-08-03 | I (info disclosure) | `GET /fornecedores`, `GET /entregas` | mitigate | Leitura restrita a admin/secretaria via `require_perfil` (ASVS V4.1.3); sem dados sensíveis além de CNPJ de fornecedor (aceito: dado público fiscal) |
</threat_model>

<verification>
- `cd backend && source venv/bin/activate && pytest backend/tests/ -x` — suíte completa verde (payloads antigos de outros arquivos continuam válidos: ItemCreate tem default 5.0).
- `cd frontend && npm run build` — typecheck verde com tipos novos.
- Execução dupla da migração no `merenda.db` dev: idempotente (2ª execução sem erro e sem ALTERs repetidos).
</verification>

<success_criteria>
- Item com limiar default 5.0; entregas legadas com origem="manual" e data_entrega preenchida (backfill).
- Fornecedores criáveis/listáveis por admin e secretaria.
- types.ts espelha schemas; `pytest backend/tests/` verde.
</success_criteria>

<output>
After completion, create `.planning/phases/08-improvements/08-01-SUMMARY.md`
</output>
