---
phase: 05-p-ginas-admin-frontend
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/admin/Itens.tsx
  - frontend/src/pages/admin/Itens.css
autonomous: true
requirements: ["F8"]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Admin gerencia itens (criar/editar/excluir) pelo padrão tabela + modal com feedback visual; 409 de nome duplicado exibido verbatim em role=\"alert\""
    - "Item com saldo_atual < 5.0 (LIMIAR_BAIXO_ESTOQUE de constants.ts) exibe o saldo em --erro negrito (700) + badge 'Baixo estoque' (--erro-fundo/--erro); demais exibem badge 'OK' (--verde-tint/--verde-escuro) — nunca fundo vermelho cheio (D-07, DESIGN.md §5.2)"
    - "Saldo sempre formatado com toFixed(2) na tabela (cobre populated do UI-SPEC)"
    - "Página segue o ternário carregando → erro → vazio → conteúdo; vazio exibe 'Nenhum item no estoque' + 'Cadastre o primeiro item para começar o controle de estoque.'"
    - "Admin abre 'Conversões' de um item e adiciona/remove conversões (medida caseira + peso em kg); vazio exibe 'Nenhuma conversão cadastrada. Sem conversão, o lançamento de refeições com medidas caseiras falha.'"
    - { statement: "Tabela de Itens (5 colunas) e modal de conversões não quebram o layout em larguras estreitas — .tabela-container com scroll horizontal e modal max-height 90vh com overflow-y auto (verificado visualmente em F8)", verification: backstop }
  artifacts:
    - frontend/src/pages/admin/Itens.tsx — página CRUD de itens + gerenciador de conversões embutido (substitui o scaffolding do plano 01)
    - frontend/src/pages/admin/Itens.css — estilos co-localizados com badges de status e saldo-baixo
  key_links:
    - "Itens.tsx → fetchJson('/itens' | POST/PUT/DELETE) — CRUD principal (D-03)"
    - "Itens.tsx → fetchJson('/conversoes?item_id={id}' | POST/DELETE /conversoes) — conversões por item"
---

<objective>
Substituir o scaffolding de Itens pelo CRUD completo de estoque — com o destaque de baixo estoque que "grita" (a cozinheira e a secretaria dependem desse alerta para compras PNAE) — e o gerenciador de conversões por item embutido na página.

Purpose: Itens é a página operacional mais crítica do admin (F8); sem conversão cadastrada, o lançamento de refeições falha com 400 — o admin precisa de onde cadastrá-las (RESEARCH 5.3).
Output: `/admin/itens` funcional com CRUD, badge de baixo estoque e conversões (F8).

Nota registrada: spec-less probe fallback pulado visivelmente — fase sem REQ-IDs; baseline = ROADMAP fase 5 + F6–F12.
</objective>

<execution_context>
@/home/paulo/.opencode/gsd-core/workflows/execute-plan.md
@/home/paulo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-p-ginas-admin-frontend/05-CONTEXT.md
@.planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md
@.planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md
@.planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md
@.planning/phases/05-p-ginas-admin-frontend/05-01-SUMMARY.md

@frontend/src/types.ts
@frontend/src/api.ts
@frontend/src/pages/admin/constants.ts
@frontend/src/pages/admin/Usuarios.tsx
@frontend/src/pages/admin/Usuarios.css
@frontend/src/pages/DashboardGestao.tsx
@frontend/src/index.css
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Página Itens — CRUD de estoque com destaque de baixo estoque</name>
  <files>frontend/src/pages/admin/Itens.tsx, frontend/src/pages/admin/Itens.css</files>
  <read_first>
    - frontend/src/pages/admin/Itens.tsx (arquivo modificado — scaffolding do plano 01, substituído integralmente)
    - frontend/src/pages/admin/Usuarios.tsx e Usuarios.css (template CRUD provado no plano 01 — copiar a estrutura: estados, tabela, modal, form, role="alert")
    - frontend/src/types.ts (interface Item criada no plano 01)
    - frontend/src/pages/admin/constants.ts (LIMIAR_BAIXO_ESTOQUE)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `Itens.tsx` — saldo/badge pattern re-tokenizado)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.3; Color — regra de baixo estoque; Typography — peso 700 restrito a saldos)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção 5.3 — contratos /itens; limiar 5.0 definido no backend)
    - frontend/src/pages/DashboardGestao.tsx (linhas 93-100: toFixed(2) + badge por limiar — re-tokenizar, NÃO copiar inline styles)
  </read_first>
  <action>
    Substituir o scaffolding pela página real (F8, D-01, D-02, D-03, D-07), copiando a estrutura de Usuarios.tsx: estados `itens`, `carregando`, `erro`, `salvando`, `modalAberto`, `editando: Item | null`, `excluindo: Item | null`, campos do form `nome`/`unidadeOficial`/`saldoAtual`. Fetch inicial `fetchJson<Item[]>('/itens')`.

    Tabela com colunas Nome, Unidade (KG/L), Saldo, Status, Ações (UI-SPEC 5.3): célula de saldo com `{item.saldo_atual.toFixed(2)}` e classe condicional `saldo-baixo` quando `item.saldo_atual < LIMIAR_BAIXO_ESTOQUE` (--erro, peso 700 — nunca fundo vermelho cheio); coluna Status com badge "Baixo estoque" (fundo --erro-fundo, texto --erro) ou "OK" (fundo --verde-tint, texto --verde-escuro) — classes `status-alerta`/`status-ok` no molde pill 4px 8px/999px/13px/600/uppercase. O destaque deve gritar visualmente (CONTEXT.md specifics). Linha de vazio: colSpan 5 com "Nenhum item no estoque" + "Cadastre o primeiro item para começar o controle de estoque.".

    Modal criar/editar (mesma estrutura overlay/content de Usuarios, overlay rgba(18, 76, 15, 0.35)): título "Novo item"/"Editar item"; campos Nome (text), Unidade oficial (select restrito às opções "KG" e "L" — espelha UNIDADES_VALIDAS do backend), Saldo atual (number, `step="0.1"`, default 0 na criação). Submit: POST /itens `{ nome, unidade_oficial, saldo_atual }` ou PUT /itens/{id}; ApiError (409 duplicado, 400 unidade inválida) exibido verbatim em role="alert"; sucesso fecha + refetch; botão "Salvando…" desabilitado.

    Modal destrutivo: "Excluir o item {nome}? Esta ação não pode ser desfeita." → DELETE /itens/{id} → refetch. Botão de ação por linha "Conversões" (secundário — abre o modal da task 2). Header: `<h1>` "Itens / Estoque" + CTA "Novo item". Erro 401: "Sua sessão expirou. Entre novamente.". `Itens.css` novo com o vocabulário compartilhado (.pagina-header, .card, .tabela-container/.tabela, .badge, .btn-*, .modal-*, .form-*, .alerta-erro) + `.saldo-baixo`, `.status-alerta`, `.status-ok` — somente tokens var(--*), .tabela-container com overflow-x auto para o backstop de overflow. D-13: import type separado; LIMIAR_BAIXO_ESTOQUE importado de './constants' (nunca literal 5.0 inline).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "LIMIAR_BAIXO_ESTOQUE" src/pages/admin/Itens.tsx && grep -c "toFixed(2)" src/pages/admin/Itens.tsx && grep -c "Baixo estoque" src/pages/admin/Itens.tsx && grep -c "Nenhum item no estoque" src/pages/admin/Itens.tsx && grep -c "status-alerta" src/pages/admin/Itens.css</automated>
  </verify>
  <done>
    - Build + lint exit 0
    - Itens.tsx: CRUD completo via fetchJson (/itens GET/POST/PUT/DELETE), select KG/L, saldo toFixed(2), badge Baixo estoque/OK por LIMIAR_BAIXO_ESTOQUE (grep ≥1 para cada)
    - Itens.css: .saldo-baixo/.status-alerta/.status-ok com tokens --erro/--erro-fundo/--verde-tint; nenhum fundo vermelho cheio
    - F8 (manual, /gsd-verify-work): item com saldo < 5.0 exibe destaque; 409 em nome duplicado visível
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "fetchJson<Item[]>('/itens')" frontend/src/pages/admin/Itens.tsx` ≥ 1
    - `grep -c "LIMIAR_BAIXO_ESTOQUE" frontend/src/pages/admin/Itens.tsx` ≥ 2 (condição do saldo + badge)
    - `grep -c "export default function Itens" frontend/src/pages/admin/Itens.tsx` ≥ 1
    - `grep -c "color: var(--erro)" frontend/src/pages/admin/Itens.css` ≥ 1
    - `grep -c "background-color: var(--erro-fundo)" frontend/src/pages/admin/Itens.css` ≥ 1
    - Comportamento: badge "Baixo estoque" só quando saldo_atual < 5.0; saldo em negrito --erro; demais linhas badge "OK"
  </acceptance_criteria>
  <reversibility rating="reversible">Substituição de scaffolding por página nova + CSS novo; sem mudanças fora de Itens.</reversibility>
</task>

<task type="auto">
  <name>Gerenciador de conversões por item — modal com listagem, adição e remoção</name>
  <files>frontend/src/pages/admin/Itens.tsx, frontend/src/pages/admin/Itens.css</files>
  <read_first>
    - frontend/src/pages/admin/Itens.tsx (arquivo modificado — página da task 1, modal de conversões adicionado)
    - frontend/src/types.ts (interface Conversao criada no plano 01)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção 5.3 — contratos /conversoes: query item_id obrigatória; 404 item inexistente; 409 duplicada)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `Itens.tsx` — "Conversões: gerenciador por item embutido na página (modal ou linha expansível)")
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.3 — título "Conversões de {item}", copy de vazio; Destructive inventory #3 — exclusão de conversão usa o modal destrutivo global)
    - frontend/src/pages/admin/Usuarios.tsx (padrão de modal aninhado/segundo modal na mesma página)
  </read_first>
  <action>
    Adicionar à página Itens o gerenciador de conversões embutido (RESEARCH 5.3 — discricionariedade resolvida: modal por item, pois PLAN.md 5.1–5.7 não cria página separada e sem conversão o POST /refeicoes falha com 400). Estado novo: `conversoesDe: Item | null`, `conversoes: Conversao[]`, `carregandoConversoes`, campos `medidaCaseira`/`pesoEmKg`, `excluindoConversao: Conversao | null`.

    Fluxo: botão "Conversões" na linha do item → abre modal "Conversões de {item.nome}" e dispara `fetchJson<Conversao[]>('/conversoes?item_id=' + item.id)`. Corpo do modal: tabela das conversões (Medida caseira, Peso em kg formatado toFixed(3) ou toFixed(2) — discricionariedade consistente, Ação remover) com linha de vazio "Nenhuma conversão cadastrada. Sem conversão, o lançamento de refeições com medidas caseiras falha."; formulário inline de adição (mesmo form pattern: .form-group/.form-input) com campos Medida caseira (text, placeholder ex.: "xícara") e Peso em kg (number, step="0.001") + botão primário "Adicionar conversão" com "Salvando…".

    Submit de adição: POST /conversoes `{ item_id: conversoesDe.id, medida_caseira, peso_em_kg }` → refetch de '/conversoes?item_id=…'; ApiError (404 item, 409 duplicada) verbatim em role="alert" dentro do modal. Remoção: modal destrutivo global aninhado "Excluir a conversão {medida_caseira}? Esta ação não pode ser desfeita." → DELETE /conversoes/{id} → refetch das conversões. Fechar o modal limpa conversoesDe/conversoes. CSS: reutilizar .modal-* e .tabela; ajustar max-width do modal de conversões se necessário (discricionariedade); modal respeita max-height 90vh com overflow-y auto (backstop de overflow). D-13: import type { Conversao }.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "conversoes?item_id=" src/pages/admin/Itens.tsx && grep -c "Conversões de " src/pages/admin/Itens.tsx && grep -c "Nenhuma conversão cadastrada" src/pages/admin/Itens.tsx && grep -c "Conversao" src/pages/admin/Itens.tsx</automated>
  </verify>
  <done>
    - Build + lint exit 0
    - Itens.tsx: modal "Conversões de {item}" com GET/POST/DELETE /conversoes via fetchJson (grep ≥1 para query item_id), copy de vazio presente, exclusão via modal destrutivo
    - F8 (manual, /gsd-verify-work): conversão cadastrada via UI aparece no GET /conversoes; erros 404/409 visíveis
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "'/conversoes?item_id='" frontend/src/pages/admin/Itens.tsx` ≥ 1
    - `grep -c "'/conversoes'" frontend/src/pages/admin/Itens.tsx` ≥ 1 (POST)
    - `grep -c "/conversoes/" frontend/src/pages/admin/Itens.tsx` ≥ 1 (DELETE por id)
    - `grep -c "Nenhuma conversão cadastrada. Sem conversão" frontend/src/pages/admin/Itens.tsx` ≥ 1
    - Comportamento: adicionar conversão refaz o GET; remover exige confirmação no modal destrutivo; erros do backend exibidos em role="alert" dentro do modal
  </acceptance_criteria>
  <reversibility rating="reversible">Adição de modal/estado na página Itens; reversível por git revert sem afetar o CRUD base.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Dados de estoque e conversões cruzam em forms; backend valida (400/404/409) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering | Form de saldo (saldo_atual direto) | medium | mitigate | Edição de saldo é capability admin por desenho (endpoint protegido por perfil); deduções reais ocorrem só via refeições/entregas no backend — frontend não expõe bypass |
| T-05-02-02 | Tampering | Inputs numéricos (saldo, peso_em_kg) | low | mitigate | Inputs type=number com step; backend é a autoridade de validação (400 exibido verbatim); React escapa strings |
| T-05-02-03 | Information Disclosure | Tabela de estoque | low | accept | Dados de estoque são operacionais internos; acesso já restrito a admin por ProtectedRoute + backend |
| T-05-02-SC | Tampering | npm installs | high | mitigate | Nenhum pacote novo neste plano; nenhum npm install permitido |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — zero erros/warnings por task
- Greps de badge/limiar/conversões (verifies das tasks)
- Manual (fase /gsd-verify-work): F8 — CRUD itens, destaque baixo estoque, conversão cadastrada via UI aparece no GET /conversoes
</verification>

<success_criteria>
- `/admin/itens` completo: CRUD + baixo estoque gritando + conversões por item
- Nenhum arquivo fora de Itens.tsx/Itens.css tocado (types/constants/App.tsx pertencem ao plano 01)
- Padrão visual consistente com a página-modelo Usuários (plano 01)
</success_criteria>

<output>
Create `.planning/phases/05-p-ginas-admin-frontend/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 02)

**Components:** `Itens` (página real — substitui scaffolding)

**CSS classes introduzidas:** `.saldo-baixo`, `.status-alerta`, `.status-ok` (+ reutilização do vocabulário compartilhado em Itens.css)

**New file paths:** `frontend/src/pages/admin/Itens.css` (Itens.tsx já existe como scaffolding do plano 01, substituído)
