---
phase: 05-p-ginas-admin-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types.ts
  - frontend/src/pages/admin/constants.ts
  - frontend/src/App.tsx
  - frontend/src/pages/admin/Usuarios.tsx
  - frontend/src/pages/admin/Usuarios.css
  - frontend/src/pages/admin/Dashboard.tsx
  - frontend/src/pages/admin/Dashboard.css
  - frontend/src/pages/admin/Itens.tsx
  - frontend/src/pages/admin/Cardapio.tsx
  - frontend/src/pages/admin/Receitas.tsx
  - frontend/src/pages/admin/Planejamento.tsx
  - frontend/src/pages/admin/Entregas.tsx
autonomous: true
requirements: ["F6", "F7"]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Admin abre /admin/usuarios e vê a tabela de usuários carregada via GET /usuarios, seguindo o ternário carregando → erro → vazio → conteúdo com role=\"alert\" nos erros (cobre estados loading/error/empty/populated do UI-SPEC)"
    - "Admin cria usuário pelo modal; nome duplicado exibe o detail 409 do backend verbatim em role=\"alert\""
    - "Admin edita usuário deixando a senha em branco e a senha atual é mantida (campo senha omitido do payload PUT)"
    - "Admin exclui usuário somente após confirmar no modal destrutivo 'Excluir usuário {nome}? Esta ação não pode ser desfeita.'"
    - "Dashboard /admin exibe as 4 seções com dados reais do GET /admin/dashboard; com banco fresco, zeros são estado populado válido e as seções sempre renderizam (F6)"
    - "Dashboard trata prato null como 'Prato não definido' e ultima_data null como 'Nenhuma entrega registrada ainda.' (cobre partial do UI-SPEC)"
    - "Listas do dashboard (itens_criticos 0..n) renderizam 0 → copy de vazio, 1..n → mesma estrutura de lista (cobre zero-one-many do UI-SPEC)"
    - "As 7 rotas admin (/admin, /admin/usuarios, /admin/itens, /admin/cardapio, /admin/receitas/:id, /admin/planejamento, /admin/entregas) resolvem dentro de ProtectedRoute+Layout com os perfis do D-05, e os links da sidebar (NAV_POR_PERFIL, já existente) deixam de levar a 404 (D-06)"
  artifacts:
    - frontend/src/types.ts — interfaces Item, Conversao, CardapioItem, ReceitaItem, PlanejamentoEntrada, EntregaResumo, EntregaDetalhe, ItemEntrega, DashboardResponse (+ sub-shapes) e union AcaoEntrega com 'excluído' acentuado (D-04)
    - frontend/src/pages/admin/constants.ts — TIPOS_REFEICAO, DIAS_SEMANA, LIMIAR_BAIXO_ESTOQUE, PERFIL_ROTULOS
    - frontend/src/App.tsx — 6 novas rotas protegidas registradas (D-05)
    - frontend/src/pages/admin/Usuarios.tsx + Usuarios.css — CRUD completo (F7)
    - frontend/src/pages/admin/Dashboard.tsx + Dashboard.css — dashboard real (F6)
    - frontend/src/pages/admin/{Itens,Cardapio,Receitas,Planejamento,Entregas}.tsx — scaffolding mínimo substituído integralmente pelos planos 02–05 nesta mesma fase
  key_links:
    - "Usuarios.tsx → fetchJson('/usuarios' | POST/PUT/DELETE) via src/api.ts (D-03) — ApiError.detail exibido em role=\"alert\""
    - "Dashboard.tsx → fetchJson('/admin/dashboard') — shape DashboardResponse de types.ts"
    - "App.tsx → ProtectedRoute perfis={...} → Layout — secretaria só em planejamento/entregas; backend 403 é a autoridade final"
---

<objective>
Provar a arquitetura da fase de ponta a ponta com UMA fatia vertical completa — tipos compartilhados, constantes, registro de rotas e a página CRUD-modelo (Usuários) funcionando sobre a API real — e entregar o Dashboard admin real. Todos os planos seguintes (02–05) copiam o padrão provado aqui sem tocar novamente em types.ts, constants.ts ou App.tsx.

Purpose: Esta é a fatia tracer da fase (TRACER_MODE). Ela toca todas as camadas que a fase modifica (types, constants, router, page, CSS, API client) uma única vez, deixando o esqueleto de navegação inteiro de pé desde a Wave 1.
Output: Fundação compartilhada (types + constants + rotas), página Usuários completa (F7), Dashboard completo (F6), scaffolding das 5 páginas restantes (substituído na Wave 2).

Nota registrada: spec-less probe fallback pulado visivelmente — a fase não tem REQ-IDs (projeto sem REQUIREMENTS.md); nenhum predicado de probe gerado nesta execução. Baseline de cobertura: goal + critérios de sucesso da ROADMAP fase 5 e IDs de teste manual F6–F12.
</objective>

<execution_context>
@/home/paulo/.opencode/gsd-core/workflows/execute-plan.md
@/home/paulo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-p-ginas-admin-frontend/05-CONTEXT.md
@.planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md
@.planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md
@.planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md

@frontend/src/api.ts
@frontend/src/types.ts
@frontend/src/App.tsx
@frontend/src/auth-context.ts
@frontend/src/components/ProtectedRoute.tsx
@frontend/src/components/Layout.tsx
@frontend/src/pages/Login.tsx
@frontend/src/pages/Login.css
@frontend/src/pages/CardapioPublico.tsx
@frontend/src/pages/CardapioPublico.css
@frontend/src/pages/PainelCozinha.css
@frontend/src/pages/DashboardGestao.tsx
@frontend/src/index.css
@.planning/PROJECT.md
</context>

<tasks>

<task type="tracer">
  <name>Fatia vertical end-to-end: tipos + constantes + 6 rotas + página Usuários CRUD completa</name>
  <files>frontend/src/types.ts, frontend/src/pages/admin/constants.ts, frontend/src/App.tsx, frontend/src/pages/admin/Usuarios.tsx, frontend/src/pages/admin/Usuarios.css, frontend/src/pages/admin/Itens.tsx, frontend/src/pages/admin/Cardapio.tsx, frontend/src/pages/admin/Receitas.tsx, frontend/src/pages/admin/Planejamento.tsx, frontend/src/pages/admin/Entregas.tsx</files>
  <read_first>
    - frontend/src/types.ts (arquivo modificado — estado atual: seção auth com Perfil/Usuario/LoginResponse + legados Ingrediente/PratoPadrao/ItemEstoque que NÃO podem quebrar)
    - frontend/src/App.tsx (arquivo modificado — padrão de rota protegida nas linhas 22-31)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seções: `src/types.ts`, `src/App.tsx`, `src/pages/admin/Usuarios.tsx`, `constants.ts` — shapes e patterns verbatim)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (seções Color, Typography, Page Contracts, Copywriting 5.2)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção "API Contracts per Page" 5.1 e 5.2 — shapes exatos do backend)
    - frontend/src/api.ts (fetchJson/ApiError — único caminho de API, D-03)
    - frontend/src/pages/Login.tsx e Login.css (form pattern: controlled inputs, role="alert", botão com texto de progresso)
    - frontend/src/pages/PainelCozinha.css (estrutura .modal-overlay/.modal-content — re-tokenizar: overlay rgba(18, 76, 15, 0.35), sem cores hex)
    - frontend/src/pages/DashboardGestao.tsx (estrutura de tabela/estados — NÃO copiar o fetch crú nem inline styles)
    - frontend/src/auth-context.ts (precedente de split .ts p/ lint react-refresh; useAuth/isAdmin)
  </read_first>
  <action>
    Implementar a fatia vertical completa em 4 movimentos (D-01, D-03, D-04, D-05, D-13):

    1) `frontend/src/types.ts` (modificar, D-04): ADICIONAR após a seção auth, com comentário de seção `// --- Admin (espelha os schemas do backend) ---`, SEM alterar Ingrediente/PratoPadrao/ItemEstoque/Perfil/Usuario/LoginResponse:
       - `export type AcaoEntrega = 'recebido' | 'alterado' | 'excluído'` — a terceira string é COM acento (backend rejeita variantes com 400, RESEARCH gotcha #2)
       - `Item { id: number; nome: string; unidade_oficial: string; saldo_atual: number }`
       - `Conversao { id: number; item_id: number; medida_caseira: string; peso_em_kg: number }`
       - `CardapioItem { id: number; nome_refeicao: string; tipo_refeicao: string }`
       - `ReceitaItem { id: number; cardapio_item_id: number; item_id: number; item_nome?: string; quantidade: number; medida_caseira: string }` (item_nome opcional: GET o inclui, POST/PUT não — RESEARCH 5.5)
       - `PlanejamentoEntrada { id: number; dia_semana: number; tipo_refeicao: string; cardapio_item_id: number; nome_refeicao: string; data_inicio_vigencia: string }`
       - `ItemEntrega { id: number; item_id: number; item_nome: string; quantidade: number; acao: string; justificativa: string | null }`
       - `EntregaResumo { id: number; data_hora: string; id_usuario: number; qtd_itens: number }`
       - `EntregaDetalhe { id: number; data_hora: string; id_usuario: number; itens: ItemEntrega[] }`
       - `EntregaItemRequest { item_id: number; quantidade: number; acao: AcaoEntrega; justificativa?: string | null }` (payload do POST /entregas)
       - `DashboardResponse { estoque: DashboardEstoque; refeicoes_hoje: DashboardRefeicaoHoje[]; entregas: DashboardEntregas; alunos_hoje: DashboardAlunosHoje }` com sub-shapes: `DashboardEstoque { total_itens: number; baixo_estoque: number; itens_criticos: DashboardItemCritico[] }`, `DashboardItemCritico { id: number; nome: string; saldo_atual: number }`, `DashboardRefeicaoHoje { tipo_refeicao: string; status: string; prato: string | null; alunos: number | null }`, `DashboardEntregas { ultimos_7_dias: number; ultimos_30_dias: number; ultima_data: string | null }`, `DashboardAlunosHoje { total: number; por_tipo: Record<string, number> }`

    2) `frontend/src/pages/admin/constants.ts` (novo, .ts puro sem JSX — regra react-refresh, precedente auth-context.ts): header comment explicando o split + `export const TIPOS_REFEICAO = ['Lanche da Manhã', 'Almoço', 'Lanche da Tarde', 'Janta'] as const`; `export const DIAS_SEMANA = ['Segunda', 'Terça', 'Quarta', 'Quinta', 'Sexta', 'Sábado', 'Domingo'] as const` com comentário "backend: 0=segunda…6=domingo; JS Date.getDay(): 0=domingo — converter com (jsDay + 6) % 7"; `export const LIMIAR_BAIXO_ESTOQUE = 5.0` (espelha backend main.py); `export const PERFIL_ROTULOS: Record<Perfil, string> = { admin: 'Admin', secretaria: 'Secretaria', cozinheira: 'Cozinheira' }` (import type { Perfil } from '../../types').

    3) `frontend/src/App.tsx` (modificar, D-05): adicionar imports default de Usuarios, Itens, Cardapio, Receitas, Planejamento, Entregas (de './pages/admin/...') e registrar 6 rotas no bloco `{/* Admin */}`, replicando o padrão existente: `/admin/usuarios`, `/admin/itens`, `/admin/cardapio`, `/admin/receitas/:id` → `perfis={['admin']}`; `/admin/planejamento`, `/admin/entregas` → `perfis={['admin', 'secretaria']}` — todas com `<ProtectedRoute perfis={…}><Layout><Pagina /></Layout></ProtectedRoute>`.

    4) Scaffolding das 5 páginas da Wave 2 (`Itens.tsx`, `Cardapio.tsx`, `Receitas.tsx`, `Planejamento.tsx`, `Entregas.tsx`): cada uma é um componente default mínimo e válido (função nomeada retornando `<div><h1>Título da página</h1></div>`), SEM css import ainda. São substituídas integralmente pelos planos 02–05 desta mesma fase — lacuna de funcionalidade, não de arquitetura (as rotas e a navegação já funcionam).

    5) `frontend/src/pages/admin/Usuarios.tsx` + `Usuarios.css` (novos — página-modelo do padrão CRUD, D-07): estados `usuarios`, `carregando`, `erro`, `salvando`, `modalAberto`, `editando: Usuario | null`, campos do form `nome`/`senha`/`perfil`, e `excluindo: Usuario | null`. Fetch inicial `fetchJson<Usuario[]>('/usuarios')` no useEffect. Tabela (Nome, Perfil com badge, Ações) com linha de vazio colSpan e copy "Nenhum usuário cadastrado" + "Crie usuários para controlar quem acessa o sistema."; badges de perfil: admin → fundo verde-escuro/texto branco, secretaria → verde-tint/verde-escuro, cozinheira → amarelo/texto (molde das pills de Layout.css, re-tokenizado). Modal criar/editar (estrutura overlay/content/header/btn-fechar re-tokenizada — overlay rgba(18, 76, 15, 0.35), raio 12px): título "Novo usuário"/"Editar usuário"; label de senha na edição "Nova senha (deixe em branco para manter a atual)" — senha vazia OMITE o campo do payload PUT; select de perfil com as 3 opções de PERFIL_ROTULOS. Submit: POST /usuarios `{ nome, senha, perfil }` ou PUT /usuarios/{id} com campos preenchidos; `try/catch` com `err instanceof ApiError` exibindo `err.message` (409 nome duplicado chega pronto) em `<p role="alert">`; sucesso fecha modal + refetch da listagem; botão desabilitado com texto "Salvando…". Modal destrutivo de exclusão: "Excluir usuário {nome}? Esta ação não pode ser desfeita." com botões "Cancelar" (secundário) + "Excluir" (perigo: fundo branco, borda+texto --erro, hover --erro-fundo) → DELETE /usuarios/{id} → refetch. Header da página: `<h1>` "Usuários" + CTA "Novo usuário" (.btn-primario). Ternário de estados carregando → erro → vazio → conteúdo (estrutura de DashboardGestao/CardapioPublico, sem inline styles). Erro 401 em sessão: mensagem "Sua sessão expirou. Entre novamente." (RESEARCH gotcha #8 — sem mecanismo global novo).

    6) `Usuarios.css` (D-01, D-02): classes por UI-SPEC Page Contracts — `.pagina-header`, `.card`, `.tabela-container`/`.tabela` (header fundo --verde-tint/texto --verde-escuro 600; linhas border-bottom --borda; hover --verde-tint 50%), `.badge` + variantes `-perfil-admin`/`-perfil-secretaria`/`-perfil-cozinheira` (padding 4px 8px, radius 999px, 13px/600/uppercase), `.btn-primario`/`.btn-secundario`/`.btn-perigo`, `.modal-overlay`/`.modal-content`/`.modal-header`/`.btn-fechar`, `.form-group`/`.form-input` (label 13px 600 --texto-suave; focus ring verde-vivo), `.alerta-erro` (--erro-fundo/--erro, 14px). SOMENTE tokens var(--*) de index.css — nenhum literal de cor, nenhum estilo inline no TSX, fonte --fonte-sans (serif proibida em tela operacional), alvos de toque ≥40px.

    TypeScript estrito (D-13) em todos os arquivos: `import type { Usuario, Perfil }` separado; sem enums/namespaces/parameter properties; constantes/tipos fora dos .tsx.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c 'admin/usuarios' src/App.tsx && grep -c 'admin/receitas/:id' src/App.tsx && grep -c "excluído" src/types.ts && grep -c 'LIMIAR_BAIXO_ESTOQUE' src/pages/admin/constants.ts && grep -c 'role="alert"' src/pages/admin/Usuarios.tsx</automated>
  </verify>
  <done>
    - `npm run build` e `npm run lint` exit 0 (zero erros/warnings) de frontend/
    - App.tsx contém as 6 rotas novas com perfis corretos (grep ≥1 para admin/usuarios e admin/receitas/:id)
    - types.ts exporta AcaoEntrega com a string acentuada (grep ≥1 para a forma com acento) e as ~13 interfaces novas; legados Ingrediente/PratoPadrao/ItemEstoque intactos
    - constants.ts exporta TIPOS_REFEICAO (4), DIAS_SEMANA (7), LIMIAR_BAIXO_ESTOQUE, PERFIL_ROTULOS
    - Usuarios.tsx: CRUD completo via fetchJson (GET/POST/PUT/DELETE /usuarios), senha vazia omitida do PUT, 409 exibido em role="alert", modal destrutivo antes do DELETE
    - As 5 páginas-scaffolding existem e as 7 rotas admin resolvem (build prova imports)
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0
    - `cd frontend && npm run lint` exit 0 (zero warnings)
    - `grep -c "path=\"/admin/usuarios\"" frontend/src/App.tsx` ≥ 1 e `grep -c "perfis={['admin', 'secretaria']}" frontend/src/App.tsx` ≥ 2
    - `grep -c "export type AcaoEntrega" frontend/src/types.ts` ≥ 1 e `grep -c "excluído" frontend/src/types.ts` ≥ 1
    - `grep -c "export const TIPOS_REFEICAO" frontend/src/pages/admin/constants.ts` ≥ 1
    - `grep -c "fetchJson" frontend/src/pages/admin/Usuarios.tsx` ≥ 4 (GET + POST + PUT + DELETE)
    - `grep -c "role=\"alert\"" frontend/src/pages/admin/Usuarios.tsx` ≥ 1
    - `grep -c "export default function Itens" frontend/src/pages/admin/Itens.tsx` ≥ 1 (idem Cardapio/Receitas/Planejamento/Entregas)
    - F7 (manual, fase de /gsd-verify-work): criar `teste/teste123` secretaria → aparece na tabela; editar perfil; excluir; nome duplicado → 409 visível; login com o usuário criado funciona
  </acceptance_criteria>
  <reversibility rating="reversible">Arquivos novos + adições aditivas em types.ts/App.tsx; scaffolding substituído na Wave 2; nenhuma decisão irreversível.</reversibility>
</task>

<task type="auto">
  <name>Dashboard admin real — 4 cards de métricas + seções Refeições de hoje, Itens críticos e Entregas</name>
  <files>frontend/src/pages/admin/Dashboard.tsx, frontend/src/pages/admin/Dashboard.css</files>
  <read_first>
    - frontend/src/pages/admin/Dashboard.tsx (arquivo modificado — placeholder de 11 linhas da Phase 4, substituído integralmente)
    - frontend/src/types.ts (DashboardResponse + sub-shapes criados na task 1)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `src/pages/admin/Dashboard.tsx` — fetch/estados/grid patterns)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.1 — títulos, cards, copies de null/vazio; Color — border-top verde-vivo nos cards)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção 5.1 — shape verificado do GET /admin/dashboard e notas: refeicoes_hoje usa os 3 tipos atuais; ultima_data pode ser null)
    - frontend/src/pages/CardapioPublico.tsx e CardapioPublico.css (grid responsivo + card branco com border-top 3px verde-vivo — renomear classes p/ dashboard-*)
    - frontend/src/api.ts (fetchJson/ApiError, D-03)
  </read_first>
  <action>
    Substituir o placeholder por implementação real (F6, D-01, D-02, D-03): estados `dados: DashboardResponse | null`, `carregando`, `erro`; useEffect com `fetchJson<DashboardResponse>('/admin/dashboard')`, catch exibindo ApiError.message (fallback "Não foi possível carregar o dashboard.") em role="alert", finally limpando carregando. Ternário carregando → erro → conteúdo (sem estado de vazio global: banco fresco com zeros é estado populado válido — seções sempre renderizam, UI-SPEC 5.1).

    Layout: `<h1>` "Dashboard" + subtítulo 14px --texto-suave "Visão geral da merenda escolar". Grid de 4 metric cards (brancos, `border-top: 3px solid var(--verde-vivo)`, classes `dashboard-grid`/`dashboard-card` no molde de CardapioPublico.css): "Itens no estoque" (estoque.total_itens), "Itens em baixo estoque" (estoque.baixo_estoque — número em --erro peso 700 quando > 0), "Entregas nos últimos 7 dias" (entregas.ultimos_7_dias), "Alunos hoje" (alunos_hoje.total). Valores das métricas com peso 700 permitido (UI-SPEC Typography).

    Seções abaixo do grid (cards `.card`): "Refeições de hoje" — 3 linhas sempre (dados.refeicoes_hoje), cada uma com tipo_refeicao, badge "Confirmado" (fundo --verde-vivo, texto branco — badge grande, exceção `.planning/PROJECT.md`) ou "Pendente" (fundo --amarelo, texto --texto), prato ou "Prato não definido" em --texto-suave quando null, alunos quando não null. "Itens críticos" — lista de estoque.itens_criticos com nome + saldo `.toFixed(2)`; lista vazia → "Nenhum item em baixo estoque.". "Entregas" — últimos 7/30 dias (ultimos_7_dias/ultimos_30_dias) e última data; ultima_data null → "Nenhuma entrega registrada ainda.". Formatação de data: string do backend exibida como vem ou formatada localmente (discricionariedade).

    `Dashboard.css` (novo, D-01): `dashboard-grid` (repeat(auto-fit, minmax(260px, 1fr)), gap 1rem), `dashboard-card` (branco, borda --borda, raio --raio, sombra-card, padding 1.25rem, border-top 3px verde-vivo), badges `-confirmado`/`-pendente` no molde UI-SPEC (4px 8px, 999px, 13px/600/uppercase), listas e copies de vazio em --texto-suave 14px. Somente tokens var(--*), --fonte-sans, sem estilos inline, sem literais de cor. D-13: `import type { DashboardResponse } from '../../types'`; ApiError importado de '../../api'.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "/admin/dashboard" src/pages/admin/Dashboard.tsx && grep -c "Prato não definido" src/pages/admin/Dashboard.tsx && grep -c "toFixed(2)" src/pages/admin/Dashboard.tsx && grep -c "verde-vivo" src/pages/admin/Dashboard.css</automated>
  </verify>
  <done>
    - `npm run build` + `npm run lint` exit 0
    - Dashboard.tsx substituído: fetch via fetchJson, 4 metric cards + 3 seções renderizando de DashboardResponse
    - Nulls tratados: "Prato não definido" e "Nenhuma entrega registrada ainda." presentes no source (grep ≥1 cada)
    - Saldos de itens críticos com toFixed(2); badges Confirmado/Pendente nos tokens corretos
    - F6 (manual, /gsd-verify-work): 4 seções exibem dados reais; com banco fresco, zeros são válidos; após criar entrega/refeição, números sobem
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "fetchJson<DashboardResponse>('/admin/dashboard')" frontend/src/pages/admin/Dashboard.tsx` ≥ 1
    - `grep -c "Prato não definido" frontend/src/pages/admin/Dashboard.tsx` ≥ 1
    - `grep -c "Nenhuma entrega registrada ainda." frontend/src/pages/admin/Dashboard.tsx` ≥ 1
    - `grep -c "Nenhum item em baixo estoque." frontend/src/pages/admin/Dashboard.tsx` ≥ 1
    - `grep -c "border-top: 3px solid var(--verde-vivo)" frontend/src/pages/admin/Dashboard.css` ≥ 1
    - `grep -c "placeholder da Fase 4" frontend/src/pages/admin/Dashboard.tsx` == 0 (placeholder removido)
  </acceptance_criteria>
  <reversibility rating="reversible">Substituição de um placeholder por página nova + CSS novo; git revert restaura o placeholder.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Credenciais e dados de usuários cruzam em forms; JWT no header Authorization via fetchWithAuth |
| localStorage → Browser | JWT persistido; qualquer XSS vira roubo de sessão |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Elevation of Privilege | Rotas /admin/* em App.tsx | high | mitigate | ProtectedRoute com perfis explícitos por rota (D-05) + backend require_perfil como autoridade (401/403); secretaria restrita a planejamento/entregas; verificado por grep das rotas e F7 manual |
| T-05-01-02 | Information Disclosure | Usuarios.tsx (senha) | medium | mitigate | Senha nunca lida de GET (backend não retorna); edição omite campo vazio do PUT; nenhum valor de senha renderizado na tabela |
| T-05-01-03 | Tampering | Forms controlados (nome/perfil) | low | mitigate | React escapa interpolações por padrão; nenhuma injeção HTML direta; validação final é do backend (400/409 exibidos verbatim) |
| T-05-01-SC | Tampering | npm installs | high | mitigate | Nenhum pacote novo nesta fase (Package Legitimacy Audit do RESEARCH: fast-xml-parser já instalado, OK); nenhum npm install permitido nas tasks |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — zero erros/warnings (D-12, gate por task)
- Grep das rotas/constantes/tipos (verifies das tasks)
- Sidebar: NAV_POR_PERFIL em Layout.tsx:18-35 já contém os links admin/secretaria (D-06 cumprido pela Phase 4) — este plano NÃO edita Layout.tsx; as rotas criadas eliminam os 404 dos links existentes
- Manual (fase /gsd-verify-work): F6 (dashboard 4 seções reais), F7 (CRUD usuários com feedback)
</verification>

<success_criteria>
- Fundação compartilhada (types, constants, rotas) criada uma única vez — planos 02–05 não tocam nesses arquivos
- Página Usuários funciona de ponta a ponta sobre a API real (template do padrão CRUD)
- Dashboard exibe dados reais do GET /admin/dashboard com nulls tratados
- Build + lint limpos; nenhuma regressão nas rotas Phase 4 (/, /cardapio, /cozinha, /gestao)
</success_criteria>

<output>
Create `.planning/phases/05-p-ginas-admin-frontend/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 01)

**Types (frontend/src/types.ts):** `AcaoEntrega`, `Item`, `Conversao`, `CardapioItem`, `ReceitaItem`, `PlanejamentoEntrada`, `ItemEntrega`, `EntregaResumo`, `EntregaDetalhe`, `EntregaItemRequest`, `DashboardResponse`, `DashboardEstoque`, `DashboardItemCritico`, `DashboardRefeicaoHoje`, `DashboardEntregas`, `DashboardAlunosHoje`

**Constants (frontend/src/pages/admin/constants.ts):** `TIPOS_REFEICAO`, `DIAS_SEMANA`, `LIMIAR_BAIXO_ESTOQUE`, `PERFIL_ROTULOS`

**Components:** `Usuarios` (novo), `Dashboard` (reescrito), scaffolding temporário `Itens`/`Cardapio`/`Receitas`/`Planejamento`/`Entregas` (substituído nos planos 02–05)

**CSS classes introduzidas:** `.pagina-header`, `.card`, `.tabela-container`, `.tabela`, `.badge` (+ `-perfil-admin`/`-perfil-secretaria`/`-perfil-cozinheira`/`-confirmado`/`-pendente`), `.btn-primario`, `.btn-secundario`, `.btn-perigo`, `.modal-overlay`, `.modal-content`, `.modal-header`, `.btn-fechar`, `.form-group`, `.form-input`, `.alerta-erro`, `.dashboard-grid`, `.dashboard-card` (por página, co-localizadas — nenhum stylesheet global novo)

**New file paths:** `frontend/src/pages/admin/constants.ts`, `frontend/src/pages/admin/Usuarios.tsx`, `frontend/src/pages/admin/Usuarios.css`, `frontend/src/pages/admin/Dashboard.css`, `frontend/src/pages/admin/Itens.tsx`, `frontend/src/pages/admin/Cardapio.tsx`, `frontend/src/pages/admin/Receitas.tsx`, `frontend/src/pages/admin/Planejamento.tsx`, `frontend/src/pages/admin/Entregas.tsx`
