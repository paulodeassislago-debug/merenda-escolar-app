---
phase: 05-p-ginas-admin-frontend
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/admin/Planejamento.tsx
  - frontend/src/pages/admin/Planejamento.css
autonomous: true
requirements: ["F10"]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Grade semanal 7×4 renderiza: linhas = 4 tipos de refeição (TIPOS_REFEICAO), colunas = 7 dias (DIAS_SEMANA indexados pelo valor do backend 0=segunda…6=domingo); células vigentes preenchidas com o prato atual e ausentes exibem 'A definir' em --texto-suave itálico (cobre empty/zero-one-many do UI-SPEC)"
    - "O dropdown de cada célula lista APENAS pratos cujo tipo_refeicao casa com o tipo da linha — backend não valida coerência prato×tipo (RESEARCH gotcha #6), o filtro é obrigatório"
    - "Salvar persiste: POST /planejamento por slot alterado com data_inicio_vigencia = segunda-feira da semana em edição; após F5 a grade reidrata via GET (F10, D-09)"
    - "Escolher '— A definir —' em um slot preenchido remove a entrada vigente no salvar (DELETE /planejamento/{id} vigente)"
    - "dia_semana enviado ao backend segue 0=segunda…6=domingo; qualquer deriva de Date.getDay() do JS passa pela conversão (jsDay + 6) % 7 (RESEARCH gotcha #1)"
    - "Navegação de semana: '‹ Semana anterior' / input de data (label 'Semana de referência') / 'Próxima semana ›' e caption 'Semana de {segunda} a {domingo}' (14px, --texto-suave)"
  artifacts:
    - frontend/src/pages/admin/Planejamento.tsx — grade semanal com upsert (substitui scaffolding)
    - frontend/src/pages/admin/Planejamento.css — estilos da grade co-localizados
  key_links:
    - "Planejamento.tsx → fetchJson('/planejamento?data=YYYY-MM-DD') — só slots vigentes; grade construída por junção local 7×4 com mapa `${dia_semana}|${tipo_refeicao}`"
    - "Planejamento.tsx → fetchJson('/cardapio') — opções do dropdown filtradas por tipo_refeicao da linha"
    - "Salvar → POST /planejamento (upsert por slot+vigência) e DELETE /planejamento/{id} (limpar slot)"
---

<objective>
Entregar a grade semanal de planejamento (dias × tipos de refeição) com dropdowns filtrados por tipo, navegação de semana e salvamento idempotente por vigência — o teste F10 exige persistência após reload.

Purpose: O planejamento alimenta o cardápio público e o painel da cozinheira; as semânticas de vigência/upsert do backend (RESEARCH §"Planejamento Semantics") determinam o desenho da página.
Output: `/admin/planejamento` funcional para admin + secretaria (F10, D-09).

Nota registrada: spec-less probe fallback pulado visivelmente — fase sem REQ-IDs; baseline = ROADMAP fase 5 + F6–F12.
</objective>

<execution_context>
@/home/paulo/.opencode/gsd-core/workflows/execute-plan.md
@/home/paulo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-p-ginas-admin-frontend/05-CONTEXT.md
@.planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md
@.planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md
@.planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md
@.planning/phases/05-p-ginas-admin-frontend/05-01-SUMMARY.md

@frontend/src/types.ts
@frontend/src/api.ts
@frontend/src/pages/admin/constants.ts
@frontend/src/pages/admin/Usuarios.tsx
@frontend/src/pages/PainelCozinha.tsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Grade semanal — leitura vigente, junção local 7×4, dropdowns filtrados e navegação de semana</name>
  <files>frontend/src/pages/admin/Planejamento.tsx, frontend/src/pages/admin/Planejamento.css</files>
  <read_first>
    - frontend/src/pages/admin/Planejamento.tsx (arquivo modificado — scaffolding do plano 01, substituído integralmente)
    - frontend/src/pages/admin/constants.ts (TIPOS_REFEICAO, DIAS_SEMANA + comentário da conversão (jsDay + 6) % 7)
    - frontend/src/types.ts (PlanejamentoEntrada, CardapioItem do plano 01)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção "Planejamento Semantics" INTEGRA — vigência, upsert, implicações 1-5; gotchas #1, #6, #7)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `Planejamento.tsx` — estrutura derivada do contrato: mapa slot, grade tabela, célula A definir)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.6 — título, nav de semana, caption, "— A definir —", CTA "Salvar planejamento")
    - frontend/src/pages/PainelCozinha.tsx (linhas 137-144: select pattern — NÃO copiar dados hardcoded)
  </read_first>
  <action>
    Substituir o scaffolding pela grade real (F10, D-01, D-03, D-09). Sem análogo de grade no projeto — construção nova sobre os padrões compartilhados (fetch/estados/select).

    Estado: `semanaRef: Date` (qualquer dia dentro da semana exibida; default hoje), `entradas: PlanejamentoEntrada[]`, `pratos: CardapioItem[]`, `carregando`, `erro`, e `selecoes: Record<string, number | null>` (rascunho editável: chave `${dia_semana}|${tipo_refeicao}` → cardapio_item_id ou null).

    Derivação de datas (cuidado gotcha #1): função local `segundaDaSemana(d: Date): Date` — `const diff = (d.getDay() + 6) % 7; const seg = new Date(d); seg.setDate(d.getDate() - diff);` (converte o getDay JS 0=domingo para o índice backend 0=segunda). `formatISO(d: Date)` → 'YYYY-MM-DD' (componentes locais, sem toISOString/UTC — backend trabalha com datas locais, STATE.md decisão Phase 3). Domingo da caption = segunda + 6 dias.

    Fetch: ao montar e quando semanaRef muda — Promise.all de `fetchJson<PlanejamentoEntrada[]>('/planejamento?data=' + formatISO(segundaDaSemana(semanaRef)))` e `fetchJson<CardapioItem[]>('/cardapio')`. Construir `mapaVigente: Map<string, PlanejamentoEntrada>` com chave `${e.dia_semana}|${e.tipo_refeicao}` (o GET já retorna só vigentes — gotcha #7; guardar o `id` para o DELETE da task 2). Inicializar `selecoes` a partir do mapa (slot sem entrada → null).

    Render: título "Planejamento semanal"; nav "‹ Semana anterior" (semanaRef − 7 dias) / input type="date" com label "Semana de referência" (value = formatISO(segunda), onChange atualiza semanaRef) / "Próxima semana ›" (+ 7 dias); caption "Semana de {segunda} a {domingo}" (14px, --texto-suave, datas formatadas dd/mm — discricionariedade). Grade como `<table>`: thead com os 7 DIAS_SEMANA; tbody com 4 linhas (TIPOS_REFEICAO). Cada célula: `<select>` cuja lista de opções é `pratos.filter(p => p.tipo_refeicao === tipoDaLinha)` (filtro OBRIGATÓRIO — gotcha #6) precedida da opção vazia "— A definir —" (value ""); value = selecoes[chave] ?? ""; onChange atualiza selecoes ("" → null). Célula sem seleção renderiza o placeholder "A definir" em --texto-suave itálico (a opção do select cobre; o estilo itálico aplica-se ao select sem valor ou a um span — discricionariedade, manter a copy exata). Ternário carregando → erro → grade; role="alert" no erro.

    `Planejamento.css` novo: `.planejamento-grade` (tabela, header --verde-tint/--verde-escuro 600, células com --borda), `.planejamento-nav` (flex, gap sm, botões secundários), `.planejamento-caption`, `.celula-vazia` (--texto-suave, italic), selects com .form-input compacto — somente tokens var(--*). D-13: import type; DIAS_SEMANA/TIPOS_REFEICAO importados de './constants'.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "planejamento?data=" src/pages/admin/Planejamento.tsx && grep -c "tipo_refeicao === " src/pages/admin/Planejamento.tsx && grep -c "A definir" src/pages/admin/Planejamento.tsx && grep -c "DIAS_SEMANA" src/pages/admin/Planejamento.tsx && grep -c "getDay" src/pages/admin/Planejamento.tsx</automated>
  </verify>
  <done>
    - Build + lint exit 0
    - Grade 7×4 renderiza com junção local; dropdowns filtrados por tipo da linha (grep ≥1 para a comparação de tipo_refeicao)
    - Navegação de semana (±7 dias + input date) e caption presentes
    - Após esta task o usuário VÊ a grade vigente da semana e navega entre semanas (leitura real)
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "'/planejamento?data='" frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - `grep -c "pratos.filter" frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - `grep -c "— A definir —" frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - `grep -c "Semana de referência" frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - `grep -c "export default function Planejamento" frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - Comportamento: célula com entrada vigente mostra o prato; célula sem entrada mostra a opção "— A definir —"; trocar de semana refaz o GET
  </acceptance_criteria>
  <reversibility rating="reversible">Substituição de scaffolding por página nova; isolada em Planejamento.</reversibility>
</task>

<task type="auto">
  <name>Salvar planejamento — upsert por slot alterado, limpar slot via DELETE e feedback de sucesso</name>
  <files>frontend/src/pages/admin/Planejamento.tsx</files>
  <read_first>
    - frontend/src/pages/admin/Planejamento.tsx (arquivo modificado — grade da task 1, fluxo de salvar adicionado)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção "Planejamento Semantics" — regra de upsert: mesmo (dia_semana, tipo_refeicao, data_inicio_vigencia) substitui; payload de exemplo; implicação 2: vigência = segunda da semana em edição)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.6 — CTA "Salvar planejamento", progresso "Salvando…", sucesso "Planejamento salvo.", "— A definir —" em slot preenchido deleta a entrada vigente no salvar)
    - frontend/src/types.ts (PlanejamentoEntrada — campo id necessário para DELETE)
  </read_first>
  <action>
    Adicionar o fluxo de salvamento (F10, D-09). Estado novo: `salvando`, `sucesso: boolean` (feedback transitório). CTA primário "Salvar planejamento" abaixo da grade; desabilitado enquanto `salvando` ou sem alterações pendentes (diff entre selecoes e mapaVigente — discricionariedade: pode habilitar sempre e salvar idempotente).

    Handler `salvarPlanejamento`: para cada chave `${dia_semana}|${tipo_refeicao}` de `selecoes` cujo valor difere do mapaVigente:
    - valor não-null (prato escolhido/alterado) → `fetchJson('/planejamento', { method: 'POST', body: JSON.stringify({ cardapio_item_id: valor, tipo_refeicao, dia_semana: Number(dia_semana), data_inicio_vigencia: formatISO(segundaDaSemana(semanaRef)) }) })` — upsert idempotente por slot+vigência (RESEARCH implicação 2: salvar duas vezes a mesma semana = upsert limpo).
    - valor null e existia entrada vigente → `fetchJson('/planejamento/' + entradaVigente.id, { method: 'DELETE' })` (limpar slot = DELETE do id vigente, RESEARCH implicação 5).
    Executar as mutações sequencialmente ou com Promise.all (discricionariedade; backend é transacional por request). Em erro (400 dia/tipo inválido, 404 prato): exibir ApiError.message verbatim em role="alert" e abortar o restante, mantendo a grade no estado atual. Sucesso total: refetch do GET da semana (reidrata mapaVigente e selecoes — prova da persistência), exibir texto transitório "Planejamento salvo." em --verde-escuro (some após ~3s ou na próxima edição — discricionariedade).

    Botão desabilitado com texto "Salvando…" durante o fluxo. Manter D-13 (sem enums; tipos importados) e a proibição de strings inline de tipo/dia — sempre de TIPOS_REFEICAO/DIAS_SEMANA e das chaves do mapa.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "method: 'POST'" src/pages/admin/Planejamento.tsx && grep -c "method: 'DELETE'" src/pages/admin/Planejamento.tsx && grep -c "data_inicio_vigencia" src/pages/admin/Planejamento.tsx && grep -c "Planejamento salvo." src/pages/admin/Planejamento.tsx && grep -c "Salvar planejamento" src/pages/admin/Planejamento.tsx</automated>
  </verify>
  <done>
    - Build + lint exit 0
    - Salvar faz POST por slot alterado com data_inicio_vigencia = segunda da semana (grep ≥1 para data_inicio_vigencia) e DELETE ao limpar slot vigente (grep ≥1 para method DELETE)
    - Sucesso refaz o GET e exibe "Planejamento salvo."
    - F10 (manual, /gsd-verify-work): selecionar pratos → salvar → F5 → seleções mantidas; mudar prato de um slot → reflete; prato do dia aparece no cardápio público
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "data_inicio_vigencia" frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - `grep -c "method: 'POST'" frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - `grep -c "method: 'DELETE'" frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - `grep -c "Planejamento salvo." frontend/src/pages/admin/Planejamento.tsx` ≥ 1
    - Comportamento: salvar duas vezes a mesma semana não duplica entradas (upsert); "— A definir —" em slot preenchido + salvar remove a entrada vigente; reload reidrata a grade
  </acceptance_criteria>
  <reversibility rating="reversible">Adição de handler de salvar na página; reversível por git revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Slots de planejamento cruzam via POST/DELETE; datas derivadas de JS entram no payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Tampering | Coerência prato×tipo (backend NÃO valida) | high | mitigate | Dropdown filtrado por tipo_refeicao da linha (gotcha #6) — impossível gravar prato de Janta em slot de Almoço pela UI; grep do filter no verify |
| T-05-04-02 | Tampering | Deriva errada de dia_semana (JS 0=domingo vs backend 0=segunda) | medium | mitigate | Conversão centralizada (jsDay + 6) % 7 documentada em constants.ts; data_inicio_vigencia sempre da segunda derivada pela mesma função |
| T-05-04-03 | Elevation of Privilege | Rota acessível a secretaria | low | accept | Por desenho (D-05): secretaria TEM permissão total em planejamento; backend require_perfil é a autoridade |
| T-05-04-SC | Tampering | npm installs | high | mitigate | Nenhum pacote novo neste plano; nenhum npm install permitido |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — zero erros/warnings por task
- Greps de filtro por tipo/data_inicio_vigencia/DELETE (verifies das tasks)
- Manual (fase /gsd-verify-work): F10 — salvar → F5 → persiste; reflete no cardápio público
</verification>

<success_criteria>
- `/admin/planejamento` funcional para admin + secretaria: grade vigente, edição, upsert, limpar slot
- Nenhuma incoerência prato×tipo possível pela UI
- Nenhum arquivo fora de Planejamento.tsx/Planejamento.css tocado
</success_criteria>

<output>
Create `.planning/phases/05-p-ginas-admin-frontend/05-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 04)

**Components:** `Planejamento` (página real — substitui scaffolding)

**Funções locais (não exportadas):** `segundaDaSemana`, `formatISO`, `salvarPlanejamento` (helpers internos do componente — permanecem no .tsx por não serem exportadas, respeitando react-refresh)

**CSS classes introduzidas:** `.planejamento-grade`, `.planejamento-nav`, `.planejamento-caption`, `.celula-vazia` (+ reutilização do vocabulário compartilhado)

**New file paths:** `frontend/src/pages/admin/Planejamento.css` (Planejamento.tsx já existe como scaffolding do plano 01, substituído)
