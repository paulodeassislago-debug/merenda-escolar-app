---
phase: 05-p-ginas-admin-frontend
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/admin/Cardapio.tsx
  - frontend/src/pages/admin/Cardapio.css
  - frontend/src/pages/admin/Receitas.tsx
  - frontend/src/pages/admin/Receitas.css
autonomous: true
requirements: ["F9"]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Admin gerencia pratos (criar/editar/excluir) com o select de tipo restrito exatamente às 4 strings de TIPOS_REFEICAO ('Lanche da Manhã', 'Almoço', 'Lanche da Tarde', 'Janta')"
    - "Admin navega pelo botão 'Editar receita' da linha do prato para /admin/receitas/:id"
    - "Editor de receitas adiciona/edita/remove ingredientes (item + quantidade + medida caseira) consumindo GET/POST/PUT/DELETE /cardapio/{id}/receita (D-08)"
    - "Após POST/PUT de ingrediente (resposta sem item_nome), a tabela exibe o nome correto via refetch do GET da receita ou merge local a partir da lista de itens já carregada (cobre partial do UI-SPEC)"
    - "Ambas as páginas seguem o ternário carregando → erro → vazio → conteúdo; vazios: 'Nenhum prato cadastrado' e 'Este prato ainda não tem ingredientes' com as bodies do UI-SPEC"
    - "Prato criado no Cardápio aparece como opção no dropdown do Planejamento (ponta a ponta F9, verificado manualmente)"
    - { statement: "Nomes longos de pratos e itens quebram linha nas células da tabela sem truncar nem esconder dados (verificado visualmente em F9)", verification: backstop }
  artifacts:
    - frontend/src/pages/admin/Cardapio.tsx + Cardapio.css — CRUD de pratos (substitui scaffolding)
    - frontend/src/pages/admin/Receitas.tsx + Receitas.css — editor de ingredientes por prato (substitui scaffolding)
  key_links:
    - "Cardapio.tsx → fetchJson('/cardapio' | POST/PUT/DELETE) + navigate('/admin/receitas/' + id)"
    - "Receitas.tsx → useParams id → fetchJson('/cardapio/{id}/receita') + fetchJson('/itens') (dropdown de ingredientes)"
---

<objective>
Entregar o par Cardápio + Receitas: CRUD de pratos com tipo de refeição restrito e o editor de ingredientes por prato — o fluxo completo do teste F9 (criar prato → abrir receita → adicionar/editar/remover ingredientes → prato disponível no Planejamento).

Purpose: Receitas é o coração do motor de conversão (medidas caseiras → kg/L); sem ingredientes cadastrados por prato, o lançamento de refeições não deduz estoque.
Output: `/admin/cardapio` e `/admin/receitas/:id` funcionais (F9, D-08).

Nota registrada: spec-less probe fallback pulado visivelmente — fase sem REQ-IDs; baseline = ROADMAP fase 5 + F6–F12.
</objective>

<execution_context>
@/home/paulo/.opencode/gsd-core/workflows/execute-plan.md
@/home/paulo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-p-ginas-admin-frontend/05-CONTEXT.md
@.planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md
@.planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md
@.planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md
@.planning/phases/05-p-ginas-admin-frontend/05-01-SUMMARY.md

@frontend/src/types.ts
@frontend/src/api.ts
@frontend/src/pages/admin/constants.ts
@frontend/src/pages/admin/Usuarios.tsx
@frontend/src/pages/admin/Usuarios.css
@frontend/src/pages/PainelCozinha.tsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Página Cardápio — CRUD de pratos com tipo restrito e navegação para receita</name>
  <files>frontend/src/pages/admin/Cardapio.tsx, frontend/src/pages/admin/Cardapio.css</files>
  <read_first>
    - frontend/src/pages/admin/Cardapio.tsx (arquivo modificado — scaffolding do plano 01, substituído integralmente)
    - frontend/src/pages/admin/Usuarios.tsx e Usuarios.css (template CRUD do plano 01)
    - frontend/src/types.ts (interface CardapioItem do plano 01)
    - frontend/src/pages/admin/constants.ts (TIPOS_REFEICAO)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `Cardapio.tsx` — select restrito pattern + navegação por linha)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.4 — "Novo prato", vazio, destrutivo)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção 5.4 — contratos /cardapio; 400 tipo inválido)
    - frontend/src/pages/PainelCozinha.tsx (linhas 137-144: select com opção placeholder disabled — NÃO copiar o cardapiosPadrao hardcoded)
  </read_first>
  <action>
    Substituir o scaffolding pela página real (F9, D-01, D-03, D-07), copiando a estrutura de Usuarios.tsx: estados `pratos`, `carregando`, `erro`, `salvando`, `modalAberto`, `editando: CardapioItem | null`, `excluindo: CardapioItem | null`, campos `nomeRefeicao`/`tipoRefeicao`. Fetch inicial `fetchJson<CardapioItem[]>('/cardapio')`.

    Tabela (Prato, Tipo, Ações) com vazio colSpan 3: "Nenhum prato cadastrado" + "Cadastre o primeiro prato para montar o planejamento semanal.". Ações por linha: "Editar receita" (botão secundário → `navigate('/admin/receitas/' + prato.id)` via useNavigate de react-router-dom), "Editar", "Excluir".

    Modal criar/editar: título "Novo prato"/"Editar prato"; campo Nome do prato (text); select Tipo de refeição com opção placeholder disabled "-- Selecione o tipo de refeição --" e opções mapeadas de TIPOS_REFEICAO (importado de './constants' — NUNCA strings inline; são as strings exatas do backend, 400 se inválidas). Submit: POST /cardapio `{ nome_refeicao, tipo_refeicao }` ou PUT /cardapio/{id}; ApiError verbatim em role="alert"; sucesso fecha + refetch; "Salvando…".

    Modal destrutivo: "Excluir o prato {nome}? Esta ação não pode ser desfeita." → DELETE /cardapio/{id} → refetch. Header: `<h1>` "Cardápio" + CTA "Novo prato". Células da tabela com quebra de linha natural (backstop long-text: sem ellipsis que esconda nomes). `Cardapio.css` novo com o vocabulário compartilhado (.pagina-header, .card, .tabela-*, .badge, .btn-*, .modal-*, .form-*, .alerta-erro) — somente tokens var(--*). D-13: import type; useNavigate importado de 'react-router-dom'.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "TIPOS_REFEICAO" src/pages/admin/Cardapio.tsx && grep -c "/admin/receitas/" src/pages/admin/Cardapio.tsx && grep -c "Nenhum prato cadastrado" src/pages/admin/Cardapio.tsx && grep -c "fetchJson" src/pages/admin/Cardapio.tsx</automated>
  </verify>
  <done>
    - Build + lint exit 0
    - Cardapio.tsx: CRUD /cardapio completo, select de tipo populado de TIPOS_REFEICAO (grep ≥1), navegação "Editar receita" por linha (grep ≥1 para /admin/receitas/)
    - F9 parcial (manual, /gsd-verify-work): criar prato → aparece na tabela; editar; excluir; tipo inválido impossível pela UI
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "fetchJson<CardapioItem[]>('/cardapio')" frontend/src/pages/admin/Cardapio.tsx` ≥ 1
    - `grep -c "TIPOS_REFEICAO.map" frontend/src/pages/admin/Cardapio.tsx` ≥ 1
    - `grep -c "navigate(\`/admin/receitas/" frontend/src/pages/admin/Cardapio.tsx` ≥ 1
    - `grep -c "export default function Cardapio" frontend/src/pages/admin/Cardapio.tsx` ≥ 1
    - Comportamento: as 4 opções do select são exatamente 'Lanche da Manhã', 'Almoço', 'Lanche da Tarde', 'Janta'
  </acceptance_criteria>
  <reversibility rating="reversible">Substituição de scaffolding por página nova + CSS novo; isolada em Cardapio.</reversibility>
</task>

<task type="auto">
  <name>Editor de receitas (/admin/receitas/:id) — ingredientes do prato com quantidade e medida caseira</name>
  <files>frontend/src/pages/admin/Receitas.tsx, frontend/src/pages/admin/Receitas.css</files>
  <read_first>
    - frontend/src/pages/admin/Receitas.tsx (arquivo modificado — scaffolding do plano 01, substituído integralmente)
    - frontend/src/pages/admin/Usuarios.tsx (template CRUD — form pattern, role="alert", modal destrutivo)
    - frontend/src/types.ts (interfaces ReceitaItem e Item do plano 01)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `Receitas.tsx` — fluxo de dados: GET receita + GET itens em paralelo; refetch ou merge de item_nome após POST/PUT)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção 5.5 — contratos: GET inclui item_nome, POST/PUT não; useParams → Number(id))
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.5 — header "Receita: {nome do prato}", form inline, vazio, destrutivo "Remover {item} da receita?")
  </read_first>
  <action>
    Substituir o scaffolding pelo editor real (F9, D-01, D-03, D-08). Rota com parâmetro: `const { id } = useParams<{ id: string }>()`; `const cardapioId = Number(id)` (primeira página do projeto com useParams — padrão react-router, sem análogo interno). Estados: `prato: CardapioItem | null` (header), `ingredientes: ReceitaItem[]`, `itens: Item[]` (catálogo do dropdown), `carregando`, `erro`, `salvando`, campos do form de adição `itemId`/`quantidade`/`medidaCaseira`, `editandoId: number | null` (edição inline da linha) com campos `editQuantidade`/`editMedidaCaseira`, `removendo: ReceitaItem | null`.

    Fetch inicial em paralelo (Promise.all): `fetchJson<ReceitaItem[]>('/cardapio/' + cardapioId + '/receita')`, `fetchJson<Item[]>('/itens')`, e `fetchJson<CardapioItem[]>('/cardapio')` para achar o prato atual (ou endpoint equivalente disponível — usar a listagem e filtrar por id, pois não há GET /cardapio/{id} isolado no contrato; o nome do prato vem da listagem).

    Header: "Receita: {prato.nome_refeicao}" + badge de tipo + botão secundário "Voltar ao cardápio" (`navigate('/admin/cardapio')`). Formulário inline acima da tabela (UI-SPEC 5.5): select de item (opções de `itens`: `{item.nome} ({item.unidade_oficial})`), Quantidade por aluno (number, `step="0.1"`), Medida caseira (text, placeholder ex.: "1 xícara"), botão primário "Adicionar ingrediente". Submit: POST /cardapio/{id}/receita `{ item_id: Number(itemId), quantidade, medida_caseira }` → resposta NÃO traz item_nome → refazer o GET da receita (ou mergear item_nome localmente a partir de `itens`); ApiError (404 prato/item) verbatim em role="alert".

    Tabela de ingredientes (Ingrediente, Quantidade, Medida caseira, Ações): vazio "Este prato ainda não tem ingredientes" + "Adicione ingredientes com a quantidade por aluno em medida caseira (ex.: 1 xícara, 2 colheres).". Edição inline: botão "Editar" troca a linha para inputs (quantidade step="0.1", medida caseira) + "Salvar"/"Cancelar"; salvar → PUT /cardapio/{id}/receita/{receita_id} `{ quantidade, medida_caseira }` → refetch do GET da receita. Remover: modal destrutivo "Remover {item_nome} da receita? Esta ação não pode ser desfeita." → DELETE /cardapio/{id}/receita/{receita_id} → refetch.

    `Receitas.css` novo com o vocabulário compartilhado + classes do form inline (discricionariedade) — somente tokens var(--*); células com quebra de linha (backstop long-text). D-13: import type; sem strings de endpoint montadas com concatenação insegura — template literals com cardapioId numérico.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "useParams" src/pages/admin/Receitas.tsx && grep -c "/receita" src/pages/admin/Receitas.tsx && grep -c "Este prato ainda não tem ingredientes" src/pages/admin/Receitas.tsx && grep -c "step=\"0.1\"" src/pages/admin/Receitas.tsx</automated>
  </verify>
  <done>
    - Build + lint exit 0
    - Receitas.tsx: useParams + Number(id); GET receita + GET itens; adicionar/editar/remover ingredientes via fetchJson; refetch (ou merge) de item_nome após POST/PUT
    - Header com nome do prato + tipo + "Voltar ao cardápio"
    - F9 (manual, /gsd-verify-work): adicionar 2 ingredientes → editar qtd → remover 1 → prato aparece no dropdown do Planejamento
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "useParams<{ id: string }>" frontend/src/pages/admin/Receitas.tsx` ≥ 1
    - `grep -c "/cardapio/" frontend/src/pages/admin/Receitas.tsx` ≥ 3 (GET + POST/PUT/DELETE com ids)
    - `grep -c "fetchJson<Item[]>('/itens')" frontend/src/pages/admin/Receitas.tsx` ≥ 1
    - `grep -c "Voltar ao cardápio" frontend/src/pages/admin/Receitas.tsx` ≥ 1
    - `grep -c "export default function Receitas" frontend/src/pages/admin/Receitas.tsx` ≥ 1
    - Comportamento: após adicionar/editar ingrediente, a coluna Ingrediente exibe o nome do item (não o id)
  </acceptance_criteria>
  <reversibility rating="reversible">Substituição de scaffolding por página nova + CSS novo; isolada em Receitas.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Pratos e ingredientes cruzam em forms; parâmetro :id da URL chega ao path do endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Tampering | Parâmetro :id da rota usado no path do fetch | medium | mitigate | `Number(id)` coage para número antes de montar o path; backend 404 para ids inexistentes exibido verbatim; nenhum dado de URL interpolado em HTML |
| T-05-03-02 | Tampering | Strings de tipo_refeicao | low | mitigate | Select restrito a TIPOS_REFEICAO (fonte única em constants.ts); backend rejeita variantes com 400 |
| T-05-03-03 | Elevation of Privilege | Rotas /admin/cardapio e /admin/receitas/:id | high | mitigate | `perfis={['admin']}` registrado no plano 01 (D-05) + require_perfil no backend; secretaria redirecionada por ProtectedRoute |
| T-05-03-SC | Tampering | npm installs | high | mitigate | Nenhum pacote novo neste plano; nenhum npm install permitido |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — zero erros/warnings por task
- Greps de TIPOS_REFEICAO/useParams/endpoints (verifies das tasks)
- Manual (fase /gsd-verify-work): F9 — criar prato → receita → 2 ingredientes → editar qtd → remover 1 → prato no dropdown do Planejamento
</verification>

<success_criteria>
- `/admin/cardapio` e `/admin/receitas/:id` completos e encadeados (botão Editar receita)
- item_nome sempre visível após mutações (refetch/merge — gotcha #11 do RESEARCH coberto)
- Nenhum arquivo fora de Cardapio/Receitas (.tsx/.css) tocado
</success_criteria>

<output>
Create `.planning/phases/05-p-ginas-admin-frontend/05-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 03)

**Components:** `Cardapio`, `Receitas` (páginas reais — substituem scaffolding)

**CSS classes introduzidas:** classes do form inline de ingredientes em Receitas.css (nomes à discricionariedade do executor, prefixo `receitas-*`) (+ reutilização do vocabulário compartilhado)

**New file paths:** `frontend/src/pages/admin/Cardapio.css`, `frontend/src/pages/admin/Receitas.css` (os .tsx já existem como scaffolding do plano 01, substituídos)
