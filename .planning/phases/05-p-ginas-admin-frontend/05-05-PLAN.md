---
phase: 05-p-ginas-admin-frontend
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/admin/nfe.ts
  - frontend/src/pages/admin/Entregas.tsx
  - frontend/src/pages/admin/Entregas.css
autonomous: true
requirements: ["F11", "F12"]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Entrega manual: tabela editável começa vazia; cada linha = select de item (GET /itens) + quantidade + ação; alterar quantidade de linha parseada marca ação 'alterado'; remover linha a mantém visível riscada com badge 'Excluído' — nunca some silenciosamente (D-10, F11)"
    - "Modal 'Justificativa obrigatória' abre antes do submit quando há linhas 'alterado'/'excluído' sem justificativa; textarea com borda --amarelo até preencher (.planning/PROJECT.md §5.4); copy: 'Alterar ou excluir um item da nota exige justificativa. Esse registro faz parte da prestação de contas do PNAE.'; Confirmar desabilitado enquanto vazio"
    - "Botão 'Confirmar recebimento' desabilitado com 0 linhas (backend retorna 422 — guarda de UI) (cobre zero-one-many do UI-SPEC)"
    - "Upload de XML NF-e popula a tabela editável com ação 'recebido' por padrão; item não reconhecido fica destacado com borda --amarelo + helper 'Item não reconhecido — selecione o item correspondente no estoque.' e select manual (F12, D-11; cobre partial do UI-SPEC)"
    - "XML malformado ou sem estrutura de NF-e exibe 'Não foi possível ler o arquivo. Verifique se é um XML de NF-e válido.' sem quebrar a página (cobre error do UI-SPEC)"
    - "Após confirmar: mensagem do backend exibida verbatim, listagem refeita (GET /entregas) e saldos sobem na página Itens (F11/F12, verificado manualmente)"
    - "Listagem de entregas (Data/hora, Itens (qtd), Registrado por, Ações) com vazio 'Nenhuma entrega registrada' + 'Registre a primeira entrega manualmente ou importe o XML da nota fiscal.'; 'Ver detalhes' abre modal 'Entrega #{id}' via GET /entregas/{id} com badges de ação e justificativas em parágrafo com quebra de linha"
    - { statement: "Tabela editável de entregas (Item/Quantidade/Ação/remover) e modal de detalhe não quebram o layout em larguras estreitas — .tabela-container com scroll horizontal e modal max-height 90vh com overflow-y auto (verificado visualmente em F11)", verification: backstop }
    - { statement: "Justificativas longas renderizam como parágrafo com quebra de linha no modal de detalhe, sem truncar dados de auditoria (verificado visualmente em F11)", verification: backstop }
  artifacts:
    - frontend/src/pages/admin/nfe.ts — parser NF-e (parseNfe + normalização de texto), .ts puro
    - frontend/src/pages/admin/Entregas.tsx — página completa: listagem, entrada manual, fluxo XML, modais de justificativa e detalhe (substitui scaffolding)
    - frontend/src/pages/admin/Entregas.css — estilos co-localizados com badges de ação
  key_links:
    - "Entregas.tsx → fetchJson('/entregas' | '/entregas/{id}' | POST '/entregas') — EntregaCreate { itens: EntregaItemRequest[] }"
    - "nfe.ts → fast-xml-parser XMLParser({ ignoreAttributes: false }) — nfeProc.NFe.infNFe.det normalizado objeto|array"
    - "Linha editada → AcaoEntrega de types.ts ('recebido' | 'alterado' | 'excluído' com acento) — nunca strings inline"
---

<objective>
Entregar a página mais complexa da fase: Entregas com entrada manual auditada (justificativa obrigatória ao alterar/excluir — exigência de prestação de contas PNAE) e importação de XML NF-e parseada no frontend (D-11), alimentando o mesmo fluxo de revisão humana.

Purpose: Entregas é a entrada de estoque do sistema; a auditoria de divergências é requisito PNAE e o backend rejeita (400) qualquer alteração/exclusão sem justificativa.
Output: `/admin/entregas` funcional para admin + secretaria (F11, F12, D-10, D-11).

Nota registrada: spec-less probe fallback pulado visivelmente — fase sem REQ-IDs; baseline = ROADMAP fase 5 + F6–F12.
</objective>

<execution_context>
@/home/paulo/.opencode/gsd-core/workflows/execute-plan.md
@/home/paulo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-p-ginas-admin-frontend/05-CONTEXT.md
@.planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md
@.planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md
@.planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md
@.planning/phases/05-p-ginas-admin-frontend/05-01-SUMMARY.md

@frontend/src/types.ts
@frontend/src/api.ts
@frontend/src/pages/admin/constants.ts
@frontend/src/pages/admin/Usuarios.tsx
@frontend/src/pages/PainelCozinha.css
@frontend/src/components/Layout.css
@frontend/src/index.css
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Utilitário nfe.ts — parser NF-e (XML → linhas da tabela editável) com normalizações</name>
  <files>frontend/src/pages/admin/nfe.ts</files>
  <read_first>
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção "Parse NF-e no frontend (fast-xml-parser)" INTEGRA — API verificada empiricamente, tabela de mapeamento campo→linha, três armadilhas: det objeto|array, raiz nfeProc|NFe, uCom ≠ unidade oficial)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `nfe.ts` — NO ANALOG; contrato: match xProd↔Item.nome normalizado, sem match → seleção manual, parser.parse lança em XML malformado)
    - frontend/src/types.ts (interface Item do plano 01)
    - frontend/src/auth-context.ts (precedente de arquivo .ts puro com header comment — regra react-refresh)
    - frontend/package.json (fast-xml-parser ^5.10.1 já instalado — NÃO instalar nada)
  </read_first>
  <action>
    Criar `frontend/src/pages/admin/nfe.ts` (D-11, D-13) — arquivo .ts puro sem JSX, com header comment explicando o propósito e o split. Imports: `import { XMLParser } from 'fast-xml-parser'` + `import type { Item } from '../../types'`.

    Exportar:
    - `export interface LinhaNfe { codigo: string; descricao: string; quantidade: number; unidadeNf: string; valorUnitario: number | null; itemId: number | null }` (itemId null = não reconhecido → seleção manual)
    - `export interface NfeParseResult { linhas: LinhaNfe[]; numeroNota: string | null; emitente: string | null }`
    - `export function normalizarTexto(s: string): string` — lowercase, trim, remoção de acentos via `s.normalize('NFD').replace(/[\u0300-\u036f]/g, '')`
    - `export function parseNfe(xmlText: string, itens: Item[]): NfeParseResult`

    Implementação de parseNfe: `new XMLParser({ ignoreAttributes: false })` → `parser.parse(xmlText)` (lança em XML malformado — o chamador captura). Raiz tolerante: `const nfe = doc.nfeProc?.NFe ?? doc.NFe`; se ausente ou sem `infNFe`, lançar `new Error('XML sem estrutura de NF-e')`. Normalizar `det`: `const dets = Array.isArray(infNFe.det) ? infNFe.det : [infNFe.det]` (armadilha #1). Por det, ler `prod`: `cProd` (String), `xProd` (String), `qCom` (Number), `uCom` (String), `vUnCom` (Number ou null). Match de item: `itens.find(i => normalizarTexto(i.nome) === normalizarTexto(xProd))` → itemId ou null. Cabeçalho: `infNFe.ide?.nNF` e `infNFe.emit?.xNome` (Strings ou null). `uCom` é apenas exibição — NÃO é a unidade oficial do estoque (armadilha #3); qCom pode estar em unidade diferente (CX/PCT/UN) → a tabela editável é revisão humana obrigatória, não importação cega. Filtrar dets sem prod. Retornar `{ linhas, numeroNota, emitente }`.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "XMLParser" src/pages/admin/nfe.ts && grep -c "Array.isArray" src/pages/admin/nfe.ts && grep -c "nfeProc?.NFe ?? doc.NFe" src/pages/admin/nfe.ts && grep -c "normalizarTexto" src/pages/admin/nfe.ts && node --input-type=module -e "import { XMLParser } from 'fast-xml-parser'; const p = new XMLParser({ ignoreAttributes: false }); const d = p.parse('<nfeProc><NFe><infNFe><det><prod><cProd>1</cProd><xProd>ARROZ</xProd><qCom>10</qCom><uCom>KG</uCom><vUnCom>5.5</vUnCom></prod></det><ide><nNF>42</nNF></ide><emit><xNome>Fornecedor</xNome></emit></infNFe></NFe></nfeProc>'); const det = d.nfeProc.NFe.infNFe.det; if (det.prod.xProd !== 'ARROZ') process.exit(1); console.log('parse-ok');"</automated>
  </verify>
  <done>
    - Build + lint exit 0; nfe.ts é .ts puro (sem JSX)
    - Greps ≥1: XMLParser, Array.isArray (normalização det), fallback de raiz nfeProc|NFe, normalizarTexto
    - Smoke node confirma o shape do parser instalado (det.prod.{cProd,xProd,qCom,uCom,vUnCom}) — saída "parse-ok"
    - Comportamento coberto de ponta a ponta pelo F12 manual (parse → tabela → confirmar)
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "export function parseNfe" frontend/src/pages/admin/nfe.ts` ≥ 1
    - `grep -c "export function normalizarTexto" frontend/src/pages/admin/nfe.ts` ≥ 1
    - `grep -c "Array.isArray" frontend/src/pages/admin/nfe.ts` ≥ 1
    - `grep -c "from 'fast-xml-parser'" frontend/src/pages/admin/nfe.ts` ≥ 1
    - Comando node do verify imprime "parse-ok"
    - Comportamento: nota com 1 item (det objeto) e nota com N itens (det array) produzem linhas equivalentes; XML sem NFe lança erro capturável
  </acceptance_criteria>
  <reversibility rating="reversible">Arquivo utilitário novo, puro e isolado; reversível por git revert.</reversibility>
</task>

<task type="auto">
  <name>Entregas — listagem, escolha de fluxo, entrada manual editável e modal de justificativa obrigatória</name>
  <files>frontend/src/pages/admin/Entregas.tsx, frontend/src/pages/admin/Entregas.css</files>
  <read_first>
    - frontend/src/pages/admin/Entregas.tsx (arquivo modificado — scaffolding do plano 01, substituído integralmente)
    - frontend/src/pages/admin/Usuarios.tsx (template CRUD — modal, form, role="alert", destrutivo)
    - frontend/src/types.ts (EntregaResumo, EntregaDetalhe, ItemEntrega, EntregaItemRequest, AcaoEntrega, Item — plano 01)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seções 5.7 e "Entregas Flow" INTEGRAS — valida-tudo-antes-de-gravar, regras recebido/alterado/excluído, 400/422, fluxo 1-6)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `Entregas.tsx` — badges de ação, modal de justificativa, regras de fluxo)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.7 INTEGRA — choice modal, colunas, justificativa, CTA, erros; Color — mapeamento dos badges; §5.4 campo de auditoria)
    - frontend/src/components/Layout.css (linhas 134-157: molde pill dos badges)
    - frontend/src/pages/PainelCozinha.css (linhas 152-201: estrutura modal — re-tokenizar overlay rgba(18, 76, 15, 0.35))
  </read_first>
  <action>
    Substituir o scaffolding pela página real (F11, D-01, D-03, D-10). Estrutura de estados: `entregas: EntregaResumo[]`, `itens: Item[]` (catálogo), `carregando`, `erro`, `fluxo: 'nenhum' | 'escolha' | 'editando'` (choice modal → editor), `linhas: LinhaEdicao[]` onde `interface LinhaEdicao { itemId: number | null; quantidade: number; acao: AcaoEntrega; justificativa: string | null; descricaoNf?: string; unidadeNf?: string; removida?: boolean }` (interface local ou em nfe.ts/Entregas.tsx não-exportada — react-refresh), `justificativaPendente: { index: number; acao: 'alterado' | 'excluído' } | null`, `salvando`, `detalhe: EntregaDetalhe | null`.

    Listagem (estado inicial): fetch paralelo de `fetchJson<EntregaResumo[]>('/entregas')` e `fetchJson<Item[]>('/itens')`. Tabela (Data/hora, Itens (qtd_itens), Registrado por (id_usuario), Ações) com vazio "Nenhuma entrega registrada" + "Registre a primeira entrega manualmente ou importe o XML da nota fiscal.". Ação "Ver detalhes" → `fetchJson<EntregaDetalhe>('/entregas/' + id)` → modal "Entrega #{id}": itens com nome, quantidade, badge de ação (`badge-acao` + `badge-recebido`: fundo --verde-vivo/texto branco; `badge-alterado`: fundo --amarelo/texto --texto; `badge-excluido`: fundo --erro-fundo/texto --erro) e justificativa como parágrafo com quebra de linha quando presente (backstop long-text). Header da página: `<h1>` "Entregas" + CTA "Nova entrega" → `fluxo = 'escolha'`.

    Choice modal: "Lançamento manual" (primário → linhas vazias, fluxo 'editando') / "Importar XML (NF-e)" (secundário — input de arquivo da task 3). Estrutura overlay/content re-tokenizada.

    Editor (fluxo 'editando'): tabela editável com colunas Item (select de `itens` — placeholder disabled "-- Selecione o item --"), Quantidade (number, step="0.1"), Ação (badge somente-leitura), Remover. Botão "Adicionar item" (secundário) → nova linha `{ itemId: null, quantidade: 0, acao: 'recebido', justificativa: null }`. Regras de edição (D-10): mudar `quantidade` de uma linha que veio do XML (ou já confirmada com item) → marca `acao: 'alterado'` e abre o modal de justificativa; "Remover" NÃO deleta a linha — marca `removida: true` + `acao: 'excluído'` e abre o modal de justificativa (a linha permanece visível, riscada via CSS `text-decoration: line-through`, com badge "Excluído"); um botão "Desfazer" na linha removida reverte para o estado anterior. Linhas novas (manuais) ainda não parseadas podem ser removidas de verdade (nunca existiram na nota — discricionariedade; o que o backend exige é que o payload final contenha as linhas alterado/excluído com justificativa).

    Modal de justificativa (D-10, UI-SPEC 5.7): título "Justificativa obrigatória"; body 14px: "Alterar ou excluir um item da nota exige justificativa. Esse registro faz parte da prestação de contas do PNAE."; textarea com label "Justificativa" e classe `.campo-auditoria` — borda 1px solid var(--amarelo) ATÉ ser preenchido (.planning/PROJECT.md §5.4); "Confirmar" (primário) desabilitado enquanto o texto trim for vazio → grava na linha (`justificativa`) e fecha. Cancelar a justificativa reverte a alteração que a disparou (quantidade volta / linha desmarca — discricionariedade simples e segura).

    Submit "Confirmar recebimento" (primário): desabilitado quando `linhas.filter(l => !l.removida).length === 0` E quando total de linhas for 0 (backend 422 — guarda de UI), e quando qualquer linha tiver `itemId === null` ou quantidade ≤ 0 (helper visível). Monta `EntregaCreate`: `{ itens: linhas.map(l => ({ item_id: l.itemId, quantidade: l.quantidade, acao: l.acao, justificativa: l.justificativa })) }` tipado como EntregaItemRequest[] — ações SEMPRE via AcaoEntrega de types.ts ('excluído' com acento; nunca string inline). POST /entregas → sucesso: exibe `mensagem` do backend verbatim (feedback inline ou no modal — discricionariedade), `fluxo = 'nenhum'`, refetch de '/entregas' e '/itens' (saldos sobem); erro: ApiError.message verbatim em role="alert" (ex.: "Item 3: ação 'alterado' exige justificativa"). Erro 401: "Sua sessão expirou. Entre novamente.".

    `Entregas.css` novo: vocabulário compartilhado + `.badge-acao`/`.badge-recebido`/`.badge-alterado`/`.badge-excluido` (pill 4px 8px/999px/13px/600/uppercase, mapeamento exato da seção Color do UI-SPEC), `.campo-auditoria`, `.linha-removida` (line-through + --texto-suave), `.tabela-container` com overflow-x auto (backstop overflow) — somente tokens var(--*). D-13: import type; sem enums.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "Justificativa obrigatória" src/pages/admin/Entregas.tsx && grep -c "prestação de contas do PNAE" src/pages/admin/Entregas.tsx && grep -c "campo-auditoria" src/pages/admin/Entregas.tsx && grep -c "badge-excluido" src/pages/admin/Entregas.css && grep -c "Nenhuma entrega registrada" src/pages/admin/Entregas.tsx && grep -c "excluído" src/pages/admin/Entregas.tsx</automated>
  </verify>
  <done>
    - Build + lint exit 0
    - Listagem + choice modal + entrada manual com tabela editável funcionais
    - Modal de justificativa com copy PNAE, textarea --amarelo até preencher, Confirmar bloqueado vazio (greps ≥1)
    - Linha removida permanece riscada com badge "Excluído"; "Confirmar recebimento" desabilitado com 0 linhas
    - POST /entregas monta EntregaItemRequest[] com AcaoEntrega tipada (grep ≥1 para a string acentuada)
    - F11 (manual, /gsd-verify-work): alterar qtd sem justificativa → bloqueado na UI; com justificativa → 200; saldo sobe em Itens; entrega na listagem
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "fetchJson<EntregaResumo[]>('/entregas')" frontend/src/pages/admin/Entregas.tsx` ≥ 1
    - `grep -c "method: 'POST'" frontend/src/pages/admin/Entregas.tsx` ≥ 1
    - `grep -c "Justificativa obrigatória" frontend/src/pages/admin/Entregas.tsx` ≥ 1
    - `grep -c "border: 1px solid var(--amarelo)" frontend/src/pages/admin/Entregas.css` ≥ 1
    - `grep -c "line-through" frontend/src/pages/admin/Entregas.css` ≥ 1
    - `grep -c "AcaoEntrega" frontend/src/pages/admin/Entregas.tsx` ≥ 1
    - Comportamento: remover linha a mantém visível e marcada; alterar quantidade marca 'alterado'; submit sem justificativa nunca é disparado pela UI
  </acceptance_criteria>
  <reversibility rating="reversible">Substituição de scaffolding por página nova + CSS novo; isolada em Entregas.</reversibility>
</task>

<task type="auto">
  <name>Entregas — fluxo de upload XML (parse → revisão com destaque de não-reconhecidos → mesmo submit da manual)</name>
  <files>frontend/src/pages/admin/Entregas.tsx</files>
  <read_first>
    - frontend/src/pages/admin/Entregas.tsx (arquivo modificado — página da task 2, fluxo XML adicionado)
    - frontend/src/pages/admin/nfe.ts (parseNfe, LinhaNfe, normalizarTexto — task 1)
    - .planning/phases/05-p-ginas-admin-frontend/05-RESEARCH.md (seção "Entregas Flow" passo 3 e "Parse NF-e" — upload pattern, linhas pré-preenchidas com acao 'recebido', mesma revisão da manual)
    - .planning/phases/05-p-ginas-admin-frontend/05-PATTERNS.md (seção `Entregas.tsx` — upload pattern com input file + arquivo.text() + try/catch)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (Copywriting 5.7 — erro de XML, helper de item não reconhecido, "NF nº {nNF} — {emitente}"; Color — destaque --amarelo em linhas não reconhecidas)
  </read_first>
  <action>
    Adicionar o fluxo XML (F12, D-11) à página da task 2. No choice modal, a opção "Importar XML (NF-e)" renderiza `<input type="file" accept=".xml">`; handler: `const arquivo = e.target.files?.[0]; if (!arquivo) return; const texto = await arquivo.text();` → `try { const resultado = parseNfe(texto, itens); ... } catch { setErro('Não foi possível ler o arquivo. Verifique se é um XML de NF-e válido.'); }`.

    Sucesso do parse: converter `resultado.linhas` em `LinhaEdicao[]` — `{ itemId: linha.itemId, quantidade: linha.quantidade, acao: 'recebido', justificativa: null, descricaoNf: linha.descricao, unidadeNf: linha.unidadeNf }` — e `fluxo = 'editando'` (a partir daí é EXATAMENTE o mesmo caminho da manual, D-11: revisão humana obrigatória). Cabeçalho informativo acima da tabela quando presentes: "NF nº {numeroNota} — {emitente}". Linhas com `itemId === null`: destaque visual com borda --amarelo na linha + helper "Item não reconhecido — selecione o item correspondente no estoque." (a descrição da NF `descricaoNf` e a unidade `unidadeNf` visíveis para orientar a escolha manual; uCom NÃO é a unidade oficial — RESEARCH armadilha #3). O submit da task 2 já bloqueia linhas com itemId null.

    Regras já herdadas do editor: editar quantidade após parse → 'alterado' + justificativa; remover → 'excluído' + justificativa; badge por ação; "Confirmar recebimento" idem. Resetar o input de arquivo após o parse (e.target.value = '') para permitir re-upload do mesmo arquivo. Garantir que um segundo upload substitui as linhas atuais (ou confirmar com o usuário — discricionariedade simples: substituir, pois o fluxo ainda não foi submetido).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && grep -c "parseNfe" src/pages/admin/Entregas.tsx && grep -c "accept=\".xml\"" src/pages/admin/Entregas.tsx && grep -c "Não foi possível ler o arquivo" src/pages/admin/Entregas.tsx && grep -c "Item não reconhecido" src/pages/admin/Entregas.tsx && grep -c "NF nº" src/pages/admin/Entregas.tsx</automated>
  </verify>
  <done>
    - Build + lint exit 0
    - Upload .xml → parseNfe → tabela populada com acao 'recebido' e fluxo 'editando' (greps ≥1)
    - Não-reconhecidos: borda --amarelo + helper de seleção manual; erro de XML com copy amigável
    - Cabeçalho "NF nº {nNF} — {emitente}" quando disponível
    - F12 (manual, /gsd-verify-work): upload de NF-e real ou sintética → tabela populada → editar com justificativa → confirmar → saldos atualizados
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0
    - `grep -c "parseNfe" frontend/src/pages/admin/Entregas.tsx` ≥ 1
    - `grep -c "accept=\".xml\"" frontend/src/pages/admin/Entregas.tsx` ≥ 1
    - `grep -c "Item não reconhecido — selecione o item correspondente no estoque." frontend/src/pages/admin/Entregas.tsx` ≥ 1
    - `grep -c "Não foi possível ler o arquivo. Verifique se é um XML de NF-e válido." frontend/src/pages/admin/Entregas.tsx` ≥ 1
    - Comportamento: após upload, o fluxo de submit é o mesmo da entrada manual (mesmas regras de justificativa e bloqueios)
  </acceptance_criteria>
  <reversibility rating="reversible">Adição de fluxo de upload na página; reversível por git revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arquivo XML → Browser | Conteúdo não-confiável parseado no cliente (fast-xml-parser) |
| Browser → API | Payload EntregaCreate altera saldo de estoque e trilha de auditoria |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05-01 | Tampering | XML NF-e malicioso/malformado | high | mitigate | fast-xml-parser (sem eval, sem execução); parser.parse lança → capturado com copy amigável; dados parseados só entram em inputs controlados e React escapa a renderização; accept=".xml"; revisão humana obrigatória antes do submit (D-11) |
| T-05-05-02 | Tampering | Bypass da justificativa (payload forjado) | medium | mitigate | Backend é a autoridade: 400 sem justificativa em 'alterado'/'excluído' e valida-tudo-antes-de-gravar (RESEARCH); UI é conveniência — modal bloqueia submit, e o 400 é exibido verbatim se forçado |
| T-05-05-03 | Denial of Service | XML gigante | low | mitigate | Parse síncrono de arquivo local; aceitar apenas .xml; erro de parse capturado; sem upload ao servidor (parse no cliente) |
| T-05-05-04 | Repudiation | Entrega sem trilha | medium | mitigate | Backend registra id_usuario via token (D-03 — nunca id_usuario no body); justificativa obrigatória persistida por item divergente; detalhe consultável via GET /entregas/{id} no modal |
| T-05-05-SC | Tampering | npm installs | high | mitigate | Nenhum pacote novo neste plano (fast-xml-parser 5.10.1 já instalado e auditado OK no RESEARCH); nenhum npm install permitido |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint` — zero erros/warnings por task
- Smoke node do parser (task 1) + greps de justificativa/badges/upload (tasks 2-3)
- Manual (fase /gsd-verify-work): F11 (manual com justificativa), F12 (XML → editar → confirmar → estoque atualizado). Preparar XML NF-e sintético de 2-3 itens para o F12 (RESEARCH Wave 0 Gaps).
</verification>

<success_criteria>
- `/admin/entregas` completo: listagem + detalhe, entrada manual auditada, importação XML com revisão
- Nenhuma remoção silenciosa de linha; nenhum submit sem justificativa quando exigida
- Ação sempre tipada via AcaoEntrega (acento correto); nenhum arquivo fora de nfe.ts/Entregas.tsx/Entregas.css tocado
</success_criteria>

<output>
Create `.planning/phases/05-p-ginas-admin-frontend/05-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 05)

**Types (frontend/src/pages/admin/nfe.ts):** `LinhaNfe`, `NfeParseResult`

**Functions (frontend/src/pages/admin/nfe.ts):** `parseNfe`, `normalizarTexto`

**Components:** `Entregas` (página real — substitui scaffolding)

**CSS classes introduzidas:** `.badge-acao`, `.badge-recebido`, `.badge-alterado`, `.badge-excluido`, `.campo-auditoria`, `.linha-removida` (+ reutilização do vocabulário compartilhado)

**New file paths:** `frontend/src/pages/admin/nfe.ts`, `frontend/src/pages/admin/Entregas.css` (Entregas.tsx já existe como scaffolding do plano 01, substituído)
