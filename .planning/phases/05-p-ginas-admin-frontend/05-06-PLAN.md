---
phase: 05-p-ginas-admin-frontend
plan: 06
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04", "05-05"]
files_modified:
  - .planning/phases/05-p-ginas-admin-frontend/05-VALIDATION.md
autonomous: true
requirements: ["D-12"]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "npm run build (tsc + vite) e npm run lint saem com ZERO erros/warnings de frontend/ (critérios 1 e 2 da ROADMAP fase 5, D-12)"
    - "Naquele momento pré-05-07, pytest backend/tests/ passava com 77 testes verdes de backend/ com venv ativo — guarda de regressão das 23 rotas consumidas"
    - "Nenhuma página admin contém: endereços de API embutidos em código, literais de cor hex nos CSS (somente var(--*)), caixas de diálogo nativas do navegador, atributos de estilo inline, ou a string de ação sem acento em código"
    - "Nenhum scaffolding do plano 01 restou — as 5 páginas foram integralmente substituídas pelas implementações reais dos planos 02–05"
  artifacts:
    - .planning/phases/05-p-ginas-admin-frontend/05-VALIDATION.md — mapa por task marcado com o status final
  key_links:
    - "Gates automatizados (build/lint/pytest) → critérios de saída da fase → /gsd-verify-work executa o checklist manual F6–F12"
---

<objective>
Executar os portões de saída da fase como um plano explícito: typecheck+bundle, lint zero, regressão backend (77 testes no baseline histórico deste plano, 94 no fechamento de 05-07) e varredura de anti-padrões do UI-SPEC sobre o código-fonte das páginas admin.

Purpose: D-12 trava build+lint limpos como critério de saída; o checklist F6–F12 roda depois via /gsd-verify-work. Este plano garante que tudo que é automatizável está verde antes do aceite manual.
Output: Fase verde nos gates automatizados + VALIDATION.md com o status por task.

Nota registrada: spec-less probe fallback pulado visivelmente — fase sem REQ-IDs (projeto sem REQUIREMENTS.md); nenhum predicado de probe gerado nesta execução. Baseline de cobertura: goal + critérios de sucesso da ROADMAP fase 5 e IDs F6–F12.
</objective>

<execution_context>
@/home/paulo/.opencode/gsd-core/workflows/execute-plan.md
@/home/paulo/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-p-ginas-admin-frontend/05-CONTEXT.md
@.planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md
@.planning/phases/05-p-ginas-admin-frontend/05-VALIDATION.md

@frontend/src/pages/admin
@frontend/src/types.ts
@frontend/src/App.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Portões de saída — build, lint, regressão pytest, varredura de anti-padrões e status do VALIDATION.md</name>
  <files>.planning/phases/05-p-ginas-admin-frontend/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-p-ginas-admin-frontend/05-VALIDATION.md (arquivo modificado — tabela "Per-Task Verification Map" marcada com o status final por task ID)
    - .planning/phases/05-p-ginas-admin-frontend/05-UI-SPEC.md (seção "Anti-patterns" e "Forbidden combinations" — lista exata do que varrer)
    - AGENTS.md (comandos corretos: pytest roda de backend/ com venv; DATABASE_URL relativa)
    - frontend/src/App.tsx (conferir as 6 rotas novas presentes)
    - frontend/src/pages/admin (listar os arquivos finais: 7 páginas + 7 CSS + constants.ts + nfe.ts)
  </read_first>
  <action>
    Executar e registrar os gates (D-12), nesta ordem:

    1) Build + lint: `cd frontend && npm run build && npm run lint` — ambos exit 0, zero warnings. Se falhar, corrigir a causa no arquivo indicado (nunca suprimir regra de lint nem afrouxar tsconfig).

    2) Regressão backend: `cd backend && source venv/bin/activate && pytest tests/ -q` — 77 passed. Nenhum arquivo de backend foi tocado nesta fase; uma falha aqui indica ambiente (ex.: banco estray na raiz por rodar uvicorn do diretório errado) e não código — investigar antes de prosseguir.

    3) Varredura de anti-padrões (UI-SPEC) sobre os arquivos novos — cada grep deve retornar ZERO ocorrências (exit 1 do grep = passa):
       a) Endereços de API embutidos: buscar o literal de host+porta do backend local em `frontend/src/pages/admin/` — toda chamada passa por fetchJson/fetchWithAuth de src/api.ts (D-03).
       b) Literais de cor hex (regex `#[0-9a-fA-F]{3,8}`) em `frontend/src/pages/admin/*.css` — somente tokens var(--*) são permitidos (D-02); rgba(18, 76, 15, 0.35) do overlay é aceito por não ser hex.
       c) Caixas de diálogo nativas do navegador (chamada direta a diálogo global) em `frontend/src/pages/admin/*.tsx` — feedback é sempre via estado erro + role="alert".
       d) Atributos de estilo inline (propriedade style em JSX) em `frontend/src/pages/admin/*.tsx` — tudo via classes co-localizadas.
       e) A string de ação de exclusão SEM acento entre aspas em `frontend/src/pages/admin/` — a forma válida é com acento e vem do union AcaoEntrega de types.ts (cuidado: a classe CSS `.badge-excluido` sem acento é legítima — restringir a busca a ocorrências ENTRE aspas simples ou duplas).
       f) Chamadas fetch cruas (função global de rede) em `frontend/src/pages/admin/*.tsx` — apenas fetchJson/fetchWithAuth são permitidos; o padrão proibido é a função global seguida de parêntese (cuidado: `fetchJson(` e `fetchWithAuth(` são legítimos — o grep deve mirar a palavra exata seguida de parêntese, sem sufixo alfanumérico).
       g) Injeção HTML direta do React (propriedade perigosa de innerHTML) em `frontend/src/pages/admin/` — proibida (T-05-05-01).
       h) Scaffolding residual: cada uma das 5 páginas substituídas deve conter sua marca de implementação real — grep ≥1 para "fetchJson" em Itens.tsx, Cardapio.tsx, Receitas.tsx, Planejamento.tsx e Entregas.tsx; e o marcador de scaffolding (div com apenas h1) não deve mais ser o conteúdo completo de nenhum deles.

    4) Conferir rotas: App.tsx contém as 6 rotas novas com os perfis corretos (grep ≥1 para cada path de /admin/usuarios, /admin/itens, /admin/cardapio, /admin/receitas/:id, /admin/planejamento, /admin/entregas; ≥2 ocorrências do array de perfis admin+secretaria).

    5) Atualizar `.planning/phases/05-p-ginas-admin-frontend/05-VALIDATION.md`: na tabela "Per-Task Verification Map", marcar ✅ green nas linhas dos task IDs 05-01-01, 05-01-02, 05-02-01, 05-02-02, 05-03-01, 05-03-02, 05-04-01, 05-04-02, 05-05-01, 05-05-02, 05-05-03 e 05-06-01 (esta task) — uma linha por task, com o comando automatizado correspondente; marcar os checkboxes de "Validation Sign-Off" que estiverem efetivamente cumpridos (tasks com verify automatizado; continuidade de amostragem; Wave 0 sem gaps).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && cd ../backend && source venv/bin/activate && pytest tests/ -q && cd .. && ! grep -rn "127\.0\.0\.1" frontend/src/pages/admin/ && ! grep -rEn "#[0-9a-fA-F]{3,8}\b" frontend/src/pages/admin/*.css && ! grep -rnE "(alert|confirm|prompt)\(" frontend/src/pages/admin/*.tsx && ! grep -rn "style={{" frontend/src/pages/admin/*.tsx && ! grep -rnE "[\"']excluido[\"']" frontend/src/pages/admin/ && ! grep -rnE "(^|[^a-zA-Z])fetch\(" frontend/src/pages/admin/*.tsx && ! grep -rn "dangerouslySetInnerHTML" frontend/src/pages/admin/ && for f in Itens Cardapio Receitas Planejamento Entregas; do grep -q "fetchJson" frontend/src/pages/admin/$f.tsx || exit 1; done</automated>
  </verify>
  <done>
    - Build + lint exit 0 (zero warnings) de frontend/
    - pytest: 77 passed de backend/
    - Todas as 8 varreduras de anti-padrões com zero ocorrências (ou saída vazia/exit 1)
    - Cada uma das 5 páginas da Wave 2 contém fetchJson — verificação por arquivo (loop sobre Itens, Cardapio, Receitas, Planejamento, Entregas falha se qualquer uma não contiver), provando scaffolding substituído
    - VALIDATION.md com as 12 linhas de task marcadas ✅ e sign-off atualizado
    - Fase pronta para /gsd-verify-work (checklist manual F6–F12, com XML NF-e sintético de apoio para F12)
  </done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exit 0 e `npm run lint` exit 0 (saída sem warnings)
    - `cd backend && source venv/bin/activate && pytest tests/ -q` termina com "77 passed"
    - `grep -rn "127\.0\.0\.1" frontend/src/pages/admin/` exit 1 (zero ocorrências)
    - `grep -rEn "#[0-9a-fA-F]{3,8}\b" frontend/src/pages/admin/*.css` exit 1
    - `grep -rnE "(alert|confirm|prompt)\(" frontend/src/pages/admin/*.tsx` exit 1
    - `grep -rn "style={{" frontend/src/pages/admin/*.tsx` exit 1
    - `grep -rnE "[\"']excluido[\"']" frontend/src/pages/admin/` exit 1 (forma sem acento entre aspas ausente)
    - `grep -rnE "(^|[^a-zA-Z])fetch\(" frontend/src/pages/admin/*.tsx` exit 1 (só fetchJson/fetchWithAuth; cobre `fetch(` no início da linha)
    - `for f in Itens Cardapio Receitas Planejamento Entregas; do grep -q "fetchJson" frontend/src/pages/admin/$f.tsx || exit 1; done` exit 0 (cada página verificada individualmente — uma única ocorrência em um arquivo não aprova os demais)
    - 05-VALIDATION.md contém 12 linhas de task com status ✅
  </acceptance_criteria>
  <reversibility rating="reversible">Plano de verificação + atualização de documento de planejamento; nenhuma mudança de código de produção.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gates → Codebase | Verificação read-only sobre o código produzido na fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Tampering | Regressão silenciosa de contratos de segurança (rotas sem guard, fetch cruo, injeção HTML) | high | mitigate | Varredura grep deste plano cobre: fetch cruo, injeção HTML direta, host embutido, ação sem acento; rotas conferidas com perfis |
| T-05-06-02 | Information Disclosure | Vazamento de segredos em código | low | mitigate | Nenhum segredo novo na fase (JWT vem do login; base URL de .env); grep de host embutido cobre o vazamento mais comum |
| T-05-06-SC | Tampering | npm installs | high | mitigate | Fase inteira sem pacotes novos (auditoria do RESEARCH: fast-xml-parser já instalado, OK); este plano não instala nada |
</threat_model>

<verification>
- Gate único do verify da task (build + lint + pytest + 8 varreduras + greps de rotas) — tudo deve sair verde em < 60s
- Após este plano: /gsd-verify-work para o checklist manual F6–F12 (setup: backend de backend/ com venv + npm run dev + seeds admin/admin123 e secretaria/secretaria123; XML NF-e sintético para F12)
</verification>

<success_criteria>
- Critérios 1 e 2 da ROADMAP fase 5 cumpridos (build/lint zero)
- Regressão backend verde (77 testes no baseline histórico deste plano; o fechamento posterior está registrado em `05-07-SUMMARY.md`)
- Zero anti-padrões do UI-SPEC no código da fase
- VALIDATION.md reflete o status final por task
</success_criteria>

<output>
Create `.planning/phases/05-p-ginas-admin-frontend/05-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 06)

**Documentos atualizados:** `.planning/phases/05-p-ginas-admin-frontend/05-VALIDATION.md` (mapa por task com status final)

**Símbolos novos de código:** nenhum (plano de verificação — nenhum componente, função, tipo ou classe CSS introduzida)
